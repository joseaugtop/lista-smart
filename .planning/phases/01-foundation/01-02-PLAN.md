---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - lib/main.dart
  - lib/app.dart
  - lib/routing/app_routes.dart
  - lib/routing/router_notifier.dart
  - lib/routing/app_router.dart
  - lib/features/auth/presentation/login_screen.dart
  - lib/features/home/presentation/home_screen.dart
  - lib/features/shopping_list/presentation/shopping_list_screen.dart
  - lib/features/price_comparison/presentation/price_comparison_screen.dart
  - lib/features/smart_coins/presentation/store_screen.dart
  - lib/features/price_registration/presentation/price_registration_screen.dart
  - lib/features/profile/presentation/profile_screen.dart
autonomous: false
requirements:
  - FOUN-01
  - FOUN-02
  - FOUN-03
  - FOUN-05

must_haves:
  truths:
    - "App launches on Android or iOS without runtime errors or assertion crashes (FOUN-01)"
    - "Bottom navigation bar shows exactly 5 tabs with correct labels (FOUN-03)"
    - "Switching between tabs and back preserves the Home tab's scroll position (FOUN-03)"
    - "SharedPreferences instance is available synchronously to all providers after main() bootstrap (FOUN-05)"
    - "App navigation does not crash or reset on hot reload (confirming GoRouter is stable) (FOUN-03)"
    - "StatefulShellRoute.indexedStack is used — not ShellRoute (FOUN-03)"
    - "RouterNotifier.redirect() returns null unconditionally in Phase 1 (no auth guard yet)"
  artifacts:
    - path: "lib/main.dart"
      provides: "Async bootstrap: WidgetsFlutterBinding + GoogleFonts config + Intl locale + SharedPreferences await + ProviderScope.overrides"
      contains: "sharedPreferencesProvider.overrideWithValue"
    - path: "lib/app.dart"
      provides: "ConsumerWidget root that watches goRouterProvider and returns MaterialApp.router"
      exports: ["App"]
    - path: "lib/routing/app_router.dart"
      provides: "goRouterProvider = Provider<GoRouter> with StatefulShellRoute.indexedStack and 5 branches"
      exports: ["goRouterProvider"]
    - path: "lib/routing/router_notifier.dart"
      provides: "RouterNotifier extending AutoDisposeAsyncNotifier<void> implementing Listenable"
      exports: ["RouterNotifier", "routerNotifierProvider"]
    - path: "lib/routing/app_routes.dart"
      provides: "AppRoutes string constants for all 6 routes"
      exports: ["AppRoutes"]
  key_links:
    - from: "lib/main.dart"
      to: "lib/core/persistence/shared_preferences_provider.dart"
      via: "sharedPreferencesProvider.overrideWithValue(prefs)"
      pattern: "sharedPreferencesProvider\\.overrideWithValue"
    - from: "lib/app.dart"
      to: "lib/routing/app_router.dart"
      via: "ref.watch(goRouterProvider)"
      pattern: "ref\\.watch\\(goRouterProvider\\)"
    - from: "lib/routing/app_router.dart"
      to: "lib/routing/router_notifier.dart"
      via: "refreshListenable: notifier + redirect: notifier.redirect"
      pattern: "refreshListenable"
    - from: "lib/routing/app_router.dart"
      to: "all 6 placeholder screens"
      via: "GoRoute.builder and StatefulShellBranch.routes"
      pattern: "StatefulShellRoute\\.indexedStack"
---

## Phase Goal

**As a** developer, **I want to** have a running Flutter app with 5-tab navigation and SharedPreferences bootstrap wired end-to-end, **so that** I can visually confirm the skeleton works before building any feature on top of it.

<objective>
Plan 02 wires everything built in Plan 01 into a runnable app: the async bootstrap in main(), the ConsumerWidget root, the GoRouter as a Riverpod Provider, the RouterNotifier Listenable bridge, and the StatefulShellRoute.indexedStack with 5 placeholder screens. After this plan executes, the app runs on a device/simulator and a human can tab between 5 screens with scroll state preserved.

Purpose: Close the Walking Skeleton — prove the architecture works end-to-end before adding any feature content.

Output: lib/main.dart, lib/app.dart, lib/routing/ (3 files), 6 placeholder screen files.

Note: autonomous: false because Task 3 requires a human to launch the app on a real device or simulator to verify visual output, scroll preservation, and absence of runtime errors. Tasks 1 and 2 are fully autonomous (type="auto"); the checkpoint applies only to Task 3.
</objective>

<execution_context>
@C:/Users/Jose Augusto/Desktop/Unesc/Fase 5/Desenvolvimento Mobile/gsd/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jose Augusto/Desktop/Unesc/Fase 5/Desenvolvimento Mobile/gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-SKELETON.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Routing layer — AppRoutes + RouterNotifier + GoRouter provider</name>
  <read_first>
    Read .planning/phases/01-foundation/01-RESEARCH.md sections:
    - "Pattern 4: StatefulShellRoute.indexedStack" (full GoRouter Provider code + ScaffoldWithBottomNav)
    - "Pattern 5: RouterNotifier — Listenable Bridge" (RouterNotifier class + routerNotifierProvider)
    - "Pattern 7: AppRoutes Constants"
    - "Anti-Patterns to Avoid" (GoRouter in build(), ref.watch inside goRouterProvider, ShellRoute)
    - Assumption A2 (lucide icon names may differ in 0.257.0)
    Read .planning/phases/01-foundation/01-01-SUMMARY.md (confirm models and SharedPreferences provider exist).
    Fallback: if 01-01-SUMMARY.md does not exist, verify Plan 01 prerequisites directly — run `flutter test test/models/ test/repositories/` (must exit 0) and confirm `lib/core/persistence/shared_preferences_provider.dart` exists before proceeding.
  </read_first>
  <files>
    lib/routing/app_routes.dart,
    lib/routing/router_notifier.dart,
    lib/routing/app_router.dart,
    lib/features/auth/presentation/login_screen.dart,
    lib/features/home/presentation/home_screen.dart,
    lib/features/shopping_list/presentation/shopping_list_screen.dart,
    lib/features/price_comparison/presentation/price_comparison_screen.dart,
    lib/features/smart_coins/presentation/store_screen.dart,
    lib/features/price_registration/presentation/price_registration_screen.dart,
    lib/features/profile/presentation/profile_screen.dart
  </files>
  <action>
    1. Create `lib/routing/app_routes.dart`:
       ```
       abstract class AppRoutes {
         static const String login        = '/login';
         static const String home         = '/home';
         static const String shoppingList = '/shopping-list';
         static const String comparison   = '/comparison';
         static const String store        = '/store';
         static const String profile      = '/profile';
       }
       ```
       No imports needed.

    2. Create `lib/routing/router_notifier.dart`. The class MUST:
       - Extend `AutoDisposeAsyncNotifier<void>` (from flutter_riverpod)
       - Implement `Listenable` (from flutter/foundation.dart)
       - Declare `VoidCallback? _routerListener;`
       - In `build()`: call `ref.listenSelf((_, __) => _routerListener?.call());` and return immediately (no auth watch in Phase 1 — Phase 2 will add it)
       - Implement `redirect(BuildContext context, GoRouterState state)` that returns `null` unconditionally in Phase 1 (auth guard is Phase 2)
       - Implement `addListener(VoidCallback listener) => _routerListener = listener;`
       - Implement `removeListener(VoidCallback listener) => _routerListener = null;`
       - Declare `final routerNotifierProvider = AutoDisposeAsyncNotifierProvider<RouterNotifier, void>(RouterNotifier.new);`
       CRITICAL: The `redirect` method signature must be `String? redirect(BuildContext context, GoRouterState state)` — this is the go_router 14.x two-parameter signature.

    3. Create `lib/routing/app_router.dart`. Key rules (all are CRASH-PREVENTION):
       - Declare 5 tab `GlobalKey<NavigatorState>` constants OUTSIDE the provider: `_tab0Key` through `_tab4Key` with debugLabel 'tab0' through 'tab4', plus `_rootNavigatorKey`.
       - `goRouterProvider` is a `Provider<GoRouter>` — NEVER `StateProvider`, `NotifierProvider`, or inline widget construction.
       - Inside the provider: `final notifier = ref.watch(routerNotifierProvider.notifier);` — use `.notifier` to get the `RouterNotifier` instance without subscribing to its state (avoids provider recreation on auth state change).
       - `refreshListenable: notifier` and `redirect: notifier.redirect`.
       - Use `StatefulShellRoute.indexedStack(...)` — NOT `ShellRoute`.
       - The `builder` of `StatefulShellRoute.indexedStack` must be: `(context, state, navigationShell) => ScaffoldWithBottomNav(navigationShell: navigationShell)`.
       - Each `StatefulShellBranch` receives its corresponding `GlobalKey<NavigatorState>` as `navigatorKey:`.
       - Create `ScaffoldWithBottomNav` as a `StatelessWidget` in the same file (or a separate file in `lib/routing/`). It wraps `Scaffold(body: navigationShell, bottomNavigationBar: NavigationBar(...))`.

       ICON NAMES — Assumption A2 from RESEARCH.md flags that lucide_icons 0.257.0 icon names are not fully verified. Use these names first; if `flutter analyze` reports "undefined name", check `~/.pub-cache/hosted/pub.dev/lucide_icons-0.257.0/lib/` for the actual icon names:
       - Tab 0 (Home): `LucideIcons.home`
       - Tab 1 (Lista): `LucideIcons.shoppingCart`
       - Tab 2 (Comparar): `LucideIcons.barChart2`
       - Tab 3 (Loja): `LucideIcons.store`
       - Tab 4 (Perfil): `LucideIcons.user`
       If any name is wrong, run: `grep -r "static const IconData" ~/.pub-cache/hosted/pub.dev/lucide_icons-0.257.0/lib/ | grep -i "cart\|store\|bar\|home\|user" | head -20` and use the correct names.

       Use `NavigationBar` (Material 3) instead of `BottomNavigationBar` for visual consistency with the dark theme. Set `backgroundColor: AppColors.background` and `indicatorColor: AppColors.primary.withValues(alpha: 0.2)` on `NavigationBar`. Each `NavigationDestination` uses `Icon(LucideIcons.xxx)` for icon and selected icon.

       Tab switching: `onDestinationSelected: (index) => navigationShell.goBranch(index, initialLocation: index == navigationShell.currentIndex)`.

    4. Create placeholder screens for all 6 routes. Each placeholder is a minimal `StatelessWidget` that satisfies two requirements:
       a. Compiles without errors.
       b. Shows a `CustomScrollView` with a `SliverList` of 30 items so the Home tab's scroll preservation can be verified manually. The other 5 screens can use a simple `Scaffold` + `Center(child: Text('[ScreenName]'))`.

       HomeScreen specifically: use `Scaffold(backgroundColor: AppColors.background, body: CustomScrollView(slivers: [SliverAppBar(title: Text('Home'), backgroundColor: AppColors.background), SliverList(delegate: SliverChildBuilderDelegate((context, index) => ListTile(title: Text('Item $index'), key: ValueKey(index)), childCount: 30))]))`.

       Other screens (LoginScreen, ShoppingListScreen, PriceComparisonScreen, StoreScreen, PriceRegistrationScreen, ProfileScreen): `Scaffold(backgroundColor: AppColors.background, body: Center(child: Text('[ScreenName]', style: Theme.of(context).textTheme.headlineSmall)))`.

    5. Run `flutter analyze lib/routing/ lib/features/` — must exit 0.
  </action>
  <verify>
    <automated>flutter analyze lib/routing/ lib/features/</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze lib/routing/ lib/features/` exits 0 with "No issues found"
    - `lib/routing/app_router.dart` does NOT contain `GoRouter(` inside any class's `build(` method (grep: `build\(.*\).*\{` followed by `GoRouter\(` within 10 lines — none found)
    - `lib/routing/app_router.dart` contains `StatefulShellRoute.indexedStack` (not `ShellRoute(`)
    - `lib/routing/router_notifier.dart` contains `implements Listenable`
    - `lib/routing/router_notifier.dart` contains `AutoDisposeAsyncNotifier`
    - `lib/routing/router_notifier.dart`'s `redirect` method returns `null` unconditionally (Phase 1 — no auth guard)
    - All 6 screen files exist and are non-empty
    - `lib/features/home/presentation/home_screen.dart` contains `CustomScrollView` and `SliverList`
  </acceptance_criteria>
  <done>All routing files compile without errors. GoRouter is a Riverpod Provider. RouterNotifier implements Listenable. StatefulShellRoute.indexedStack with 5 branches is configured. 6 placeholder screens exist.</done>
</task>

<task type="auto">
  <name>Task 2: main.dart + app.dart bootstrap + human verification checkpoint prep</name>
  <read_first>
    Read .planning/phases/01-foundation/01-RESEARCH.md sections:
    - "Pattern 1: SharedPreferences Bootstrap" (complete main.dart code)
    - "Code Examples / Complete main.dart Bootstrap"
    - "Code Examples / app.dart"
    - "Pitfall 3: Google Fonts FOIT on Cold Start"
    - "Pitfall 4: RouterNotifier Redirect Loop on Async Loading"
    Read .planning/phases/01-foundation/01-SKELETON.md "Bootstrap Sequence" diagram.
  </read_first>
  <files>
    lib/main.dart,
    lib/app.dart
  </files>
  <action>
    1. Create `lib/main.dart` with the EXACT bootstrap sequence from RESEARCH.md Pattern 1:
       ```
       void main() async {
         WidgetsFlutterBinding.ensureInitialized();
         GoogleFonts.config.allowRuntimeFetching = false;
         Intl.defaultLocale = 'pt_BR';
         final prefs = await SharedPreferences.getInstance();
         runApp(
           ProviderScope(
             overrides: [
               sharedPreferencesProvider.overrideWithValue(prefs),
             ],
             child: const App(),
           ),
         );
       }
       ```
       Imports: `flutter/material.dart`, `flutter_riverpod/flutter_riverpod.dart`, `google_fonts/google_fonts.dart`, `intl/intl.dart`, `shared_preferences/shared_preferences.dart`, relative imports for `app.dart` and `core/persistence/shared_preferences_provider.dart`.
       Order matters: `WidgetsFlutterBinding.ensureInitialized()` FIRST, then `GoogleFonts.config.allowRuntimeFetching = false`, then SharedPreferences await, then `runApp`.

    2. Create `lib/app.dart`:
       ```
       class App extends ConsumerWidget {
         const App({super.key});

         @override
         Widget build(BuildContext context, WidgetRef ref) {
           final router = ref.watch(goRouterProvider);
           return MaterialApp.router(
             title: 'Lista Smart',
             theme: appTheme,
             routerConfig: router,
             debugShowCheckedModeBanner: false,
           );
         }
       }
       ```
       Imports: `flutter/material.dart`, `flutter_riverpod/flutter_riverpod.dart`, `core/theme/app_theme.dart`, `routing/app_router.dart`.
       CRITICAL: `theme:` uses `appTheme` (not `darkTheme:` — the theme is already dark; using both would create a toggle). Do NOT pass `themeMode: ThemeMode.dark` alongside a `darkTheme:` — only `theme: appTheme` is needed.

    3. Run `flutter analyze lib/main.dart lib/app.dart` — must exit 0.

    4. Run `flutter analyze lib/` — must exit 0 (full project check).

    5. Run `flutter test test/` — must exit 0 (regression guard: models and shared prefs tests from Plan 01 must still pass).
  </action>
  <verify>
    <automated>flutter analyze lib/ && flutter test test/</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze lib/` exits 0 with "No issues found"
    - `flutter test test/` exits 0, all tests pass
    - `lib/main.dart` contains `GoogleFonts.config.allowRuntimeFetching = false` before `runApp(`
    - `lib/main.dart` contains `await SharedPreferences.getInstance()` before `runApp(`
    - `lib/main.dart` contains `sharedPreferencesProvider.overrideWithValue(prefs)`
    - `lib/app.dart` contains `ref.watch(goRouterProvider)` inside `build(`
    - `lib/app.dart` does NOT contain `darkTheme:` or `themeMode:`
    - `lib/app.dart` uses `MaterialApp.router(` not `MaterialApp(`
    - `lib/app.dart` passes the dark theme to MaterialApp.router — confirmed by: `grep -r 'AppTheme\|appTheme\|darkTheme' lib/app.dart` exits 0 with at least one match (FOUN-02: theme is wired, not absent)
  </acceptance_criteria>
  <done>main.dart and app.dart are complete. Full project compiles without errors. All unit tests pass. App is ready for device/simulator launch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify app runs on device with 5-tab nav and scroll preservation</name>
  <what-built>
    main.dart + app.dart wired to GoRouter with StatefulShellRoute.indexedStack. SharedPreferences bootstrapped before runApp(). Dark theme applied globally. 5 placeholder screens in bottom nav.
  </what-built>
  <how-to-verify>
    Run the app on an Android emulator, iOS simulator, or physical device:

    ```
    flutter run
    ```

    Verify each item:

    1. App launches without crash. The screen shows a dark background (`#09090B`).
    2. Inter font renders — text looks clean, not system default (Roboto on Android).
    3. Bottom navigation bar shows 5 tabs with labels: Home, Lista, Comparar, Loja, Perfil.
    4. Tapping each tab navigates to the corresponding placeholder screen.
    5. SCROLL PRESERVATION TEST:
       a. On the Home tab, scroll the list down to item 15 or below.
       b. Tap the "Lista" tab.
       c. Tap back on the "Home" tab.
       d. The Home list is still scrolled to item 15 — it did NOT reset to the top.
       (This proves StatefulShellRoute.indexedStack is working, not ShellRoute.)
    6. No red error banners or "debugShowCheckedModeBanner" visible (banner is disabled).
    7. Check the Flutter console output — no exceptions, no "LucideIcons undefined" errors.

    If the app crashes on launch with an assertion error mentioning "brightness", that indicates the brightness pitfall was hit — check that `brightness: Brightness.dark` is inside `ColorScheme.fromSeed(`, not at `ThemeData(` level.

    If any lucide icon name causes a compile error, check the package source and update the icon names in `lib/routing/app_router.dart`.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 7 items pass.
    If issues are found, describe what failed (e.g., "scroll resets on tab switch", "icon X not found", "assertion crash on launch") and the executor will fix before re-verifying.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User → Bottom Nav | Tab index from `onDestinationSelected` is always 0-4 (framework-enforced) |
| App startup → SharedPreferences | Awaited synchronously; no async gap between init and first provider read |
| GoRouter redirect → auth state | Phase 1: redirect always returns null; no trust boundary active yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Elevation of Privilege | RouterNotifier.redirect (Phase 1) | accept | Redirect returns null unconditionally in Phase 1. Phase 2 will activate the auth guard. Documented as intentional. |
| T-02-02 | Denial of Service | SharedPreferences bootstrap (main.dart) | accept | If SharedPreferences fails to initialize (device storage full), `runApp` is never called. This is acceptable for academic scope — no retry needed. |
| T-02-03 | Tampering | GoRouter GlobalKey declarations | mitigate | GlobalKeys are declared as top-level constants outside any widget — prevents "Multiple widgets used same GlobalKey" crash by ensuring exactly one instance per key exists for the app lifetime. |
| T-02-SC | Tampering | pub package installs | mitigate | All packages carry [ASSUMED] legitimacy from Plan 01 RESEARCH.md audit. No new packages introduced in Plan 02. |
</threat_model>

<verification>
After all 3 tasks complete (including human checkpoint):
1. `flutter analyze lib/` — exits 0
2. `flutter test test/` — all tests green
3. `flutter run` — app launches, dark background visible, 5 tabs navigable, scroll preserved on Home tab
4. Manual check: `grep "StatefulShellRoute.indexedStack" lib/routing/app_router.dart` — match found
5. Manual check: `grep "refreshListenable" lib/routing/app_router.dart` — match found
6. Manual check: `grep "overrideWithValue" lib/main.dart` — match found
</verification>

<success_criteria>
- App installs and opens on Android or iOS without runtime errors (FOUN-01 fully satisfied)
- Dark background, Inter typography, primary color visible in the UI (FOUN-02 fully satisfied)
- 5-tab navigation with StatefulShellRoute.indexedStack preserves Home tab scroll state (FOUN-03 fully satisfied)
- SharedPreferences initialized before runApp() and injected via ProviderScope.overrides (FOUN-05 fully satisfied)
- All unit tests green (`flutter test test/`)
- No analysis issues (`flutter analyze lib/`)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done, recording:
- Platform tested (Android emulator / iOS simulator / physical device)
- Whether lucide icon name corrections were needed (and which names were used)
- Whether scroll preservation was confirmed
- Whether any runtime errors were encountered and how they were resolved
- Final `flutter test` and `flutter analyze` exit codes
</output>
