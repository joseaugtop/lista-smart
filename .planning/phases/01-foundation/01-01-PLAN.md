---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - assets/fonts/Inter-Regular.ttf
  - assets/fonts/Inter-Medium.ttf
  - assets/fonts/Inter-SemiBold.ttf
  - assets/fonts/Inter-Bold.ttf
  - lib/core/constants/app_colors.dart
  - lib/core/constants/app_sizes.dart
  - lib/core/theme/app_text_theme.dart
  - lib/core/theme/app_theme.dart
  - lib/core/persistence/shared_preferences_provider.dart
  - lib/features/auth/domain/user.dart
  - lib/features/profile/domain/vehicle.dart
  - lib/features/profile/domain/product.dart
  - lib/features/shopping_list/domain/cart_item.dart
  - lib/features/smart_coins/domain/coin_transaction.dart
  - test/models/models_test.dart
  - test/repositories/shared_prefs_test.dart
autonomous: true
requirements:
  - FOUN-01
  - FOUN-02
  - FOUN-04
  - FOUN-05

must_haves:
  truths:
    - "flutter pub get exits 0 with lucide_icons: ^0.257.0 resolved (FOUN-01)"
    - "ThemeData uses ColorScheme.fromSeed with brightness: Brightness.dark INSIDE fromSeed() — no assertion crash in debug builds (FOUN-02)"
    - "AppColors exposes all 8 design-system tokens as static const Color fields (FOUN-02)"
    - "Inter font is served from bundled .ttf assets; allowRuntimeFetching = false prevents FOIT (FOUN-02)"
    - "All 5 domain models round-trip toJson/fromJson without data loss — verified by unit tests (FOUN-04)"
    - "sharedPreferencesProvider sentinel throws UnimplementedError if not overridden (FOUN-05)"
    - "flutter test test/models/ exits 0 (FOUN-04)"
    - "flutter test test/repositories/ exits 0 (FOUN-05)"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Resolved dependency graph with all 6 packages at correct versions"
      contains: "lucide_icons: ^0.257.0"
    - path: "lib/core/constants/app_colors.dart"
      provides: "Design system color tokens"
      exports: ["AppColors"]
    - path: "lib/core/theme/app_theme.dart"
      provides: "Dark ThemeData without brightness assertion crash"
      exports: ["appTheme"]
    - path: "lib/core/persistence/shared_preferences_provider.dart"
      provides: "Sentinel provider for SharedPreferences injection"
      exports: ["sharedPreferencesProvider"]
    - path: "lib/features/auth/domain/user.dart"
      provides: "User domain model with toJson/fromJson/copyWith"
      exports: ["User"]
    - path: "lib/features/profile/domain/vehicle.dart"
      provides: "Vehicle domain model with toJson/fromJson/copyWith"
      exports: ["Vehicle"]
    - path: "lib/features/profile/domain/product.dart"
      provides: "Product domain model with toJson/fromJson/copyWith"
      exports: ["Product"]
    - path: "lib/features/shopping_list/domain/cart_item.dart"
      provides: "CartItem domain model with toJson/fromJson/copyWith"
      exports: ["CartItem"]
    - path: "lib/features/smart_coins/domain/coin_transaction.dart"
      provides: "CoinTransaction domain model with toJson/fromJson/copyWith and DateTime ISO 8601 serialization"
      exports: ["CoinTransaction"]
    - path: "test/models/models_test.dart"
      provides: "Unit tests for all 5 domain models (round-trip)"
      contains: "User.fromJson"
    - path: "test/repositories/shared_prefs_test.dart"
      provides: "Unit test for SharedPreferences provider injection"
      contains: "sharedPreferencesProvider"
  key_links:
    - from: "lib/core/theme/app_theme.dart"
      to: "lib/core/constants/app_colors.dart"
      via: "import AppColors"
      pattern: "AppColors\\.background"
    - from: "lib/core/theme/app_theme.dart"
      to: "lib/core/theme/app_text_theme.dart"
      via: "textTheme: appTextTheme"
      pattern: "appTextTheme"
    - from: "lib/features/shopping_list/domain/cart_item.dart"
      to: "lib/features/profile/domain/product.dart"
      via: "No direct import — CartItem is flat (productId, productName, etc.) to avoid cross-feature model coupling"
      pattern: "productId"
---

## Phase Goal

**As a** developer, **I want to** have the project scaffold, design system, and domain models in place, **so that** all subsequent features can be built on a stable, typed, and visually consistent foundation.

<objective>
Plan 01 delivers the static foundation: pubspec.yaml with all dependencies resolved, the dark design system (colors + typography), all 5 domain models with serialization, and the SharedPreferences sentinel provider. After this plan executes, `flutter pub get` exits 0, `flutter test test/` exits 0, and the design system tokens are importable by any widget in later plans.

Purpose: Establish every structural dependency that Wave 2 (navigation shell) and later phases (features) consume. This is the data contracts + visual contracts layer.

Output: pubspec.yaml, assets/fonts/, lib/core/ (colors + theme + persistence), lib/features/*/domain/ (all 5 models), test/models/, test/repositories/.
</objective>

<execution_context>
@C:/Users/Jose Augusto/Desktop/Unesc/Fase 5/Desenvolvimento Mobile/gsd/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jose Augusto/Desktop/Unesc/Fase 5/Desenvolvimento Mobile/gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-SKELETON.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pubspec.yaml + design system constants + theme</name>
  <read_first>
    Read .planning/phases/01-foundation/01-RESEARCH.md sections "Standard Stack" (pubspec.yaml block), "Pattern 2: ThemeData", "Pattern 3: Google Fonts", "Pitfall 1: ColorScheme.fromSeed Brightness", "Pitfall 6: Material3 surfaceTintColor".
    Read .planning/phases/01-foundation/01-SKELETON.md section "Directory Layout".
  </read_first>
  <files>
    pubspec.yaml,
    assets/fonts/Inter-Regular.ttf,
    assets/fonts/Inter-Medium.ttf,
    assets/fonts/Inter-SemiBold.ttf,
    assets/fonts/Inter-Bold.ttf,
    lib/core/constants/app_colors.dart,
    lib/core/constants/app_sizes.dart,
    lib/core/theme/app_text_theme.dart,
    lib/core/theme/app_theme.dart
  </files>
  <behavior>
    - `flutter pub get` exits 0 — all 6 packages resolve without version conflicts
    - `flutter analyze` exits 0 — no static analysis errors in the 4 Dart files
    - `AppColors.background` equals `Color(0xFF09090B)`
    - `AppColors.primary` equals `Color(0xFFA3E615)`
    - `appTheme.colorScheme.brightness` equals `Brightness.dark`
    - `appTheme.scaffoldBackgroundColor` equals `Color(0xFF09090B)`
    - `appTheme.cardTheme.surfaceTintColor` equals `Colors.transparent`
  </behavior>
  <action>
    1. Create `pubspec.yaml` in the project root using the EXACT content from RESEARCH.md "Standard Stack / Full pubspec.yaml". Key constraints:
       - `lucide_icons: ^0.257.0` (NOT ^3.0.0 — that version does not exist on pub.dev; FOUN-01 blocker)
       - `google_fonts: ^6.1.0`
       - `flutter_riverpod: ^2.5.1`
       - `go_router: ^14.0.0`
       - `shared_preferences: ^2.2.0`
       - `intl: ^0.19.0`
       - `assets/fonts/` declared under `flutter.assets`
       - Inter font family declared under `flutter.fonts` with four weights (Regular, Medium=500, SemiBold=600, Bold=700)
       If `flutter pub get` fails with an intl version conflict, add `dependency_overrides: { intl: ^0.19.0 }` to pubspec.yaml and re-run.

    2. Download the four Inter .ttf files from https://fonts.google.com/specimen/Inter and place them at `assets/fonts/Inter-Regular.ttf`, `assets/fonts/Inter-Medium.ttf`, `assets/fonts/Inter-SemiBold.ttf`, `assets/fonts/Inter-Bold.ttf`. If the download is not possible in the execution environment, create placeholder binary files and add a comment in pubspec.yaml noting the manual step. The font files must match the filenames declared in pubspec.yaml under `flutter.fonts`.

    3. Create `lib/core/constants/app_colors.dart`:
       ```
       abstract class AppColors {
         static const Color background      = Color(0xFF09090B);
         static const Color primary         = Color(0xFFA3E615);
         static const Color surface         = Color(0xFF18181B);
         static const Color surfaceElevated = Color(0xFF27272A);
         static const Color success         = Color(0xFF22C55E);
         static const Color error           = Color(0xFFEF4444);
         static const Color textMain        = Color(0xFFFAFAFA);
         static const Color textSecondary   = Color(0xFFA1A1AA);
       }
       ```
       Import: `import 'package:flutter/material.dart';`. No other imports.

    4. Create `lib/core/constants/app_sizes.dart` with spacing and radius tokens:
       - `spacingXS = 4.0`, `spacingS = 8.0`, `spacingM = 16.0`, `spacingL = 24.0`, `spacingXL = 32.0`
       - `radiusS = 8.0`, `radiusM = 12.0`, `radiusL = 16.0`, `radiusXL = 24.0`
       Use `abstract class AppSizes` with `static const double` fields.

    5. Create `lib/core/theme/app_text_theme.dart`:
       ```
       final appTextTheme = GoogleFonts.interTextTheme(
         ThemeData(brightness: Brightness.dark).textTheme,
       );
       ```
       Import: `google_fonts/google_fonts.dart`, `flutter/material.dart`.

    6. Create `lib/core/theme/app_theme.dart` using the pattern from RESEARCH.md Pattern 2. Critical rules:
       - `brightness: Brightness.dark` MUST be inside `ColorScheme.fromSeed(...)`, NOT at the `ThemeData(...)` level.
       - `ThemeData` does NOT receive a `brightness:` parameter — that triggers the assertion crash.
       - Set `surfaceTintColor: Colors.transparent` on both `CardTheme` and `AppBarTheme` to suppress Material3 elevation tint.
       - Set `scaffoldBackgroundColor: AppColors.background`.
       - Wire `textTheme: appTextTheme`.
       Full signature: `final appTheme = ThemeData(useMaterial3: true, colorScheme: ColorScheme.fromSeed(seedColor: AppColors.primary, brightness: Brightness.dark, surface: AppColors.surface, onSurface: AppColors.textMain, primary: AppColors.primary, error: AppColors.error), scaffoldBackgroundColor: AppColors.background, textTheme: appTextTheme, cardTheme: const CardTheme(surfaceTintColor: Colors.transparent, color: AppColors.surface), appBarTheme: const AppBarTheme(backgroundColor: AppColors.background, surfaceTintColor: Colors.transparent, elevation: 0));`

    7. Run `flutter pub get`. Run `flutter analyze lib/core/`. Both must exit 0.
  </action>
  <verify>
    <automated>flutter pub get && flutter analyze lib/core/</automated>
  </verify>
  <acceptance_criteria>
    - `flutter pub get` exits 0 with no version-solving errors
    - `pubspec.lock` contains `lucide_icons 0.257.0` (not 3.x)
    - `flutter analyze lib/core/` exits 0 with "No issues found"
    - `lib/core/constants/app_colors.dart` contains `Color(0xFF09090B)` and `Color(0xFFA3E615)`
    - `lib/core/theme/app_theme.dart` does NOT contain `brightness: Brightness.dark` at the `ThemeData(` call level (must be inside `fromSeed(`)
    - `lib/core/theme/app_theme.dart` contains `surfaceTintColor: Colors.transparent` (at least twice: CardTheme + AppBarTheme)
  </acceptance_criteria>
  <done>pubspec resolves cleanly, 4 Dart core files compile without errors, design system colors and theme are importable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Domain models + SharedPreferences sentinel + unit tests</name>
  <read_first>
    Read .planning/phases/01-foundation/01-RESEARCH.md section "Pattern 6: Domain Models — toJson / fromJson / copyWith" (all field lists and the DateTime serialization pattern for CoinTransaction).
    Read .planning/phases/01-foundation/01-RESEARCH.md section "Pattern 1: SharedPreferences Bootstrap" (sentinel provider snippet).
    Read .planning/phases/01-foundation/01-VALIDATION.md section "Wave 0 Requirements".
  </read_first>
  <files>
    lib/core/persistence/shared_preferences_provider.dart,
    lib/features/auth/domain/user.dart,
    lib/features/profile/domain/vehicle.dart,
    lib/features/profile/domain/product.dart,
    lib/features/shopping_list/domain/cart_item.dart,
    lib/features/smart_coins/domain/coin_transaction.dart,
    test/models/models_test.dart,
    test/repositories/shared_prefs_test.dart
  </files>
  <behavior>
    TDD — write tests FIRST, then implement models until all tests pass.

    Test cases for models_test.dart:
    - User.fromJson(User(...).toJson()) produces equal field values for id, name, email, address, coinBalance
    - User.fromJson handles missing optional fields (address defaults to '', coinBalance defaults to 0)
    - Vehicle.fromJson(Vehicle(...).toJson()) round-trips id, model, fuelEfficiencyKmPerLiter
    - Product.fromJson(Product(...).toJson()) round-trips id, name, brand, category, imageUrl, averagePrice, tags (List<String>)
    - CartItem.fromJson(CartItem(...).toJson()) round-trips productId, productName, brand, imageUrl, quantity, unitPrice
    - CoinTransaction.fromJson(CoinTransaction(...).toJson()) round-trips id, description, amount (int), createdAt (DateTime, ISO 8601)
    - CoinTransaction with negative amount (redemption) round-trips correctly

    Test cases for shared_prefs_test.dart:
    - sharedPreferencesProvider accessed without override throws UnimplementedError
    - sharedPreferencesProvider accessed WITH ProviderScope.overrideWithValue returns the injected instance
  </behavior>
  <action>
    STEP 1 — Write tests first (RED phase):

    Create `test/models/models_test.dart` with `flutter_test` and `flutter_riverpod/flutter_riverpod.dart` imports. Write all 7 test cases listed in the behavior block. Run `flutter test test/models/models_test.dart` — it must FAIL (models don't exist yet).

    Create `test/repositories/shared_prefs_test.dart` with `flutter_test`, `flutter_riverpod`, and `shared_preferences` imports. Write both test cases. Run `flutter test test/repositories/shared_prefs_test.dart` — it must FAIL.

    STEP 2 — Implement (GREEN phase):

    Create `lib/core/persistence/shared_preferences_provider.dart`:
    ```
    final sharedPreferencesProvider = Provider<SharedPreferences>(
      (_) => throw UnimplementedError('sharedPreferencesProvider must be overridden in main()'),
    );
    ```
    Import: `flutter_riverpod/flutter_riverpod.dart`, `shared_preferences/shared_preferences.dart`.

    Create `lib/features/auth/domain/user.dart` — @immutable class `User` with fields:
    - `id` (String, required)
    - `name` (String, required)
    - `email` (String, required)
    - `address` (String, optional, default '')
    - `coinBalance` (int, optional, default 0)
    Implement `factory User.fromJson(Map<String, dynamic> json)`, `Map<String, dynamic> toJson()`, and `User copyWith(...)`. Import only `package:flutter/foundation.dart` (for `@immutable`). No Flutter widget imports.

    Create `lib/features/profile/domain/vehicle.dart` — @immutable class `Vehicle` with fields:
    - `id` (String, required)
    - `model` (String, required)
    - `fuelEfficiencyKmPerLiter` (double, required)
    Same toJson/fromJson/copyWith pattern.

    Create `lib/features/profile/domain/product.dart` — @immutable class `Product` with fields:
    - `id` (String, required)
    - `name` (String, required)
    - `brand` (String, required)
    - `category` (String, required)
    - `imageUrl` (String, required)
    - `averagePrice` (double, required)
    - `tags` (List<String>, required)
    For tags in fromJson: `(json['tags'] as List<dynamic>).cast<String>()`.
    For tags in toJson: `List<String>.from(tags)`.

    Create `lib/features/shopping_list/domain/cart_item.dart` — @immutable class `CartItem` with fields:
    - `productId` (String, required)
    - `productName` (String, required)
    - `brand` (String, required)
    - `imageUrl` (String, required)
    - `quantity` (int, required)
    - `unitPrice` (double, required)
    Note: CartItem is intentionally flat (no Product reference) to avoid cross-feature coupling. Later phases create CartItem from Product data at the point of adding to cart.

    Create `lib/features/smart_coins/domain/coin_transaction.dart` — @immutable class `CoinTransaction` with fields:
    - `id` (String, required)
    - `description` (String, required)
    - `amount` (int, required — positive = gain, negative = redemption)
    - `createdAt` (DateTime, required)
    DateTime serialization: fromJson reads `DateTime.parse(json['createdAt'] as String)`, toJson writes `createdAt.toIso8601String()`.

    STEP 3 — Run tests (verify GREEN):
    `flutter test test/models/models_test.dart` — must pass all cases.
    `flutter test test/repositories/shared_prefs_test.dart` — must pass all cases.
    `flutter analyze lib/features/ lib/core/persistence/` — must exit 0.
  </action>
  <verify>
    <automated>flutter test test/models/models_test.dart && flutter test test/repositories/shared_prefs_test.dart && flutter analyze lib/features/ lib/core/persistence/</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/models/models_test.dart` exits 0, all 7 test cases pass
    - `flutter test test/repositories/shared_prefs_test.dart` exits 0, both test cases pass
    - `flutter analyze lib/features/ lib/core/persistence/` exits 0 with "No issues found"
    - No model file imports `package:flutter/material.dart` or `package:flutter_riverpod/flutter_riverpod.dart` (domain models are pure Dart — only `foundation.dart` for @immutable is allowed)
    - `lib/features/smart_coins/domain/coin_transaction.dart` contains `createdAt.toIso8601String()` and `DateTime.parse(`
    - `lib/core/persistence/shared_preferences_provider.dart` contains `throw UnimplementedError`
  </acceptance_criteria>
  <done>All 5 domain models and the SharedPreferences sentinel compile and pass their unit tests. The data contracts layer is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| App local storage → SharedPreferences | Data written to device storage; readable by OS on rooted devices |
| pubspec.yaml → pub.dev registry | Dependency resolution trusts pub.dev package legitimacy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-01 | Information Disclosure | SharedPreferences (local storage) | accept | App stores only academic mock data (no PII, no secrets). SharedPreferences is sandboxed per-app on non-rooted Android/iOS. Academic scope — acceptable. |
| T-01-02 | Tampering | Domain models (toJson/fromJson) | accept | All data is mocked/static in Phase 1; no untrusted input source exists yet. Validation will be added per-field in Phase 3 when user inputs arrive. |
| T-01-SC | Tampering | npm/pub package installs | mitigate | All 6 packages carry [ASSUMED] provenance from RESEARCH.md legitimacy audit. All are maintained by official teams (Flutter, Google, Dart, lucide.dev). No blocking checkpoint required; risk is negligible. |
</threat_model>

<verification>
After both tasks complete:
1. `flutter pub get` — exits 0, pubspec.lock contains lucide_icons 0.257.0
2. `flutter analyze lib/` — exits 0, no issues
3. `flutter test test/models/ test/repositories/` — all tests green
4. Manual grep: `grep -n "brightness: Brightness.dark" lib/core/theme/app_theme.dart` — the match must appear on the same line as `fromSeed(` or on the line between `fromSeed(` and `)`, NOT on a line containing `ThemeData(`
</verification>

<success_criteria>
- pubspec.yaml resolves with lucide_icons 0.257.0 (FOUN-01 satisfied for dependency layer)
- Dark theme compiles without assertion crash; brightness is inside fromSeed() (FOUN-02)
- AppColors exports all 8 design-system color tokens (FOUN-02)
- Inter font bundled and wired via GoogleFonts.interTextTheme (FOUN-02)
- All 5 domain models pass round-trip serialization tests (FOUN-04)
- SharedPreferences sentinel throws on unoverridden access (FOUN-05 structural prerequisite)
- `flutter test test/` exits 0
- `flutter analyze lib/` exits 0
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done, recording:
- Exact lucide_icons version resolved in pubspec.lock
- Whether intl dependency_overrides was needed
- Any deviations from the planned file list
- Test counts (passing/total) for models_test and shared_prefs_test
</output>
