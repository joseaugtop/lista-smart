---
phase: 02-auth-state-layer
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/features/smart_coins/domain/coin_transaction.dart
  - lib/core/providers/cart_notifier.dart
  - lib/core/providers/favorites_notifier.dart
  - lib/core/providers/coin_notifier.dart
  - test/providers/cart_notifier_test.dart
  - test/providers/favorites_notifier_test.dart
  - test/providers/coin_notifier_test.dart
autonomous: true
requirements: [AUTH-02]
must_haves:
  truths:
    - "CartNotifier adiciona/remove/limpa itens e persiste a lista no SharedPreferences (sobrevive ao restart)"
    - "FavoritesNotifier alterna favoritos por productId e persiste via setStringList (sobrevive ao restart)"
    - "CoinNotifier expõe balance + transactions, addCoins() incrementa e registra transação, persiste saldo e histórico"
    - "CoinNotifier inicializa balance a partir de User.coinBalance (750) quando não há saldo persistido"
    - "CoinTransaction.fromJson usa DateTime.tryParse com fallback — SharedPreferences corrompido não crasha (fix WR-02)"
  artifacts:
    - path: "lib/core/providers/cart_notifier.dart"
      provides: "CartNotifier (Notifier<List<CartItem>>) + cartProvider com addItem/removeItem/clear"
      contains: "class CartNotifier extends Notifier<List<CartItem>>"
    - path: "lib/core/providers/favorites_notifier.dart"
      provides: "FavoritesNotifier (Notifier<List<String>>) + favoritesProvider com toggle/isFavorite"
      contains: "class FavoritesNotifier extends Notifier<List<String>>"
    - path: "lib/core/providers/coin_notifier.dart"
      provides: "CoinState + CoinNotifier (Notifier<CoinState>) + coinProvider com addCoins"
      contains: "class CoinNotifier extends Notifier<CoinState>"
    - path: "lib/features/smart_coins/domain/coin_transaction.dart"
      provides: "fromJson seguro com DateTime.tryParse (WR-02)"
      contains: "DateTime.tryParse"
  key_links:
    - from: "lib/core/providers/coin_notifier.dart"
      to: "lib/core/providers/user_notifier.dart"
      via: "ref.watch(userNotifierProvider) para saldo inicial (user.coinBalance)"
      pattern: "userNotifierProvider"
    - from: "lib/core/providers/coin_notifier.dart"
      to: "lib/core/data/mock_data.dart"
      via: "MockData.initialTransactions como histórico inicial"
      pattern: "MockData\\.initialTransactions"
    - from: "lib/core/providers/cart_notifier.dart"
      to: "lib/core/persistence/shared_preferences_provider.dart"
      via: "ref.watch em build(), ref.read em _persist()"
      pattern: "sharedPreferencesProvider"
---

<objective>
Implementar os três Notifiers de estado global restantes — CartNotifier, FavoritesNotifier e CoinNotifier — todos com persistência via SharedPreferences (D-15), e corrigir WR-02 em CoinTransaction.fromJson (DateTime.tryParse). Ao final, os 4 Notifiers globais da fase (UserNotifier do Plano 01 + estes 3) respondem a ações sem exceções de estado nulo e estão prontos para as features das Fases 3-5 consumirem.

Implementa o critério de sucesso 4 da fase (todos os Notifiers respondem sem exceções de estado nulo) e a persistência D-15. Vinculado a AUTH-02 pois o CoinNotifier deriva o saldo inicial (750) do User de José Augusto carregado no login.

Purpose: As Fases 3-5 (carrinho, favoritos, loja de moedas, cadastro de notas) dependem destes Notifiers existirem e persistirem. Implementá-los agora, junto da camada de auth, garante que o estado global esteja completo e testado antes do loop de compras.
Output: 3 arquivos de provider novos, 1 arquivo de domínio corrigido (coin_transaction.dart), 3 arquivos de teste novos.
</objective>

<execution_context>
@C:/Users/Jose Augusto/Desktop/Unesc/Fase 5/Desenvolvimento Mobile/gsd/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jose Augusto/Desktop/Unesc/Fase 5/Desenvolvimento Mobile/gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-auth-state-layer/02-CONTEXT.md
@.planning/phases/02-auth-state-layer/02-PATTERNS.md
@.planning/phases/02-auth-state-layer/02-RESEARCH.md
@.planning/phases/02-auth-state-layer/02-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CartNotifier + FavoritesNotifier com persistência (Wave 0 tests first)</name>
  <files>lib/core/providers/cart_notifier.dart, lib/core/providers/favorites_notifier.dart, test/providers/cart_notifier_test.dart, test/providers/favorites_notifier_test.dart</files>
  <read_first>
    - lib/features/shopping_list/domain/cart_item.dart (CartItem: productId, productName, brand, imageUrl, quantity int, unitPrice double; fromJson/toJson/copyWith prontos)
    - lib/core/persistence/shared_preferences_provider.dart (sentinel — ref.watch em build, ref.read em mutações)
    - test/repositories/shared_prefs_test.dart (padrão setMockInitialValues + ProviderContainer + override + addTearDown)
    - .planning/phases/02-auth-state-layer/02-PATTERNS.md (seções cart_notifier.dart e favorites_notifier.dart — código e imports exatos)
    - .planning/phases/02-auth-state-layer/02-RESEARCH.md (Pattern 4 CartNotifier, Pattern 5 FavoritesNotifier)
    - CLAUDE.md (NotifierProvider; ref.watch só em build; ref.read em mutações; providers top-level)
  </read_first>
  <behavior>
    - Cart Test 1: build() retorna [] quando não há chave 'lista_smart_cart'
    - Cart Test 2: addItem() de produto novo adiciona à lista; addItem() do mesmo productId incrementa quantity em vez de duplicar
    - Cart Test 3: removeItem(productId) remove o item correspondente
    - Cart Test 4: clear() esvazia a lista
    - Cart Test 5: após addItem(), um novo ProviderContainer com o mesmo prefs hidrata a lista persistida (sobrevive ao restart)
    - Cart Test 6: build() retorna [] quando o JSON persistido está corrompido (fallback seguro)
    - Fav Test 1: build() retorna [] quando não há chave 'lista_smart_favorites'
    - Fav Test 2: toggle(id) adiciona id ausente; toggle(id) novamente remove id presente
    - Fav Test 3: isFavorite(id) reflete o estado atual
    - Fav Test 4: após toggle(), novo ProviderContainer com mesmo prefs hidrata via getStringList (sobrevive ao restart)
  </behavior>
  <action>
    Criar os dois arquivos de teste PRIMEIRO (RED) cobrindo os casos acima, seguindo o padrão de test/repositories/shared_prefs_test.dart. Para os testes de persistência (Cart Test 5, Fav Test 4) mutar no primeiro container e reler em um segundo ProviderContainer compartilhando a mesma instância de prefs.

    Criar lib/core/providers/cart_notifier.dart per Pattern 4: imports dart:convert, flutter_riverpod, '../persistence/shared_preferences_provider.dart', '../../features/shopping_list/domain/cart_item.dart'. class CartNotifier extends Notifier&lt;List&lt;CartItem&gt;&gt; com static const _key = 'lista_smart_cart'; build() lê ref.watch(sharedPreferencesProvider).getString(_key), retorna [] se ausente, jsonDecode → List → CartItem.fromJson dentro de try/catch retornando [] em erro; addItem(CartItem) verifica indexWhere por productId, se existe usa copyWith(quantity: +1) via sublist splice, senão append, depois _persist(); removeItem(String productId) filtra por productId != e _persist(); clear() seta [] e _persist(); _persist() usa ref.read(sharedPreferencesProvider).setString(_key, jsonEncode(state.map((e) => e.toJson()).toList())). Declarar final cartProvider = NotifierProvider&lt;CartNotifier, List&lt;CartItem&gt;&gt;(CartNotifier.new) top-level.

    Criar lib/core/providers/favorites_notifier.dart per Pattern 5: imports flutter_riverpod, '../persistence/shared_preferences_provider.dart'. class FavoritesNotifier extends Notifier&lt;List&lt;String&gt;&gt; com static const _key = 'lista_smart_favorites'; build() retorna ref.watch(sharedPreferencesProvider).getStringList(_key) ?? [] (usar getStringList/setStringList, NÃO JSON encode — mais eficiente para List<String>); toggle(String productId) adiciona ou remove e ref.read(sharedPreferencesProvider).setStringList(_key, state); bool isFavorite(String productId) => state.contains(productId). Declarar final favoritesProvider = NotifierProvider&lt;FavoritesNotifier, List&lt;String&gt;&gt;(FavoritesNotifier.new) top-level.

    Rodar a suite (GREEN). NÃO usar StateNotifierProvider.
  </action>
  <verify>
    <automated>cd /d P:\ && flutter test test/providers/cart_notifier_test.dart test/providers/favorites_notifier_test.dart --no-pub</automated>
    <automated>cd /d P:\ && flutter analyze lib/core/providers/</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/providers/cart_notifier_test.dart test/providers/favorites_notifier_test.dart` sai 0 com todos os testes passando
    - `flutter analyze lib/core/providers/` sai 0
    - grep `class CartNotifier extends Notifier<List<CartItem>>` encontra match
    - grep `class FavoritesNotifier extends Notifier<List<String>>` encontra match
    - grep `getStringList` e `setStringList` ambos encontram match em favorites_notifier.dart
    - grep `StateNotifierProvider` NÃO encontra match em nenhum dos dois arquivos
    - grep `NotifierProvider<CartNotifier` e `NotifierProvider<FavoritesNotifier` ambos encontram match
  </acceptance_criteria>
  <done>CartNotifier e FavoritesNotifier persistem e hidratam via SharedPreferences; cart incrementa quantidade em duplicata; favorites usa getStringList/setStringList; testes verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix WR-02 em CoinTransaction + CoinNotifier com CoinState (Wave 0 tests first)</name>
  <files>lib/features/smart_coins/domain/coin_transaction.dart, lib/core/providers/coin_notifier.dart, test/providers/coin_notifier_test.dart</files>
  <read_first>
    - lib/features/smart_coins/domain/coin_transaction.dart (versão atual: linha 25 usa DateTime.parse — substituir por DateTime.tryParse; resto idêntico)
    - lib/core/providers/user_notifier.dart (userNotifierProvider — Plano 01; CoinNotifier faz ref.watch para saldo inicial)
    - lib/core/data/mock_data.dart (MockData.initialTransactions, MockData.user.coinBalance=750 — Plano 01)
    - lib/features/auth/domain/user.dart (User.coinBalance — fonte do saldo inicial)
    - lib/core/persistence/shared_preferences_provider.dart (sentinel)
    - test/repositories/shared_prefs_test.dart (padrão de teste)
    - .planning/phases/02-auth-state-layer/02-PATTERNS.md (seções coin_notifier.dart e coin_transaction.dart — CoinState @immutable, build com fallback, fix WR-02 exato)
    - .planning/phases/02-auth-state-layer/02-RESEARCH.md (Pattern 6 CoinNotifier, Pitfall 6 WR-02)
    - .planning/phases/01-foundation/01-REVIEW.md (WR-02 linhas 156-174)
    - CLAUDE.md (NotifierProvider; @immutable + copyWith para state composto)
  </read_first>
  <behavior>
    - Domain Test 1: CoinTransaction.fromJson com createdAt válido (ISO 8601) parseia corretamente
    - Domain Test 2: CoinTransaction.fromJson com createdAt inválido NÃO lança — retorna DateTime.fromMillisecondsSinceEpoch(0) (fix WR-02)
    - Coin Test 1: build() sem persistência e com user logado (login() antes) retorna balance == 750 (user.coinBalance) e transactions == MockData.initialTransactions
    - Coin Test 2: addCoins(10, 'Nota fiscal') incrementa balance em 10 e insere uma transação no início da lista (transactions.first.amount == 10)
    - Coin Test 3: após addCoins(), novo ProviderContainer com mesmo prefs hidrata balance e transactions persistidos (sobrevive ao restart)
    - Coin Test 4: build() com histórico de transações corrompido faz fallback para MockData.initialTransactions sem lançar
  </behavior>
  <action>
    Criar test/providers/coin_notifier_test.dart PRIMEIRO (RED) cobrindo os casos acima. Incluir os 2 testes de domínio de CoinTransaction.fromJson (válido e inválido) — podem ficar em grupos separados no mesmo arquivo ou usar o test/models existente; preferir o arquivo coin_notifier_test.dart para manter coesão da Wave 0 desta tarefa. Para Coin Test 1, fazer login() via userNotifierProvider.notifier antes de ler coinProvider, no mesmo ProviderContainer.

    Corrigir lib/features/smart_coins/domain/coin_transaction.dart per WR-02: trocar a linha createdAt: DateTime.parse(json['createdAt'] as String) por createdAt: DateTime.tryParse(json['createdAt'] as String) ?? DateTime.fromMillisecondsSinceEpoch(0). Manter todo o resto do arquivo idêntico (construtor, campos, toJson, copyWith).

    Criar lib/core/providers/coin_notifier.dart per Pattern 6: imports dart:convert, flutter/foundation.dart show immutable, flutter_riverpod, '../data/mock_data.dart', '../persistence/shared_preferences_provider.dart', '../../features/smart_coins/domain/coin_transaction.dart', 'user_notifier.dart'. Definir @immutable class CoinState com const constructor {required int balance, required List&lt;CoinTransaction&gt; transactions} e copyWith. class CoinNotifier extends Notifier&lt;CoinState&gt; com static const _balanceKey = 'lista_smart_coins_balance', _txKey = 'lista_smart_coins_tx'; build() lê ref.watch(sharedPreferencesProvider) e ref.watch(userNotifierProvider); balance = prefs.getInt(_balanceKey) ?? user?.coinBalance ?? 0; transactions = decode de prefs.getString(_txKey) com try/catch → fallback MockData.initialTransactions, ou MockData.initialTransactions se ausente; retorna CoinState(balance, transactions). addCoins(int amount, String description) cria CoinTransaction(id: DateTime.now().millisecondsSinceEpoch.toString(), description, amount, createdAt: DateTime.now()), seta state = state.copyWith(balance: state.balance + amount, transactions: [tx, ...state.transactions]) e _persist(); _persist() usa ref.read(sharedPreferencesProvider) para setInt(_balanceKey, balance) e setString(_txKey, jsonEncode(transactions.map((t) => t.toJson()).toList())). Declarar final coinProvider = NotifierProvider&lt;CoinNotifier, CoinState&gt;(CoinNotifier.new) top-level.

    Rodar a suite (GREEN). NÃO usar StateNotifierProvider.
  </action>
  <verify>
    <automated>cd /d P:\ && flutter test test/providers/coin_notifier_test.dart --no-pub</automated>
    <automated>cd /d P:\ && flutter analyze lib/</automated>
    <automated>cd /d P:\ && flutter test test/ --no-pub</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/providers/coin_notifier_test.dart` sai 0 com todos os testes passando
    - `flutter analyze lib/` sai 0 (toda a lib limpa)
    - `flutter test test/ --no-pub` sai 0 (suite completa da fase verde — regression incluído)
    - grep `DateTime.tryParse` encontra match em lib/features/smart_coins/domain/coin_transaction.dart
    - grep `DateTime.parse(` NÃO encontra match em coin_transaction.dart (substituído por tryParse)
    - grep `class CoinNotifier extends Notifier<CoinState>` encontra match
    - grep `class CoinState` com `@immutable` presente em coin_notifier.dart
    - grep `user?.coinBalance` encontra match (saldo inicial derivado do User)
    - grep `MockData.initialTransactions` encontra match em coin_notifier.dart
    - grep `NotifierProvider<CoinNotifier, CoinState>` encontra match
  </acceptance_criteria>
  <done>CoinTransaction.fromJson seguro (WR-02 corrigido); CoinNotifier expõe balance+transactions, deriva saldo inicial 750 do User, addCoins incrementa e persiste; suite completa verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SharedPreferences → Notifiers (deserialização) | Cart, favorites e coin history persistidos podem estar corrompidos; entram via fromJson/decode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Denial of Service | CoinTransaction.fromJson DateTime.parse (WR-02) | mitigate | DateTime.tryParse com fallback DateTime.fromMillisecondsSinceEpoch(0) — dado de data corrompido não crasha a inicialização do app |
| T-02-07 | Denial of Service | CartNotifier/CoinNotifier build() decode | mitigate | try/catch em jsonDecode retornando [] / MockData.initialTransactions — SharedPreferences corrompido não bloqueia o app |
| T-02-SC | Tampering | npm/pub installs | accept | Nenhum pacote novo instalado nesta fase (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- `flutter test test/ --no-pub` sai 0 (suite completa: Fase 1 + user_notifier + router_notifier + cart + favorites + coin)
- `flutter analyze lib/` sai 0
- Nota de ambiente: rodar via subst P: — recriar com `subst P: "C:\Users\Jose Augusto\Desktop\Unesc\Fase 5\Desenvolvimento Mobile\gsd"` se ausente
</verification>

<success_criteria>
- CartNotifier, FavoritesNotifier, CoinNotifier implementados como Notifier<T> com persistência (D-15)
- CoinNotifier deriva saldo inicial 750 do User logado (AUTH-02 link)
- WR-02 corrigido: CoinTransaction.fromJson usa DateTime.tryParse
- Todos os 4 Notifiers da fase respondem sem exceções de estado nulo (critério de sucesso 4 da fase)
- Suite completa verde
</success_criteria>

<output>
Create `.planning/phases/02-auth-state-layer/02-03-SUMMARY.md` when done
</output>
