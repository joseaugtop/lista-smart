---
phase: 02-auth-state-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/data/mock_data.dart
  - lib/core/providers/user_notifier.dart
  - lib/routing/router_notifier.dart
  - lib/routing/app_router.dart
  - test/providers/user_notifier_test.dart
  - test/routing/router_notifier_test.dart
autonomous: true
requirements: [AUTH-02, AUTH-03]
must_haves:
  truths:
    - "App inicia em /login quando não há sessão persistida (user == null)"
    - "Chamar UserNotifier.login() preenche o estado global com José Augusto (coinBalance=750) e persiste no SharedPreferences"
    - "Quando user != null e rota atual é /login, RouterNotifier.redirect() retorna /home"
    - "Quando user == null, RouterNotifier.redirect() retorna /login para qualquer rota"
    - "GoRouter é construído uma única vez (ref.read, não ref.watch) e segura o RouterNotifier vivo pela vida do app"
  artifacts:
    - path: "lib/core/data/mock_data.dart"
      provides: "MockData.user (José Augusto, 750 moedas), MockData.products (10-15), MockData.supermarketDistances (4), MockData.initialTransactions"
      contains: "abstract class MockData"
    - path: "lib/core/providers/user_notifier.dart"
      provides: "UserNotifier (Notifier<User?>) + userNotifierProvider com login(), logout(), hidratação do SharedPreferences"
      contains: "class UserNotifier extends Notifier<User?>"
    - path: "lib/routing/router_notifier.dart"
      provides: "RouterNotifier com ChangeNotifier mixin + redirect() ativo"
      contains: "with ChangeNotifier"
    - path: "lib/routing/app_router.dart"
      provides: "goRouterProvider corrigido (ref.read, initialLocation /login, kDebugMode)"
      contains: "ref.read(routerNotifierProvider.notifier)"
  key_links:
    - from: "lib/routing/router_notifier.dart"
      to: "lib/core/providers/user_notifier.dart"
      via: "ref.watch(userNotifierProvider) em build() + ref.read em redirect()"
      pattern: "userNotifierProvider"
    - from: "lib/core/providers/user_notifier.dart"
      to: "lib/core/data/mock_data.dart"
      via: "MockData.user em login()"
      pattern: "MockData\\.user"
    - from: "lib/routing/app_router.dart"
      to: "lib/routing/router_notifier.dart"
      via: "ref.read(routerNotifierProvider.notifier) na construção do GoRouter"
      pattern: "routerNotifierProvider\\.notifier"
---

<objective>
Construir o backbone de autenticação simulada: dados mock centralizados, UserNotifier global, e corrigir os bugs de roteamento CR-02 (Listenable single-slot) e CR-03 (ref.watch em Provider<GoRouter>) para ativar o auth guard. Ao final, o estado da máquina de auth funciona ponta a ponta: login() preenche o estado e o RouterNotifier redireciona automaticamente.

Implementa AUTH-02 (login preenche estado global com José Augusto) na camada de estado/roteamento, e AUTH-03 (redirect para /login se user == null). A UI de login (toque visível em "Avançar") é completada no Plano 02-02.

Purpose: Sem o backbone funcional (Listenable multi-listener + redirect ativo + estado de usuário), a tela de login do Plano 02 não tem onde se conectar e o auth guard nunca dispara. Os fixes CR-02/CR-03 são pré-requisitos críticos do guard (RESEARCH §Summary).
Output: 2 arquivos novos (mock_data.dart, user_notifier.dart), 2 arquivos corrigidos (router_notifier.dart, app_router.dart), 2 arquivos de teste novos.
</objective>

<execution_context>
@C:/Users/Jose Augusto/Desktop/Unesc/Fase 5/Desenvolvimento Mobile/gsd/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jose Augusto/Desktop/Unesc/Fase 5/Desenvolvimento Mobile/gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-auth-state-layer/02-CONTEXT.md
@.planning/phases/02-auth-state-layer/02-RESEARCH.md
@.planning/phases/02-auth-state-layer/02-PATTERNS.md
@.planning/phases/02-auth-state-layer/02-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MockData + UserNotifier (Wave 0 tests first)</name>
  <files>lib/core/data/mock_data.dart, lib/core/providers/user_notifier.dart, test/providers/user_notifier_test.dart</files>
  <read_first>
    - lib/features/auth/domain/user.dart (modelo User: construtor const com id, name, email, address='', coinBalance=0; fromJson/toJson já prontos — usar diretamente)
    - lib/features/profile/domain/product.dart (construtor const Product: id, name, brand, category, imageUrl, averagePrice, tags)
    - lib/features/smart_coins/domain/coin_transaction.dart (CoinTransaction: id, description, amount int, createdAt DateTime — NÃO é const por causa do DateTime)
    - lib/core/persistence/shared_preferences_provider.dart (sentinel Provider<SharedPreferences> — injetar via ref.watch no build, ref.read nas mutações)
    - test/repositories/shared_prefs_test.dart (padrão de teste: SharedPreferences.setMockInitialValues({}) + ProviderContainer com overrideWithValue + addTearDown(container.dispose))
    - .planning/phases/02-auth-state-layer/02-PATTERNS.md (seções "user_notifier.dart" e "mock_data.dart" — código de referência e imports exatos)
    - .planning/phases/02-auth-state-layer/02-RESEARCH.md (Pattern 3 e Pattern 8)
    - CLAUDE.md (NotifierProvider NUNCA StateNotifierProvider; providers top-level; ref.watch só em build, ref.read em mutações)
  </read_first>
  <behavior>
    - Test 1: UserNotifier.build() retorna null quando não há chave 'lista_smart_user' no SharedPreferences (sessão limpa)
    - Test 2: login() seta state == MockData.user (id 'jose_augusto_001', name 'José Augusto', coinBalance 750)
    - Test 3: login() persiste o User serializado em SharedPreferences sob a chave 'lista_smart_user' (getString não-nulo após login)
    - Test 4: após login(), um novo ProviderContainer com o mesmo SharedPreferences hidrata state == MockData.user (sessão sobrevive)
    - Test 5: logout() seta state == null e remove a chave 'lista_smart_user' do SharedPreferences
    - Test 6: build() retorna null quando o JSON persistido está corrompido (string inválida) — fallback seguro sem lançar exceção
  </behavior>
  <action>
    Criar test/providers/user_notifier_test.dart PRIMEIRO (RED) cobrindo os 6 casos acima, seguindo o padrão de test/repositories/shared_prefs_test.dart (SharedPreferences.setMockInitialValues, ProviderContainer com sharedPreferencesProvider.overrideWithValue(prefs), addTearDown(container.dispose)). Para o teste de hidratação (Test 4) escrever a string via login() no primeiro container, depois criar um segundo ProviderContainer com a MESMA instância de prefs e ler userNotifierProvider.

    Criar lib/core/data/mock_data.dart como abstract class MockData com: static const User user = User(id: 'jose_augusto_001', name: 'José Augusto', email: 'jose.rocha@zorte.com.br', address: 'Criciúma, SC', coinBalance: 750) per D-11 e D-14; static const List<Product> products com 12 itens const distribuídos nas 4 categorias 'Laticínios', 'Frutas e verduras', 'Limpeza e higiene', 'Padaria e granel' (cada Product com imageUrl: '' e tags não-vazio) per D-12; static const Map<String, double> supermarketDistances = {'Bistek': 2.3, 'Giassi': 3.7, 'Angeloni': 4.1, 'Atacadão': 6.8} per D-13; static List<CoinTransaction> get initialTransactions (getter, NÃO const) com 2 transações de bootstrap usando DateTime literais. Imports relativos para os 3 modelos de domínio.

    Criar lib/core/providers/user_notifier.dart com class UserNotifier extends Notifier<User?> per D-07: campo static const _key = 'lista_smart_user'; build() lê ref.watch(sharedPreferencesProvider).getString(_key), retorna null se ausente, faz User.fromJson(jsonDecode(...)) dentro de try/catch retornando null em caso de erro; login() seta state = MockData.user e chama _persist(user); logout() seta state = null e ref.read(sharedPreferencesProvider).remove(_key); _persist(User) usa ref.read(sharedPreferencesProvider).setString(_key, jsonEncode(user.toJson())). Declarar final userNotifierProvider = NotifierProvider&lt;UserNotifier, User?&gt;(UserNotifier.new) top-level. Nome da chave SharedPreferences é discretion do Claude — usar 'lista_smart_user' per RESEARCH.

    Rodar a suite (GREEN). Usar withValues não withOpacity (não aplicável aqui). NÃO usar StateNotifierProvider.
  </action>
  <verify>
    <automated>cd /d P:\ && flutter test test/providers/user_notifier_test.dart --no-pub</automated>
    <automated>cd /d P:\ && flutter analyze lib/core/</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/providers/user_notifier_test.dart` sai 0 com os 6 testes passando
    - `flutter analyze lib/core/` sai 0 sem erros
    - grep `class UserNotifier extends Notifier<User?>` encontra match em lib/core/providers/user_notifier.dart
    - grep `NotifierProvider<UserNotifier, User?>` encontra match (NÃO StateNotifierProvider)
    - grep `coinBalance: 750` encontra match em lib/core/data/mock_data.dart
    - grep `'jose_augusto_001'` encontra match em lib/core/data/mock_data.dart
    - mock_data.dart contém exatamente 4 chaves em supermarketDistances: 'Bistek', 'Giassi', 'Angeloni', 'Atacadão'
    - mock_data.dart contém >= 10 instâncias de `Product(` (10-15 produtos per D-12)
  </acceptance_criteria>
  <done>UserNotifier hidrata/persiste sessão via SharedPreferences, login() carrega José Augusto com 750 moedas, MockData centraliza user/products/supermarkets/transactions, 6 testes verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix CR-02 (RouterNotifier ChangeNotifier) + CR-03 (app_router ref.read) + redirect ativo (Wave 0 test first)</name>
  <files>lib/routing/router_notifier.dart, lib/routing/app_router.dart, test/routing/router_notifier_test.dart</files>
  <read_first>
    - lib/routing/router_notifier.dart (versão atual COM bugs: AutoDisposeAsyncNotifier implements Listenable, single-slot _routerListener — substituir inteiramente)
    - lib/routing/app_router.dart (versão atual COM bugs: ref.watch na linha 31, initialLocation AppRoutes.home na linha 35, debugLogDiagnostics: true na linha 36 — corrigir apenas estas 3 linhas + import)
    - lib/routing/app_routes.dart (AppRoutes.login = '/login', AppRoutes.home = '/home')
    - lib/core/providers/user_notifier.dart (userNotifierProvider — criado na Task 1; RouterNotifier depende dele)
    - .planning/phases/01-foundation/01-REVIEW.md (CR-02 detalhado linhas 68-102, CR-03 linhas 106-132, WR-03 linhas 178-187)
    - .planning/phases/02-auth-state-layer/02-PATTERNS.md (seções router_notifier.dart e app_router.dart — código de fix exato e import de kDebugMode)
    - .planning/phases/02-auth-state-layer/02-RESEARCH.md (Pattern 1, Pattern 2, Pitfall 1, Pitfall 3)
    - CLAUDE.md (proibição de ref.watch dentro de Provider<GoRouter>; Listenable via ChangeNotifier)
  </read_first>
  <action>
    Criar test/routing/router_notifier_test.dart PRIMEIRO cobrindo a lógica de redirect via ProviderContainer com sharedPreferencesProvider overridden: (a) com SharedPreferences vazio (user == null), ler routerNotifierProvider e chamar notifier.redirect com um GoRouterState mockado cuja matchedLocation == '/home' → espera retorno '/login'; (b) após login() via userNotifierProvider.notifier, redirect com matchedLocation == '/login' → espera '/home'; (c) após login(), redirect com matchedLocation == '/home' → espera null. Como GoRouterState é difícil de instanciar diretamente, testar a lógica de redirect extraindo o user de userNotifierProvider — se instanciar GoRouterState for inviável no ambiente de teste, criar um teste unitário que verifica o estado de userNotifierProvider (null vs MockData.user) que governa a decisão de redirect, e documentar essa limitação no SUMMARY. Garantir que o teste compila e roda verde.

    Substituir lib/routing/router_notifier.dart inteiramente per D-08: imports flutter/material.dart, flutter_riverpod, go_router, '../core/providers/user_notifier.dart', 'app_routes.dart'. class RouterNotifier extends AsyncNotifier&lt;void&gt; with ChangeNotifier (REMOVER AutoDispose, REMOVER implements Listenable, REMOVER _routerListener e os métodos addListener/removeListener manuais). build() async: ref.watch(userNotifierProvider); listenSelf((_, __) => notifyListeners()). redirect(BuildContext, GoRouterState state) per D-10: final user = ref.read(userNotifierProvider); if (user == null) return AppRoutes.login; if (state.matchedLocation == AppRoutes.login) return AppRoutes.home; return null. Declarar final routerNotifierProvider = AsyncNotifierProvider&lt;RouterNotifier, void&gt;(RouterNotifier.new) — SEM autoDispose (Pitfall 1: GoRouter segura referência pela vida do app).

    Corrigir lib/routing/app_router.dart per D-09: adicionar import 'package:flutter/foundation.dart' show kDebugMode; mudar linha 31 de ref.watch(routerNotifierProvider.notifier) para ref.read(routerNotifierProvider.notifier); mudar initialLocation de AppRoutes.home para AppRoutes.login (Pitfall 3 — evita flash do HomeScreen); mudar debugLogDiagnostics: true para debugLogDiagnostics: kDebugMode (fix WR-03). NÃO alterar a estrutura de rotas/branches (linhas 39-97) nem ScaffoldWithBottomNav.
  </action>
  <verify>
    <automated>cd /d P:\ && flutter test test/routing/router_notifier_test.dart --no-pub</automated>
    <automated>cd /d P:\ && flutter analyze lib/routing/</automated>
    <automated>cd /d P:\ && flutter test test/ --no-pub</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/routing/router_notifier_test.dart` sai 0
    - `flutter analyze lib/routing/` sai 0 sem erros nem warnings de deprecation
    - `flutter test test/` sai 0 (todos os testes da Fase 1 + novos continuam verdes — regression guard)
    - grep `with ChangeNotifier` encontra match em lib/routing/router_notifier.dart
    - grep `implements Listenable` NÃO encontra match em lib/routing/router_notifier.dart (single-slot removido)
    - grep `AutoDispose` NÃO encontra match em lib/routing/router_notifier.dart
    - grep `ref.read(routerNotifierProvider.notifier)` encontra match em lib/routing/app_router.dart
    - grep `ref.watch(routerNotifierProvider` NÃO encontra match em lib/routing/app_router.dart
    - grep `initialLocation: AppRoutes.login` encontra match em lib/routing/app_router.dart
    - grep `debugLogDiagnostics: kDebugMode` encontra match em lib/routing/app_router.dart
  </acceptance_criteria>
  <done>CR-02 e CR-03 corrigidos: RouterNotifier usa ChangeNotifier multi-listener, goRouterProvider usa ref.read e inicia em /login com kDebugMode; redirect ativo redireciona null→/login e logado-em-/login→/home; suite inteira verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SharedPreferences → app (deserialização) | Dados locais persistidos podem estar corrompidos; entram no app via fromJson |
| UserNotifier → RouterNotifier (auth guard) | Estado de auth governa acesso a rotas protegidas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | RouterNotifier (CR-02 single-slot Listenable) | mitigate | with ChangeNotifier (D-08) — multi-listener thread-safe; impede que listener de guard seja silenciosamente substituído |
| T-02-02 | Tampering | goRouterProvider (CR-03 ref.watch) | mitigate | ref.read(.notifier) (D-09) — GoRouter construído uma vez; impede reset de estado de navegação |
| T-02-03 | Denial of Service | UserNotifier.build() deserialização | mitigate | try/catch em User.fromJson retornando null — SharedPreferences corrompido não crasha a inicialização |
| T-02-SC | Tampering | npm/pub installs | accept | Nenhum pacote novo instalado nesta fase (RESEARCH §Package Legitimacy Audit — todos aprovados na Fase 1) |
</threat_model>

<verification>
- `flutter test test/ --no-pub` sai 0 (suite completa incluindo Fase 1 + novos testes de user_notifier e router_notifier)
- `flutter analyze lib/` sai 0 sem erros
- Nota de ambiente: rodar via subst P: — se P: não existir, recriar com `subst P: "C:\Users\Jose Augusto\Desktop\Unesc\Fase 5\Desenvolvimento Mobile\gsd"` (RESEARCH §Environment Availability)
</verification>

<success_criteria>
- MockData centraliza user (José Augusto, 750 moedas), 10-15 produtos em 4 categorias, 4 supermercados, transações iniciais
- UserNotifier (Notifier<User?>) hidrata e persiste sessão; login() carrega José Augusto; logout() limpa
- CR-02 corrigido: RouterNotifier usa ChangeNotifier mixin
- CR-03 corrigido: goRouterProvider usa ref.read, initialLocation /login, debugLogDiagnostics kDebugMode
- redirect() ativo conforme D-10
- Todos os testes verdes (regression + novos)
</success_criteria>

<output>
Create `.planning/phases/02-auth-state-layer/02-01-SUMMARY.md` when done
</output>
