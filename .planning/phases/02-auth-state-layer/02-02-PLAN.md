---
phase: 02-auth-state-layer
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/features/auth/presentation/login_screen.dart
  - test/widgets/login_screen_test.dart
autonomous: true
requirements: [AUTH-01, AUTH-02]
must_haves:
  truths:
    - "Usuário vê a tela de login com nome 'Lista Smart', subtítulo, campos e-mail/senha estilizados"
    - "Campo e-mail mostra ícone LucideIcons.mail; campo senha mostra LucideIcons.lock + toggle eye/eyeOff"
    - "Fundo tem 2 blobs primary desfocados via BackdropFilter atrás de um card glassmorphic"
    - "Toque em 'Avançar' mostra CircularProgressIndicator ~500ms e chama UserNotifier.login() preenchendo José Augusto"
    - "Após login(), o RouterNotifier (Plano 01) redireciona automaticamente para /home — sem context.go manual"
  artifacts:
    - path: "lib/features/auth/presentation/login_screen.dart"
      provides: "LoginScreen glassmorphic completa (ConsumerStatefulWidget) substituindo o placeholder da Fase 1"
      contains: "class LoginScreen extends ConsumerStatefulWidget"
      min_lines: 90
  key_links:
    - from: "lib/features/auth/presentation/login_screen.dart"
      to: "lib/core/providers/user_notifier.dart"
      via: "ref.read(userNotifierProvider.notifier).login() em _handleLogin()"
      pattern: "userNotifierProvider\\.notifier"
    - from: "lib/features/auth/presentation/login_screen.dart"
      to: "lib/core/constants/app_colors.dart"
      via: "AppColors.primary nos blobs, botão e borda focada"
      pattern: "AppColors\\.primary"
---

<objective>
Substituir o placeholder de LoginScreen pela tela glassmorphic completa: fundo com blobs desfocados (BackdropFilter), card central translúcido, campos e-mail/senha com ícones Lucide e toggle de visibilidade, e botão "Avançar" que dispara loading de ~500ms antes de chamar UserNotifier.login(). Esta é a completude do slice vertical de auth — após este plano o usuário pode tocar "Avançar" e ser levado à Home automaticamente.

Implementa AUTH-01 (tela de login estilizada com ícones Lucide e círculos desfocados — D-01, D-02, D-03, D-04) e a face de UI de AUTH-02 (toque em "Avançar" → loading → login — D-05, D-06).

Purpose: O backbone do Plano 01 deixou login()/redirect() prontos mas sem ponto de entrada visível. Esta tela conecta o gesto do usuário ("Avançar") à máquina de estado de auth, completando o caminho ponta a ponta.
Output: 1 arquivo substituído (login_screen.dart), 1 teste de widget novo.
</objective>

<execution_context>
@C:/Users/Jose Augusto/Desktop/Unesc/Fase 5/Desenvolvimento Mobile/gsd/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jose Augusto/Desktop/Unesc/Fase 5/Desenvolvimento Mobile/gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-auth-state-layer/02-CONTEXT.md
@.planning/phases/02-auth-state-layer/02-UI-SPEC.md
@.planning/phases/02-auth-state-layer/02-PATTERNS.md
@.planning/phases/02-auth-state-layer/02-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LoginScreen glassmorphic completa</name>
  <files>lib/features/auth/presentation/login_screen.dart</files>
  <read_first>
    - lib/features/auth/presentation/login_screen.dart (placeholder atual StatelessWidget — substituir inteiramente)
    - lib/features/home/presentation/home_screen.dart (padrão Scaffold + AppColors.background)
    - lib/core/constants/app_colors.dart (AppColors.background #09090B, primary #A3E615, surface #18181B, textMain #FAFAFA, textSecondary #A1A1AA)
    - lib/core/constants/app_sizes.dart (spacingS=8, spacingM=16, spacingL=24, spacingXL=32; radiusM=12, radiusL=16, radiusXL=24)
    - lib/core/providers/user_notifier.dart (userNotifierProvider.notifier.login() — criado no Plano 01)
    - .planning/phases/02-auth-state-layer/02-UI-SPEC.md (Layout Structure, Typography, Color, Copywriting Contract, Interaction States, Accessibility — contrato visual completo)
    - .planning/phases/02-auth-state-layer/02-PATTERNS.md (seção login_screen.dart — imports, ConsumerStatefulWidget, Stack/BackdropFilter ordering, glassmorphic card, TextFormField com Lucide, FilledButton + loading)
    - .planning/phases/02-auth-state-layer/02-RESEARCH.md (Pattern 7, Pitfall 2 setState após dispose, Pitfall 4 ordem do BackdropFilter, Pitfall 5 withValues)
    - CLAUDE.md (ConsumerStatefulWidget; ref.read em event handler nunca ref.watch; estado efêmero de UI em StatefulWidget não em Riverpod)
  </read_first>
  <action>
    Substituir lib/features/auth/presentation/login_screen.dart inteiramente. Imports: 'dart:ui' (ImageFilter.blur), flutter/material.dart, flutter_riverpod, lucide_icons, e relativos para app_colors.dart, app_sizes.dart, ../../../core/providers/user_notifier.dart.

    Declarar class LoginScreen extends ConsumerStatefulWidget per Pattern 7; _LoginScreenState com bool _isLoading=false, bool _isPasswordVisible=false, e TextEditingController _emailController/_passwordController (estado efêmero — não Riverpod, per CLAUDE.md). dispose() libera os dois controllers.

    _handleLogin() async per D-06: setState(() => _isLoading = true); await Future.delayed(const Duration(milliseconds: 500)); ref.read(userNotifierProvider.notifier).login() (ref.read em handler — NUNCA ref.watch); if (mounted) setState(() => _isLoading = false) (Pitfall 2 — widget pode estar desmontado após redirect). NÃO chamar context.go('/home') — o RouterNotifier redireciona automaticamente (D-07).

    build() retorna Scaffold(backgroundColor: AppColors.background) com body Stack na ORDEM crítica (Pitfall 4): (1) Positioned blob 1 superior-direito ClipOval Container 200x200 cor AppColors.primary.withValues(alpha: 0.25); (2) Positioned blob 2 inferior-esquerdo ClipOval Container 120x120 cor AppColors.primary.withValues(alpha: 0.15); (3) Positioned.fill BackdropFilter(filter: ImageFilter.blur(sigmaX: 60, sigmaY: 60), child: Container(color: Colors.transparent)) — embaça os blobs ABAIXO, não o card; (4) Center SingleChildScrollView com Column.

    A Column central per UI-SPEC Layout Structure: Text('Lista Smart', style displaySmall do Theme.of(context).textTheme, color AppColors.primary) per D-02; SizedBox spacingS; Text('Faça compras mais inteligentes', style titleMedium, color textSecondary); SizedBox spacingXL; card glassmorphic Container(decoration: BoxDecoration(color: AppColors.surface.withValues(alpha: 0.7), borderRadius: BorderRadius.circular(AppSizes.radiusXL), border: Border.all(color: Colors.white.withValues(alpha: 0.1), width: 1.0)), padding: EdgeInsets.all(AppSizes.spacingL)) per D-01.

    Dentro do card: TextFormField e-mail (controller _emailController, hintText 'seu@email.com', prefixIcon Icon(LucideIcons.mail, color textSecondary), borders OutlineInputBorder radiusM, focusedBorder borderSide AppColors.primary) per D-03 ícones; SizedBox spacingM; TextFormField senha (controller _passwordController, obscureText: !_isPasswordVisible, prefixIcon LucideIcons.lock, suffixIcon IconButton com Icon(_isPasswordVisible ? LucideIcons.eyeOff : LucideIcons.eye) + Tooltip 'Mostrar senha'/'Ocultar senha', onPressed setState toggle) per D-03; SizedBox spacingL; bloco condicional _isLoading ? SizedBox(height: 48, child: Center(child: CircularProgressIndicator(color: AppColors.primary))) : SizedBox(width: double.infinity, child: FilledButton(onPressed: _handleLogin, style: FilledButton.styleFrom(backgroundColor: AppColors.primary, foregroundColor: AppColors.background, padding vertical spacingM, shape RoundedRectangleBorder radiusL), child: Text('Avançar'))) per D-04, D-06.

    Sem validação de formulário (D-05) — campos vazios são aceitos. Usar SOMENTE withValues, nunca withOpacity (Pitfall 5). Sem valores literais de spacing/radius — usar tokens AppSizes.
  </action>
  <verify>
    <automated>cd /d P:\ && flutter analyze lib/features/auth/</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze lib/features/auth/` sai 0 sem erros nem warnings
    - grep `class LoginScreen extends ConsumerStatefulWidget` encontra match
    - grep `ImageFilter.blur` encontra match em login_screen.dart
    - grep `LucideIcons.mail` e `LucideIcons.lock` ambos encontram match
    - grep `LucideIcons.eye` e `LucideIcons.eyeOff` ambos encontram match
    - grep `ref.read(userNotifierProvider.notifier)` encontra match
    - grep `ref.watch` NÃO encontra match em login_screen.dart (ref.watch proibido em handler/build desta tela)
    - grep `Future.delayed(const Duration(milliseconds: 500))` encontra match
    - grep `if (mounted)` encontra match (guarda setState pós-await)
    - grep `withOpacity` NÃO encontra match (apenas withValues)
    - grep `context.go` NÃO encontra match (redirect é automático)
    - grep `FilledButton.styleFrom` com `backgroundColor: AppColors.primary` presente
  </acceptance_criteria>
  <done>LoginScreen renderiza título + subtítulo + card glassmorphic com campos Lucide e toggle de senha sobre blobs desfocados; "Avançar" dispara loading de 500ms e chama login() sem navegação manual.</done>
</task>

<task type="auto">
  <name>Task 2: Teste de widget da LoginScreen (AUTH-01)</name>
  <files>test/widgets/login_screen_test.dart</files>
  <read_first>
    - lib/features/auth/presentation/login_screen.dart (tela criada na Task 1 — alvo do teste)
    - lib/core/persistence/shared_preferences_provider.dart (provider a sobrescrever no ProviderScope)
    - test/repositories/shared_prefs_test.dart (padrão setMockInitialValues + override)
    - .planning/phases/02-auth-state-layer/02-PATTERNS.md (seção test files — padrão pumpWidget com ProviderScope + MaterialApp)
    - .planning/phases/02-auth-state-layer/02-VALIDATION.md (AUTH-01 → test/widgets/login_screen_test.dart)
  </read_first>
  <action>
    Criar test/widgets/login_screen_test.dart com testWidgets cobrindo AUTH-01: SharedPreferences.setMockInitialValues({}); final prefs = await SharedPreferences.getInstance(); pumpWidget(ProviderScope(overrides: [sharedPreferencesProvider.overrideWithValue(prefs)], child: const MaterialApp(home: LoginScreen()))). Asserts: find.text('Lista Smart') findsOneWidget; find.text('Avançar') findsOneWidget; find.byIcon(LucideIcons.mail) findsOneWidget; find.byIcon(LucideIcons.lock) findsOneWidget; find.byType(TextFormField) findsNWidgets(2); o campo de senha começa com toggle eyeOff visível (find.byIcon(LucideIcons.eye) findsOneWidget porque obscureText=true mostra o ícone que revela). Adicionar um segundo testWidgets: toque no IconButton de toggle de senha (tester.tap + pump) alterna o ícone visível. Usar MaterialApp(theme: ...) só se necessário para displaySmall; um MaterialApp simples é suficiente para os finds.
  </action>
  <verify>
    <automated>cd /d P:\ && flutter test test/widgets/login_screen_test.dart --no-pub</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/widgets/login_screen_test.dart` sai 0 com os testes passando
    - O teste afirma find.text('Lista Smart'), find.text('Avançar'), find.byIcon(LucideIcons.mail), find.byIcon(LucideIcons.lock), find.byType(TextFormField) findsNWidgets(2)
    - Teste de toggle de senha verifica troca de ícone após tap
  </acceptance_criteria>
  <done>Teste de widget verde provando que a LoginScreen renderiza os campos, ícones Lucide e botão "Avançar" conforme AUTH-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Usuário → LoginScreen → UserNotifier | Gesto de UI dispara mutação de estado de auth global |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Denial of Service | _handleLogin() setState após dispose | mitigate | guarda `if (mounted)` antes de setState pós-await (Pitfall 2) — evita FlutterError de setState após dispose quando o redirect desmonta a tela |
| T-02-05 | Spoofing | Auth simulada sem validação (D-05) | accept | Decisão do projeto acadêmico: login 100% simulado, qualquer input aceito; sem credenciais reais nem backend (RESEARCH §Security Domain V5 não aplicável) |
| T-02-SC | Tampering | npm/pub installs | accept | Nenhum pacote novo instalado nesta fase (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- `flutter analyze lib/features/auth/` sai 0
- `flutter test test/widgets/login_screen_test.dart --no-pub` sai 0
- Verificação humana funcional (toque real em "Avançar" → spinner → navega para Home) é coberta no gate de end-of-phase via `<human-check>` abaixo, dado human_verify_mode=end-of-phase
- Nota de ambiente: rodar via subst P: (RESEARCH §Environment Availability)
</verification>

<success_criteria>
- LoginScreen glassmorphic completa substitui o placeholder (AUTH-01)
- Campos e-mail/senha com ícones Lucide e toggle de visibilidade (D-03)
- Blobs desfocados via BackdropFilter na ordem correta (D-01)
- Botão "Avançar" com loading 500ms chama login() (D-04, D-06)
- Sem context.go manual — redirect automático (D-07)
- Teste de widget verde (AUTH-01)
</success_criteria>

<verification>
<human-check>
Após o merge da fase, verificar no dispositivo/emulador:
1. App inicia na tela de Login (rota /login) com fundo #09090B, blobs verde-limão desfocados e card glassmorphic central
2. Campo e-mail mostra ícone de envelope (mail); campo senha mostra cadeado (lock) e botão de olho que alterna visibilidade do texto
3. Tocar "Avançar" → spinner verde aparece por ~500ms → app navega automaticamente para a aba Home (bottom nav visível)
4. Após login, pressionar voltar NÃO retorna para a tela de login
</human-check>
</verification>

<output>
Create `.planning/phases/02-auth-state-layer/02-02-SUMMARY.md` when done
</output>
