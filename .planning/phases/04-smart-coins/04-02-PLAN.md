---
phase: 04-smart-coins
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - lib/features/smart_coins/presentation/store_screen.dart
  - test/features/smart_coins/store_screen_test.dart
autonomous: true
requirements:
  - COIN-01
  - COIN-02
  - COIN-03
  - COIN-04
must_haves:
  truths:
    - "StoreScreen exibe badge de nível (Bronze/Prata/Ouro) com cor correspondente ao saldo do coinProvider"
    - "TweenAnimationBuilder anima a LinearProgressIndicator de 0.0 até coinLevelProgress(balance) ao entrar na tela"
    - "Grid 3-colunas com 3 PackageCard: 100 moedas, 500 moedas (+50 bônus), 1000 moedas (+200 bônus); botão 'Obter' chama coinProvider.notifier.addCoins() creditando as moedas no saldo com transação registrada"
    - "Nível Ouro exibe barra 100% + texto 'Nível máximo atingido' no lugar do indicador de próximo nível"
    - "ListView de CoinTransaction mostra últimas 10 transações; amount > 0 com LucideIcons.plus e AppColors.success; amount < 0 com LucideIcons.minus e AppColors.error"
    - "Saldo exibido como ícone LucideIcons.coins + número + 'Smart Coins' em linha única no header"
    - "flutter test test/features/smart_coins/store_screen_test.dart passa cobrindo COIN-01/02/03/04"
  artifacts:
    - path: "lib/features/smart_coins/presentation/store_screen.dart"
      provides: "StoreScreen ConsumerWidget completo: header com saldo+nível+progresso, grid de pacotes, histórico de transações"
      contains: "class StoreScreen"
      min_lines: 150
    - path: "test/features/smart_coins/store_screen_test.dart"
      provides: "Widget tests cobrindo COIN-01 (nível badge), COIN-03 (3 pacotes), COIN-04 (histórico com cores)"
      contains: "StoreScreen"
      min_lines: 80
  key_links:
    - from: "lib/features/smart_coins/presentation/store_screen.dart"
      to: "coinProvider"
      via: "ref.watch(coinProvider) em build()"
      pattern: "ref\\.watch\\(coinProvider\\)"
    - from: "lib/features/smart_coins/presentation/store_screen.dart"
      to: "coinLevelOf + coinLevelProgress"
      via: "helpers top-level importados de coin_notifier.dart"
      pattern: "coinLevelOf"
---

<objective>
Redesenhar StoreScreen do placeholder atual para a tela completa de Smart Coins: header com saldo grande + badge de nível colorido + barra de progresso animada via TweenAnimationBuilder, seção de pacotes demonstrativos em grid 2 colunas, e histórico de transações com ícones coloridos por tipo (ganho/gasto).

Purpose: É a única entrega user-facing da Phase 4. Consome coinProvider (fonte única após Plan 01), coinLevelOf() e coinLevelProgress() para o sistema de níveis. Sem esta tela, os requisitos COIN-01 a COIN-04 ficam não atendidos.
Output: store_screen.dart redesenhado + 1 arquivo de teste verde.
</objective>

<execution_context>
@CLAUDE.md
@.planning/phases/04-smart-coins/04-CONTEXT.md
@.planning/phases/04-smart-coins/04-VALIDATION.md
@.planning/phases/04-smart-coins/04-01-SUMMARY.md
</execution_context>

<context>
@lib/features/smart_coins/presentation/store_screen.dart
@lib/core/providers/coin_notifier.dart
@lib/features/smart_coins/domain/coin_transaction.dart
@lib/core/constants/app_colors.dart
@lib/core/constants/app_sizes.dart
@lib/features/auth/presentation/login_screen.dart
@lib/core/persistence/shared_preferences_provider.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redesign StoreScreen — header (saldo + nível + progresso) + pacotes + histórico</name>
  <files>lib/features/smart_coins/presentation/store_screen.dart</files>
  <read_first>
    - lib/features/smart_coins/presentation/store_screen.dart (placeholder atual — substituir por completo)
    - lib/core/providers/coin_notifier.dart (após Plan 01 — coinProvider, CoinState, CoinLevel enum, coinLevelOf, coinLevelProgress)
    - lib/features/smart_coins/domain/coin_transaction.dart (campos: id, description, amount, createdAt)
    - lib/core/constants/app_colors.dart (tokens de cor)
    - lib/core/constants/app_sizes.dart (tokens de espaçamento)
    - lib/features/auth/presentation/login_screen.dart (padrão ConsumerWidget, FilledButton.styleFrom, glassmorphic Container)
    - .planning/phases/04-smart-coins/04-CONTEXT.md (D-02 cores de nível, D-03 pacotes, D-05 ícones histórico)
  </read_first>
  <behavior>
    - StoreScreen ConsumerWidget; `final state = ref.watch(coinProvider)` em build()
    - Layout: Scaffold + AppBar('Smart Coins') + SingleChildScrollView + Column com 3 seções

    **Seção 1 — Header glassmorphic:**
    - Container glassmorphic (surface.withValues(alpha:0.7), border white 0.1, radiusXL) com padding spacingL
    - Row: Icon(LucideIcons.coins, 32, primary) + SizedBox(spacingS) + RichText ou Row[Text(saldo, headline, primary, bold) + SizedBox(4) + Text('Smart Coins', bodyMedium, textSecondary)]
    - SizedBox(spacingM)
    - Row: badge de nível (Container rounded, cor de fundo = cor do nível 20% alpha, texto = nome do nível, cor do nível) + Spacer() + Text('próx. nível: X moedas', bodySmall, textSecondary) — se Ouro: Text('Nível máximo atingido', bodySmall, primary)
    - SizedBox(spacingS)
    - TweenAnimationBuilder<double>(tween: Tween(begin: 0.0, end: coinLevelProgress(state.balance)), duration: 600ms, builder: (_, value, __) => ClipRRect(radiusS, child: LinearProgressIndicator(value: value, backgroundColor: surfaceElevated, color: primary, minHeight: 8)))

    **Seção 2 — Pacotes:**
    - Text('Pacotes', titleMedium, textMain, bold) + SizedBox(spacingS)
    - GridView.count(crossAxisCount: 3, shrinkWrap: true, physics: NeverScrollableScrollPhysics, mainAxisSpacing: spacingS, crossAxisSpacing: spacingS, childAspectRatio: 0.75)
    - 3 PackageCard (widget privado): 100 moedas / 500 moedas (+50 bônus) / 1000 moedas (+200 bônus)
    - Cada PackageCard: Container glassmorphic + Column[Icon(LucideIcons.coins, primary) + Text(qtd, titleMedium, primary, bold) + if bônus > 0: Text('+N bônus', bodySmall, success) + Spacer() + FilledButton mini('Obter', primary→background) → ref.read(coinProvider.notifier).addCoins(coins + bonus, 'Pacote $coins moedas') + SnackBar('+${coins+bonus} moedas adicionados!', success)]

    **Seção 3 — Histórico:**
    - Text('Histórico', titleMedium, textMain, bold) + SizedBox(spacingS)
    - `final recent = state.transactions.take(10).toList()` — exibir apenas as 10 mais recentes
    - ListView.separated(shrinkWrap: true, physics: NeverScrollableScrollPhysics, itemCount: recent.length) — cada item: Container(surface, radiusM) Row[Icon (plus/minus, 20px, success/error) + Column(description bodyMedium textMain, date bodySmall textSecondary) + Spacer + Text('+N'/'-N', titleSmall, success/error, bold)]
    - Data formatada: DateFormat('dd/MM/yyyy', 'pt_BR') — usar intl
    - Se transactions.isEmpty: Center(Text('Nenhuma transação ainda', textSecondary))

    PROIBIDO: withOpacity, StateNotifierProvider, ref.watch fora de build, cores/spacings literais
  </behavior>
  <action>
    Substituir o conteúdo de lib/features/smart_coins/presentation/store_screen.dart por implementação completa conforme behavior. Imports: flutter/material, flutter_riverpod, lucide_icons, intl, core/constants/{app_colors,app_sizes}, core/providers/coin_notifier. Declarar widget privado _PackageCard({required int coins, required int bonus, required VoidCallback onObtain}) StatelessWidget. Usar TweenAnimationBuilder<double> com Duration(milliseconds: 600) e curva Curves.easeOut no builder do LinearProgressIndicator.
  </action>
  <verify>
    <automated>flutter analyze lib/features/smart_coins/presentation/store_screen.dart</automated>
    <automated>flutter test</automated>
  </verify>
  <acceptance_criteria>
    - flutter analyze 0 errors
    - grep "TweenAnimationBuilder" lib/features/smart_coins/presentation/store_screen.dart retorna ≥ 1 match
    - grep "coinLevelOf" lib/features/smart_coins/presentation/store_screen.dart retorna ≥ 1 match
    - grep "coinLevelProgress" lib/features/smart_coins/presentation/store_screen.dart retorna ≥ 1 match
    - grep "LucideIcons.coins" lib/features/smart_coins/presentation/store_screen.dart retorna ≥ 1 match
    - grep -vE '^\s*//' lib/features/smart_coins/presentation/store_screen.dart | grep -c "withOpacity" retorna 0
    - grep "'Demonstrativo'" lib/features/smart_coins/presentation/store_screen.dart retorna ≥ 1 match
    - flutter test suite passa sem regressões
  </acceptance_criteria>
  <done>StoreScreen redesenhado com header (saldo+nível+progresso animado), grid de 3 pacotes demonstrativos, histórico de transações com cores; analyze limpo; suite verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widget tests cobrindo COIN-01/02/03/04</name>
  <files>test/features/smart_coins/store_screen_test.dart</files>
  <read_first>
    - lib/features/smart_coins/presentation/store_screen.dart (versão recém-criada)
    - lib/core/providers/coin_notifier.dart (coinProvider, CoinState, CoinLevel)
    - lib/features/smart_coins/domain/coin_transaction.dart (constructor para fixtures)
    - lib/core/persistence/shared_preferences_provider.dart (override pattern)
    - test/features/shopping_list/shopping_list_screen_test.dart (padrão de ProviderScope + SharedPreferences mock)
  </read_first>
  <behavior>
    - setUpAll: WidgetsFlutterBinding.ensureInitialized + SharedPreferences.setMockInitialValues({})
    - Cada testWidgets: ProviderContainer com override de sharedPreferencesProvider + coinProvider.overrideWith para injetar CoinState fixture
    - test COIN-01: coinState com balance=750 → expect find.text('750') + find.text('Prata') (badge de nível) findsWidgets
    - test COIN-01 bronze: coinState com balance=100 → expect find.text('Bronze') findsOneWidget
    - test COIN-01 ouro: coinState com balance=2000 → expect find.text('Ouro') findsOneWidget
    - test COIN-03: expect find.text('100') + find.text('500') + find.text('1000') e find.text('Demonstrativo') findNWidgets(3)
    - test COIN-04: coinState com 1 transação amount=+250 e 1 amount=-50 → expect find.text('+250') e find.text('-50') findsOneWidget each
    - test COIN-04 empty: coinState sem transactions → expect find.text('Nenhuma transação ainda') findsOneWidget
    - Override de coinProvider: `coinProvider.overrideWith(() => CoinNotifier()..state = CoinState(balance: X, transactions: [...]))`
      OU usar `ProviderContainer` + `container.read(coinProvider.notifier).addCoins(...)` para seed
  </behavior>
  <action>
    Criar test/features/smart_coins/ (se não existir) e test/features/smart_coins/store_screen_test.dart com os testWidgets acima. Cada teste monta MaterialApp(home: UncontrolledProviderScope(container: container, child: StoreScreen())). Importar: flutter_test, flutter_riverpod, shared_preferences, lista_smart providers, CoinTransaction.
  </action>
  <verify>
    <automated>flutter test test/features/smart_coins/store_screen_test.dart</automated>
    <automated>flutter test</automated>
  </verify>
  <acceptance_criteria>
    - flutter test test/features/smart_coins/store_screen_test.dart exits 0 com ≥ 6 tests passing
    - grep "testWidgets" test/features/smart_coins/store_screen_test.dart retorna ≥ 6 matches
    - grep -E "(COIN-01|COIN-02|COIN-03|COIN-04)" test/features/smart_coins/store_screen_test.dart retorna ≥ 4 matches (req IDs citados)
    - flutter test suite completa exits 0
  </acceptance_criteria>
  <done>Widget tests cobrem COIN-01 (3 níveis), COIN-03 (3 pacotes + chip Demonstrativo), COIN-04 (histórico com cores + empty state); suite verde.</done>
</task>

</tasks>

<verification>
- flutter analyze lib/ 0 errors
- flutter test verde (suite completa)
- StoreScreen visualmente completa: saldo visível, badge de nível, barra de progresso, 3 pacotes, histórico
- TweenAnimationBuilder presente e animando LinearProgressIndicator
</verification>

<success_criteria>
- COIN-01: badge Bronze/Prata/Ouro com cor correspondente ao saldo
- COIN-02: TweenAnimationBuilder com LinearProgressIndicator animado de 0 → coinLevelProgress(balance)
- COIN-03: 3 PackageCard em grid 3 colunas com chip Demonstrativo e SnackBar no botão Obter
- COIN-04: ListView de CoinTransaction com ícones/cores por tipo (verde=ganho, vermelho=gasto)
- Suite completa verde
</success_criteria>

<output>
Criar .planning/phases/04-smart-coins/04-02-SUMMARY.md ao finalizar.
</output>
