---
phase: 04-smart-coins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/providers/coin_notifier.dart
  - lib/features/shopping_list/presentation/shopping_list_screen.dart
  - test/providers/coin_notifier_test.dart
autonomous: true
requirements:
  - COIN-01
  - COIN-02
  - COIN-04
must_haves:
  truths:
    - "CoinNotifier.spendCoins(amount, description) decrementa balance, insere CoinTransaction com amount negativo no início da lista e chama _persist()"
    - "CoinLevel enum tem 3 valores: bronze, prata, ouro; helper coinLevelOf(int balance) retorna o nível correto baseado nos thresholds D-02"
    - "coinLevelProgress(int balance) retorna double 0.0–1.0 representando progresso até o próximo nível"
    - "ShoppingListScreen lê saldo de coinProvider.balance (não userNotifierProvider.coinBalance)"
    - "flutter test suite completa passa sem regressões"
  artifacts:
    - path: "lib/core/providers/coin_notifier.dart"
      provides: "CoinNotifier.spendCoins + CoinLevel enum + coinLevelOf() + coinLevelProgress()"
      contains: "void spendCoins"
    - path: "lib/features/shopping_list/presentation/shopping_list_screen.dart"
      provides: "Lê coinProvider.balance para exibir saldo e verificar suficiência"
      contains: "coinProvider"
  key_links:
    - from: "lib/features/shopping_list/presentation/shopping_list_screen.dart"
      to: "coinProvider"
      via: "ref.watch(coinProvider).balance"
      pattern: "coinProvider"
---

<objective>
Tornar coinProvider a fonte única de verdade para saldo de moedas: adicionar CoinNotifier.spendCoins(), definir o sistema de níveis (CoinLevel enum + helpers), e atualizar ShoppingListScreen para ler saldo de coinProvider em vez de userNotifierProvider.

Purpose: Plan 02 depende de coinLevelOf() e coinLevelProgress() para renderizar a barra de progresso e o badge de nível. ShoppingListScreen deve consumir coinProvider para que a dedução de moedas na comparação seja consistente com o histórico exibido na StoreScreen.
Output: coin_notifier.dart com 2 métodos novos + 3 helpers, shopping_list_screen.dart atualizado, testes verdes.
</objective>

<execution_context>
@CLAUDE.md
@.planning/phases/04-smart-coins/04-CONTEXT.md
@.planning/phases/04-smart-coins/04-VALIDATION.md
</execution_context>

<context>
@lib/core/providers/coin_notifier.dart
@lib/features/shopping_list/presentation/shopping_list_screen.dart
@lib/core/providers/user_notifier.dart
@lib/features/smart_coins/domain/coin_transaction.dart
@lib/core/constants/app_colors.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend CoinNotifier with spendCoins + CoinLevel system</name>
  <files>lib/core/providers/coin_notifier.dart</files>
  <read_first>
    - lib/core/providers/coin_notifier.dart (estado atual — addCoins como analog para spendCoins)
    - lib/features/smart_coins/domain/coin_transaction.dart (constructor de CoinTransaction)
    - .planning/phases/04-smart-coins/04-CONTEXT.md (D-02 thresholds, D-04 spendCoins spec)
  </read_first>
  <behavior>
    - CoinLevel enum no topo do arquivo: `enum CoinLevel { bronze, prata, ouro }`
    - Função top-level `CoinLevel coinLevelOf(int balance)`: balance < 500 → bronze; 500–1499 → prata; ≥ 1500 → ouro
    - Função top-level `double coinLevelProgress(int balance)`: bronze → balance/500.0 clamp(0,1); prata → (balance-500)/1000.0 clamp(0,1); ouro → 1.0
    - `void spendCoins(int amount, String description)` em CoinNotifier: mesmo padrão de addCoins mas amount negativo; guard `if (state.balance < amount) return` (não deixa balance negativo); registra CoinTransaction(amount: -amount); decrementa balance; chama _persist()
  </behavior>
  <action>
    Adicione no topo de lib/core/providers/coin_notifier.dart (antes da classe CoinState):
    1. `enum CoinLevel { bronze, prata, ouro }`
    2. `CoinLevel coinLevelOf(int balance)` com os 3 thresholds de D-02
    3. `double coinLevelProgress(int balance)` com a fórmula de D-02

    Adicione dentro de CoinNotifier (após addCoins):
    4. `void spendCoins(int amount, String description)` — guard amount ≤ 0 retorna (não faz nada); guard state.balance < amount retorna (saldo insuficiente silencioso); cria CoinTransaction com amount: -amount; atualiza state com balance decrementado e tx no início da lista; chama _persist().

    Remova ou ignore UserNotifier.spendCoins() — NÃO editar user_notifier.dart (manter retrocompat com qualquer código existente que use); ShoppingListScreen será atualizado na Task 2.
  </action>
  <verify>
    <automated>flutter analyze lib/core/providers/coin_notifier.dart</automated>
    <automated>flutter test test/providers/coin_notifier_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - flutter analyze reporta 0 errors
    - grep "enum CoinLevel" lib/core/providers/coin_notifier.dart retorna 1 match
    - grep "void spendCoins" lib/core/providers/coin_notifier.dart retorna 1 match
    - grep "CoinLevel coinLevelOf" lib/core/providers/coin_notifier.dart retorna 1 match
    - grep "double coinLevelProgress" lib/core/providers/coin_notifier.dart retorna 1 match
    - test/providers/coin_notifier_test.dart existente continua passando (sem regressões)
  </acceptance_criteria>
  <done>CoinLevel enum + coinLevelOf + coinLevelProgress + spendCoins adicionados; coin_notifier_test verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update ShoppingListScreen to read from coinProvider + extend coin_notifier_test</name>
  <files>lib/features/shopping_list/presentation/shopping_list_screen.dart, test/providers/coin_notifier_test.dart</files>
  <read_first>
    - lib/features/shopping_list/presentation/shopping_list_screen.dart (versão atual — busca `userNotifierProvider` para coinBalance)
    - lib/core/providers/coin_notifier.dart (após Task 1 — coinProvider + CoinState.balance)
    - test/providers/coin_notifier_test.dart (testes existentes — padrão de ProviderContainer + override)
  </read_first>
  <behavior>
    - ShoppingListScreen adiciona import de coin_notifier.dart
    - `ref.watch(coinProvider).balance` substitui `user?.coinBalance ?? 0` para o chip de saldo no AppBar
    - `hasEnoughCoins` usa `coinProvider.balance >= _comparisonCoinCost`
    - `_compareWithLoading()` chama `ref.read(coinProvider.notifier).spendCoins(_comparisonCoinCost, 'Comparação de supermercados')` em vez de `ref.read(userNotifierProvider.notifier).spendCoins(...)`
    - Import de user_notifier pode ser mantido se userNotifierProvider ainda é usado para o badge de iniciais (não remover se existir outro uso)
    - Novo teste em coin_notifier_test.dart: 'spendCoins() decrementa balance e registra transação negativa'
    - Novo teste: 'spendCoins() é no-op quando balance insuficiente'
  </behavior>
  <action>
    Em lib/features/shopping_list/presentation/shopping_list_screen.dart:
    1. Adicione import `'../../../core/providers/coin_notifier.dart'`
    2. Em build(): adicione `final coinBalance = ref.watch(coinProvider).balance;`
    3. Substitua qualquer `user?.coinBalance ?? 0` por `coinBalance`
    4. Em `_compareWithLoading()`: substitua a chamada `userNotifierProvider.notifier.spendCoins(...)` por `ref.read(coinProvider.notifier).spendCoins(_comparisonCoinCost, 'Comparação de supermercados')`
    5. Remova o import de user_notifier se não for mais usado; mantenha se ainda houver outro uso (ex: badge de iniciais)

    Em test/providers/coin_notifier_test.dart:
    6. Adicione grupo 'spendCoins' com 2 testes usando ProviderContainer + sharedPreferencesProvider override + addCoins para seed de saldo
  </action>
  <verify>
    <automated>flutter analyze lib/features/shopping_list/presentation/shopping_list_screen.dart lib/core/providers/coin_notifier.dart</automated>
    <automated>flutter test test/providers/coin_notifier_test.dart</automated>
    <automated>flutter test</automated>
  </verify>
  <acceptance_criteria>
    - flutter analyze 0 errors
    - grep "coinProvider" lib/features/shopping_list/presentation/shopping_list_screen.dart retorna ≥ 2 matches
    - grep "userNotifierProvider.notifier.*spendCoins" lib/features/shopping_list/presentation/shopping_list_screen.dart retorna 0 matches
    - coin_notifier_test passa com ≥ 2 novos testes de spendCoins
    - flutter test suite completa passa (0 regressões)
  </acceptance_criteria>
  <done>ShoppingListScreen lê saldo de coinProvider; spendCoins usa CoinNotifier; 2 novos testes verdes; suite completa verde.</done>
</task>

</tasks>

<verification>
- flutter analyze lib/ sem erros
- flutter test verde (suite completa)
- CoinLevel enum exportado e acessível de qualquer widget
- coinProvider é a única fonte de saldo em ShoppingListScreen
</verification>

<success_criteria>
- CoinNotifier tem spendCoins + CoinLevel enum + 2 helpers prontos para consumo pelo Plan 02
- ShoppingListScreen consistente com coinProvider
- Suite de testes verde sem regressões
</success_criteria>

<output>
Criar .planning/phases/04-smart-coins/04-01-SUMMARY.md ao finalizar.
</output>
