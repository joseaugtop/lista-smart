---
phase: 05-price-registration-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/core/constants/app_strings.dart
  - lib/features/price_registration/presentation/scanner_screen.dart
  - test/features/scanner/scanner_screen_test.dart
  - test/providers/coin_notifier_test.dart
autonomous: true
requirements: [PREG-01, PREG-02, PREG-03, PREG-04]
must_haves:
  truths:
    - "User taps QR Code or Foto do Cupom and sees a 2s loading overlay before advancing (PREG-01)"
    - "User sees a mock receipt with supermarket, date, total R$87,43 and 3-4 line items, with a Confirmar e Ganhar Moedas button (PREG-02)"
    - "Confirming the receipt awards +10 coins with description 'Cadastro de nota fiscal' and records the transaction (PREG-04)"
    - "Step 3 shows ConfettiWidget, a +10 Smart Coins headline, and return buttons (PREG-03)"
  artifacts:
    - path: "lib/features/price_registration/presentation/scanner_screen.dart"
      provides: "3-page PageView scan wizard (ConsumerStatefulWidget)"
      contains: "class ScannerScreen extends ConsumerStatefulWidget"
    - path: "lib/core/constants/app_strings.dart"
      provides: "Shared scan transaction description constant"
      contains: "scanReceiptDescription"
    - path: "test/features/scanner/scanner_screen_test.dart"
      provides: "Widget tests for PREG-01, PREG-02, PREG-03"
      contains: "ScannerScreen"
    - path: "pubspec.yaml"
      provides: "confetti dependency"
      contains: "confetti:"
  key_links:
    - from: "scanner_screen.dart Step 2 confirm handler"
      to: "coinProvider.notifier.addCoins"
      via: "ref.read in onPressed"
      pattern: "addCoins\\(10, AppStrings.scanReceiptDescription\\)"
    - from: "scanner_screen.dart Step 3"
      to: "ConfettiWidget"
      via: "confettiController.play in addPostFrameCallback"
      pattern: "ConfettiWidget"
---

<objective>
Deliver the complete ScannerScreen vertical slice: a 3-step receipt-registration wizard (choose method → confirm mock receipt → confetti celebration) that awards +10 Smart Coins on confirmation.

Purpose: Implements PREG-01..04 — the price-registration loop that drives the gamification data-contribution incentive. After this plan, a user on the Scanner tab can scan a (simulated) receipt, confirm the mocked note, earn coins, and see a celebration, all end-to-end.
Output: pubspec confetti dependency, a shared scan-description constant, Wave 0 scanner widget-test stubs + a PREG-04 coin-description test, and the full ScannerScreen replacing the placeholder.
</objective>

<execution_context>
@C:/Users/Jose Augusto/Desktop/Unesc/Fase 5/Desenvolvimento Mobile/gsd/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jose Augusto/Desktop/Unesc/Fase 5/Desenvolvimento Mobile/gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-price-registration-profile/05-CONTEXT.md
@.planning/phases/05-price-registration-profile/05-RESEARCH.md
@.planning/phases/05-price-registration-profile/05-PATTERNS.md
@.planning/phases/05-price-registration-profile/05-UI-SPEC.md
@.planning/phases/05-price-registration-profile/05-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add confetti dependency + shared scan-description constant</name>
  <files>pubspec.yaml, lib/core/constants/app_strings.dart</files>
  <read_first>
    - pubspec.yaml (current dependency block — add under intl, before dev_dependencies)
    - .planning/phases/05-price-registration-profile/05-RESEARCH.md (Package Legitimacy Audit — confetti approved; New Dependency section)
    - lib/core/constants/app_colors.dart (abstract-class constant-file convention to mirror)
  </read_first>
  <action>
    In pubspec.yaml, add `confetti: ^0.7.0` to the `dependencies:` block, on the line immediately after `intl: ^0.19.0`. Do NOT modify any other dependency line. Per RESEARCH D-03 the `^0.7.0` constraint resolves to 0.7.0 exactly (Dart semver `>=0.7.0 <0.8.0`); confetti is audited and approved in 05-RESEARCH.md Package Legitimacy Audit (funwith.app verified publisher, MIT, ~4 years old).
    Create lib/core/constants/app_strings.dart following the `abstract class` + `static const` convention used in app_colors.dart. Declare `abstract class AppStrings` with a single member `static const String scanReceiptDescription = 'Cadastro de nota fiscal';`. This is the single source of truth shared between ScannerScreen (Task 4, addCoins call per D-04) and ProfileScreen scan-count derivation (Plan 02, D-09) — the exact string must match.
    Run `flutter pub get` so the confetti package is fetched and the lockfile updates.
  </action>
  <verify>
    <automated>flutter pub get</automated>
    <automated>powershell -Command "Select-String -Path pubspec.yaml -Pattern 'confetti: \^0.7.0' -Quiet"</automated>
    <automated>powershell -Command "Select-String -Path lib/core/constants/app_strings.dart -Pattern \"scanReceiptDescription = 'Cadastro de nota fiscal'\" -Quiet"</automated>
  </verify>
  <acceptance_criteria>
    - `flutter pub get` exits 0 and resolves confetti to 0.7.0
    - `pubspec.yaml` contains exactly `confetti: ^0.7.0` in the dependencies block
    - `lib/core/constants/app_strings.dart` defines `AppStrings.scanReceiptDescription` equal to `'Cadastro de nota fiscal'`
  </acceptance_criteria>
  <done>confetti ^0.7.0 is a resolved dependency and AppStrings.scanReceiptDescription is the shared constant for the scan transaction description.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 — scanner widget-test stubs + PREG-04 coin-description test</name>
  <files>test/features/scanner/scanner_screen_test.dart, test/providers/coin_notifier_test.dart</files>
  <read_first>
    - test/features/smart_coins/store_screen_test.dart (analog: _makeContainer + _pumpScreen helpers, UncontrolledProviderScope, setUpAll/setUp with SharedPreferences.setMockInitialValues)
    - test/providers/coin_notifier_test.dart (existing structure — add PREG-04 test to the existing group)
    - lib/core/providers/coin_notifier.dart (addCoins signature: addCoins(int amount, String description); CoinState.transactions)
    - lib/core/providers/user_notifier.dart (ScannerScreen reads coinProvider; user must be logged in via userNotifierProvider.notifier.login())
    - .planning/phases/05-price-registration-profile/05-VALIDATION.md (Wave 0 Requirements; Per-Task Verification Map)
    - .planning/phases/05-price-registration-profile/05-UI-SPEC.md (Section 6.1 — exact text labels the tests will assert on)
  </read_first>
  <behavior>
    coin_notifier_test.dart (PREG-04, runs now — must pass GREEN against existing addCoins):
    - Test 'addCoins records transaction with scan description': login user, call addCoins(10, AppStrings.scanReceiptDescription), assert balance increased by 10 and transactions.first.description == AppStrings.scanReceiptDescription and transactions.first.amount == 10.
    scanner_screen_test.dart (PREG-01/02/03, stubs — marked skip: true until Task 3-4 land):
    - Test 'PREG-01: Step 1 renders QR Code and Camera method cards': pump ScannerScreen, expect find.text('Escanear QR Code') and find.text('Foto do Cupom').
    - Test 'PREG-02: Step 2 shows receipt fields and confirm button': drive to page 1, expect find.text('Bistek Supermercados'), find.text('R\$ 87,43'), find.text('Confirmar e Ganhar Moedas').
    - Test 'PREG-03: Step 3 shows ConfettiWidget and +10 Smart Coins': drive to page 2, expect find.byType(ConfettiWidget) and find.text('+10 Smart Coins').
  </behavior>
  <action>
    In test/providers/coin_notifier_test.dart, add ONE test inside the existing group covering PREG-04: import lista_smart/core/constants/app_strings.dart and lista_smart/core/providers/user_notifier.dart; create a ProviderContainer with sharedPreferencesProvider override (SharedPreferences.setMockInitialValues({}) + getInstance), call userNotifierProvider.notifier.login(), then coinProvider.notifier.addCoins(10, AppStrings.scanReceiptDescription); assert the new transaction's description and amount and that balance increased by 10. This test MUST pass now (GREEN) since addCoins already exists. Do not modify existing tests.
    Create test/features/scanner/scanner_screen_test.dart using the store_screen_test.dart helper shape: a `_makeContainer()` that overrides sharedPreferencesProvider and calls userNotifierProvider.notifier.login() (ScannerScreen needs a logged-in user for coins), and a `_pumpScreen(tester, container)` that wraps `const ScannerScreen()` in UncontrolledProviderScope + MaterialApp and pumpAndSettle. Add the three PREG tests described in <behavior>, each marked `skip: true` — they assert against the exact UI-SPEC Section 6.1 text labels so the executor knows the contract before building Task 3-4. Import lista_smart/features/price_registration/presentation/scanner_screen.dart and package:confetti/confetti.dart (for ConfettiWidget finder).
    Do NOT place fenced code in production files. These are test files only.
  </action>
  <verify>
    <automated>flutter test test/providers/coin_notifier_test.dart --no-pub</automated>
    <automated>flutter test test/features/scanner/scanner_screen_test.dart --no-pub</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/providers/coin_notifier_test.dart` passes including the new PREG-04 description test
    - `flutter test test/features/scanner/scanner_screen_test.dart` runs with the 3 PREG tests reported as skipped (not failing)
    - scanner_screen_test.dart imports scanner_screen.dart and confetti.dart and uses UncontrolledProviderScope with a logged-in user
  </acceptance_criteria>
  <done>PREG-04 is covered by a passing coin-description test; PREG-01/02/03 widget-test stubs exist (skipped) defining the UI contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ScannerScreen Step 1 + Step 2 (method cards, loading overlay, mock receipt, coin award)</name>
  <files>lib/features/price_registration/presentation/scanner_screen.dart</files>
  <read_first>
    - lib/features/price_registration/presentation/scanner_screen.dart (placeholder to replace)
    - lib/features/shopping_list/presentation/shopping_list_screen.dart (loading-overlay Stack pattern, _compareWithLoading async + `if (!mounted) return`, glassmorphic card, NumberFormat BRL)
    - lib/core/providers/coin_notifier.dart (coinProvider.notifier.addCoins)
    - lib/core/constants/app_strings.dart (scanReceiptDescription — created Task 1)
    - lib/core/constants/app_colors.dart and lib/core/constants/app_sizes.dart (all color/spacing/radius tokens — no literals)
    - lib/core/data/mock_data.dart (MockData.products — Step 2 line items: use first 4 by name/averagePrice)
    - .planning/phases/05-price-registration-profile/05-UI-SPEC.md (Section 6.1 Page 0 + Page 1; Section 3-5 spacing/typography/color; Section 11 anti-patterns)
    - .planning/phases/05-price-registration-profile/05-RESEARCH.md (Pattern 1, 2, 4; Pitfall 2, 5; ScannerScreen skeleton)
  </read_first>
  <behavior>
    - Page 0 renders two glassmorphic method cards: "Escanear QR Code" (LucideIcons.qrCode) and "Foto do Cupom" (LucideIcons.camera), both GestureDetector-wrapped calling _startScan().
    - Tapping a card sets _loading=true, shows a full-screen Container(color: Colors.black.withValues(alpha:0.6)) overlay with a centered CircularProgressIndicator(color: AppColors.primary) for 2 seconds, then advances to page 1.
    - Page 1 renders a glassmorphic _ReceiptCard: "Bistek Supermercados", today's date via DateFormat('dd/MM/yyyy'), 3-4 line items from MockData.products, total "R$ 87,43" via NumberFormat BRL, and an ElevatedButton "Confirmar e Ganhar Moedas".
    - Confirming calls ref.read(coinProvider.notifier).addCoins(10, AppStrings.scanReceiptDescription) then advances to page 2.
  </behavior>
  <action>
    Replace the placeholder ScannerScreen with a ConsumerStatefulWidget per RESEARCH Pattern 1. State class holds `late final PageController _pageController`, `late final ConfettiController _confettiController`, `bool _loading = false`. Initialize both controllers in initState (`PageController()`, `ConfettiController(duration: const Duration(seconds: 3))`) and dispose both in dispose(). The ConfettiController is created here (Step 3 wiring lands in Task 4) so ownership is correct — only this widget disposes it.
    build() returns a PopScope(canPop: false, onPopInvokedWithResult: ...) wrapping a Stack of [Scaffold(backgroundColor: AppColors.background, body: PageView(controller: _pageController, physics: const NeverScrollableScrollPhysics(), children: [step1, step2, step3placeholder])), if(_loading) overlay]. PopScope handler: `final page = _pageController.page?.round() ?? 0;` (Pitfall 5 null-safe), if page>0 call _pageController.previousPage(300ms, easeInOut) else context.pop().
    `_startScan()` async: setState(_loading=true); await Future<void>.delayed(const Duration(seconds: 2)); `if (!mounted) return;` (Pitfall 2); setState(_loading=false); _pageController.nextPage(duration: const Duration(milliseconds: 300), curve: Curves.easeInOut).
    `_confirmReceipt()`: ref.read(coinProvider.notifier).addCoins(10, AppStrings.scanReceiptDescription); _pageController.nextPage(300ms, easeInOut).
    Build Step 1 (Page 0) per UI-SPEC Section 6.1 Page 0: centered Column with header (LucideIcons.fileSearch 48px primary, "Escanear Nota Fiscal" headlineMedium, subtitle bodyMedium textSecondary), spacingXL gap, then two glassmorphic _MethodCard widgets (recipe: AppColors.surface.withValues(alpha:0.7), Border.all(Colors.white.withValues(alpha:0.1)), radiusXL) each height ~100, Row of [36px primary icon, spacingM, Column(title titleMedium w700, subtitle bodySmall textSecondary)], GestureDetector onTap: _startScan. QR card: LucideIcons.qrCode "Escanear QR Code" / "Aponte para o código da nota". Camera card: LucideIcons.camera "Foto do Cupom" / "Tire uma foto do cupom fiscal".
    Build Step 2 (Page 1) per UI-SPEC Section 6.1 Page 1: SingleChildScrollView(padding spacingM) with step indicator ("Etapa 2 de 3" bodySmall textSecondary), spacingL, glassmorphic _ReceiptCard, spacingL, ElevatedButton. _ReceiptCard: Row(LucideIcons.store 20px primary, spacingS, "Bistek Supermercados" titleMedium w700), spacingXS, date bodySmall textSecondary via `DateFormat('dd/MM/yyyy').format(DateTime.now())`, Divider(Colors.white.withValues(alpha:0.1)), 4 _ReceiptLineItem rows from MockData.products (Pão de Forma, Leite Integral, Banana Prata, + one more — name bodyMedium textMain, price bodyMedium textSecondary via BRL format of averagePrice), Divider, Row spaceBetween("Total" titleMedium w700 / "R$ 87,43" titleMedium w700 primary), spacingS, centered Row(LucideIcons.coins 14px primary, "+10 moedas ao confirmar" bodySmall primary). Use a module-level `final _brl = NumberFormat.currency(locale: 'pt_BR', symbol: r'R$', decimalDigits: 2);` (matches shopping_list_screen pattern). The total must render literally as "R$ 87,43" — hardcode the displayed total value 87.43 through _brl so the test finds "R\$ 87,43".
    "Confirmar e Ganhar Moedas" ElevatedButton: backgroundColor AppColors.primary, foregroundColor AppColors.background, radiusM, minimumSize Size(double.infinity, 52), child Row(LucideIcons.coins 18px, spacingS, "Confirmar e Ganhar Moedas" titleMedium w700), onPressed: _confirmReceipt.
    For Step 3, render a minimal placeholder child for page index 2 in this task (a SizedBox or simple Center) — full Step 3 confetti UI lands in Task 4. Loading overlay CircularProgressIndicator wrapped in Semantics(label: 'Processando, aguarde...') per UI-SPEC 7.1.
    Use ONLY AppColors.*/AppSizes.* — no color or numeric spacing literals except sizes explicitly given in UI-SPEC (icon px sizes 48/36/20/18/14, card height 100, button height 52). Use withValues(alpha:) never withOpacity(). No fenced code blocks in the file beyond normal Dart.
  </action>
  <verify>
    <automated>flutter analyze lib/features/price_registration/presentation/scanner_screen.dart</automated>
    <automated>powershell -Command "if (Select-String -Path lib/features/price_registration/presentation/scanner_screen.dart -Pattern 'withOpacity' -Quiet) { exit 1 } else { exit 0 }"</automated>
    <automated>powershell -Command "Select-String -Path lib/features/price_registration/presentation/scanner_screen.dart -Pattern 'addCoins\(10, AppStrings.scanReceiptDescription\)' -Quiet"</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` reports no errors for scanner_screen.dart
    - File contains NO `withOpacity(` occurrences
    - File contains `addCoins(10, AppStrings.scanReceiptDescription)`
    - File declares `class ScannerScreen extends ConsumerStatefulWidget` and uses `NeverScrollableScrollPhysics`
    - File uses `if (!mounted) return;` after the 2s delay
  </acceptance_criteria>
  <done>ScannerScreen Steps 1 and 2 are functional: method cards trigger a 2s loading overlay then show the mock Bistek receipt; confirming awards +10 coins and advances toward celebration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: ScannerScreen Step 3 confetti celebration + return flow + un-skip PREG tests</name>
  <files>lib/features/price_registration/presentation/scanner_screen.dart, test/features/scanner/scanner_screen_test.dart</files>
  <read_first>
    - lib/features/price_registration/presentation/scanner_screen.dart (Steps 1-2 from Task 3 — extend page index 2)
    - .planning/phases/05-price-registration-profile/05-UI-SPEC.md (Section 6.1 Page 2; Section 9 Animation Contract; the D-05 revision note: "Escanear Outra Nota" = jumpToPage(0); "Voltar para início" = context.go(AppRoutes.home))
    - .planning/phases/05-price-registration-profile/05-RESEARCH.md (Pattern 3 ConfettiWidget; Pitfall 1 ownership; Pitfall 6 jumpToPage before go; A3 mitigation postFrameCallback; Open Question 1)
    - lib/routing/app_routes.dart (AppRoutes.home, AppRoutes.scanner)
    - test/features/scanner/scanner_screen_test.dart (the 3 skipped stubs to un-skip)
  </read_first>
  <behavior>
    - Page 2 (Step 3) is a Stack(alignment: topCenter) with: a centered Column (animated coin icon, "+10 Smart Coins" headlineMedium primary, success text, two buttons) and a ConfettiWidget anchored top-center.
    - On entering Step 3, _confettiController.play() fires via WidgetsBinding.instance.addPostFrameCallback (A3 mitigation), not directly in initState.
    - The child _Step3Widget does NOT dispose the passed-in ConfettiController (Pitfall 1 — owner is _ScannerScreenState).
    - "Escanear Outra Nota" (OutlinedButton, primary) calls _returnHome() which does _pageController.jumpToPage(0) only (D-05 strict, A4 mitigation — stays on Scanner tab).
    - "Voltar para início" (TextButton) calls context.go(AppRoutes.home).
    - The 3 previously-skipped PREG widget tests now pass against the real screen.
  </behavior>
  <action>
    Replace the page-index-2 placeholder with a `_Step3Widget` StatefulWidget that receives `final ConfettiController controller` and `final VoidCallback onRepeat` (jumpToPage) and `final VoidCallback onHome` (go home). In its initState, register `WidgetsBinding.instance.addPostFrameCallback((_) { if (mounted) widget.controller.play(); });` (A3 mitigation). Do NOT override dispose to dispose the controller (Pitfall 1) — ownership stays with _ScannerScreenState.
    _Step3Widget.build per UI-SPEC Section 6.1 Page 2: Stack(alignment: Alignment.topCenter, children: [SafeArea→Column(mainAxisAlignment center, padding spacingM): TweenAnimationBuilder<double>(tween: Tween(begin: 0.0, end: 1.0), duration: const Duration(milliseconds: 600), curve: Curves.elasticOut, builder: (_, value, child) => Transform.scale(scale: value, child: child), child: const Icon(LucideIcons.coins, size: 80, color: AppColors.primary)), spacingL, Text("+10 Smart Coins" headlineMedium w700 primary center), spacingS, Text("Nota fiscal cadastrada com sucesso!" titleMedium w700 textMain center), spacingXS, Text("Obrigado por contribuir com dados de preços." bodyMedium textSecondary center), spacingXL, OutlinedButton "Escanear Outra Nota" (side BorderSide(AppColors.primary, 1.5), foreground primary, radiusM, minimumSize Size(double.infinity, 52), titleMedium w700, onPressed: widget.onRepeat), spacingS, TextButton "Voltar para início" (foreground textSecondary, onPressed: widget.onHome), ConfettiWidget(confettiController: widget.controller, blastDirectionality: BlastDirectionality.explosive, numberOfParticles: 20, gravity: 0.3, colors: const [AppColors.primary, Color(0xFFF97316), Color(0xFFEC4899), Color(0xFFEAB308)])]). Set excludeFromSemantics where UI-SPEC 8.5 specifies for confetti.
    In _ScannerScreenState add `void _returnHome() { _pageController.jumpToPage(0); }` (D-05/A4 — jumpToPage only; no context.go on the repeat path). Wire the page-2 child as `_Step3Widget(controller: _confettiController, onRepeat: _returnHome, onHome: () => context.go(AppRoutes.home))`. Add the AppRoutes import. Note the OutlinedButton "Escanear Outra Nota" is the repeat action (jumpToPage), the TextButton "Voltar para início" navigates to AppRoutes.home — matches the planning_context D-05 clarification.
    In test/features/scanner/scanner_screen_test.dart, remove `skip: true` from all three PREG tests. For PREG-02 and PREG-03, drive navigation: tap a method card (`await tester.tap(find.text('Escanear QR Code'))`), then `await tester.pump(const Duration(seconds: 2)); await tester.pumpAndSettle();` to clear the loading delay and land on Step 2; for PREG-03 also tap "Confirmar e Ganhar Moedas" then pumpAndSettle to reach Step 3. Assert the exact texts from <behavior> in Task 2. Keep confetti import for find.byType(ConfettiWidget).
    Only AppColors.*/AppSizes.* tokens plus the explicit px/size literals from UI-SPEC. withValues(alpha:) only. No withOpacity.
  </action>
  <verify>
    <automated>flutter analyze lib/features/price_registration/presentation/scanner_screen.dart</automated>
    <automated>flutter test test/features/scanner/scanner_screen_test.dart --no-pub</automated>
    <automated>powershell -Command "if (Select-String -Path test/features/scanner/scanner_screen_test.dart -Pattern 'skip: true' -Quiet) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` reports no errors for scanner_screen.dart
    - `flutter test test/features/scanner/scanner_screen_test.dart` passes with 0 skipped (all 3 PREG tests green)
    - scanner_screen_test.dart contains no `skip: true`
    - scanner_screen.dart contains `context.go(AppRoutes.home)` and `_pageController.jumpToPage(0)` and `addPostFrameCallback`
    - scanner_screen.dart contains NO call disposing the ConfettiController inside the Step 3 child widget
  </acceptance_criteria>
  <done>The full 3-step scan wizard works end-to-end: confetti fires on celebration, +10 Smart Coins shows, "Escanear Outra Nota" resets to Step 1 and "Voltar para início" returns home; all PREG widget tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pub.dev → app | New `confetti` package pulled into build |
| widget → SharedPreferences | Coin transaction persisted on confirm (no network) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | confetti package supply chain | accept | Audited in 05-RESEARCH.md Package Legitimacy Audit: funwith.app verified publisher, MIT license, ~4yr history, source repo github.com/funwithflutter/flutter_confetti; pinned to 0.7.0 via `^0.7.0` (`<0.8.0`) |
| T-05-02 | Denial of Service | ConfettiController disposed twice → crash | mitigate | Pitfall 1: only _ScannerScreenState disposes; _Step3Widget never disposes the passed controller |
| T-05-03 | Tampering | setState after dispose during 2s delay | mitigate | `if (!mounted) return;` after every await (Pitfall 2) |
| T-05-SC | Tampering | npm/pub installs | accept | Single pub package (confetti) approved in audit; no [ASSUMED]/[SUS]/[SLOP] packages — no blocking checkpoint required |
</threat_model>

<verification>
- `flutter pub get` resolves confetti 0.7.0
- `flutter test test/features/scanner/scanner_screen_test.dart --no-pub` — all 3 PREG widget tests green, 0 skipped
- `flutter test test/providers/coin_notifier_test.dart --no-pub` — PREG-04 description test green
- `flutter analyze lib/features/price_registration/presentation/scanner_screen.dart` — no errors
- No `withOpacity(` anywhere in scanner_screen.dart
</verification>

<success_criteria>
- ScannerScreen is a 3-page PageView wizard (ConsumerStatefulWidget) replacing the placeholder
- PREG-01: both method cards trigger a 2s loading overlay before advancing
- PREG-02: mock Bistek receipt with date, 4 items, total R$ 87,43 and confirm button renders
- PREG-04: confirming awards +10 coins with description AppStrings.scanReceiptDescription
- PREG-03: Step 3 shows ConfettiWidget + "+10 Smart Coins" + return buttons
- Full test suite remains green
</success_criteria>

<output>
Create `.planning/phases/05-price-registration-profile/05-01-SUMMARY.md` when done
</output>
