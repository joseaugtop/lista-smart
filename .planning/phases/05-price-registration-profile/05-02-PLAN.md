---
phase: 05-price-registration-profile
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/auth/domain/user.dart
  - lib/core/data/mock_data.dart
  - lib/core/providers/user_notifier.dart
  - test/providers/user_notifier_test.dart
  - lib/features/profile/presentation/profile_screen.dart
  - test/features/profile/profile_screen_test.dart
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03]
must_haves:
  truths:
    - "User model carries vehicleModel and fuelEfficiency, deserializing old SharedPreferences data without crashing (PROF-02)"
    - "UserNotifier.updateProfile persists name, email, address, vehicleModel and fuelEfficiency (PROF-02)"
    - "ProfileScreen shows pre-filled name, email, address fields (PROF-01)"
    - "ProfileScreen shows vehicle model and fuel efficiency fields editable and saved together (PROF-02)"
    - "ProfileScreen shows impact stats with scan count derived from coin transactions plus mocked searches and savings (PROF-03)"
    - "Saving the profile shows a 'Perfil atualizado!' SnackBar (PROF-01/PROF-02)"
  artifacts:
    - path: "lib/features/auth/domain/user.dart"
      provides: "User model extended with vehicleModel + fuelEfficiency (migration-safe fromJson)"
      contains: "vehicleModel"
    - path: "lib/core/providers/user_notifier.dart"
      provides: "updateProfile method"
      contains: "void updateProfile"
    - path: "lib/features/profile/presentation/profile_screen.dart"
      provides: "Full editable profile screen (ConsumerStatefulWidget)"
      contains: "class ProfileScreen extends ConsumerStatefulWidget"
    - path: "test/features/profile/profile_screen_test.dart"
      provides: "Widget tests for PROF-01, PROF-03"
      contains: "ProfileScreen"
  key_links:
    - from: "profile_screen.dart save handler"
      to: "userNotifierProvider.notifier.updateProfile"
      via: "ref.read in onPressed"
      pattern: "updateProfile\\("
    - from: "profile_screen.dart impact stats"
      to: "coinProvider transactions"
      via: "ref.watch + where filter on description"
      pattern: "AppStrings.scanReceiptDescription"
---

<objective>
Deliver the complete ProfileScreen vertical slice: extend the User domain with vehicle fields, add UserNotifier.updateProfile persistence, and build the full editable profile screen (personal data + vehicle + impact stats) replacing the placeholder.

Purpose: Implements PROF-01..03 — the user can view and edit their identity and vehicle, and see impact statistics. The vehicle fields (vehicleModel, fuelEfficiency) also feed the fuel-cost comparison from earlier phases. After this plan a user on the Profile tab can edit every field and persist it across restarts.
Output: extended User model with migration-safe fromJson, MockData.user defaults, UserNotifier.updateProfile + tests, Wave 0 profile widget-test stubs (then un-skipped), and the full ProfileScreen.
</objective>

<execution_context>
@C:/Users/Jose Augusto/Desktop/Unesc/Fase 5/Desenvolvimento Mobile/gsd/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jose Augusto/Desktop/Unesc/Fase 5/Desenvolvimento Mobile/gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-price-registration-profile/05-CONTEXT.md
@.planning/phases/05-price-registration-profile/05-RESEARCH.md
@.planning/phases/05-price-registration-profile/05-PATTERNS.md
@.planning/phases/05-price-registration-profile/05-UI-SPEC.md
@.planning/phases/05-price-registration-profile/05-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend User model + MockData defaults + UserNotifier.updateProfile + provider tests</name>
  <files>lib/features/auth/domain/user.dart, lib/core/data/mock_data.dart, lib/core/providers/user_notifier.dart, test/providers/user_notifier_test.dart</files>
  <read_first>
    - lib/features/auth/domain/user.dart (full model — extend constructor, fromJson, toJson, copyWith)
    - lib/core/providers/user_notifier.dart (spendCoins pattern: null guard + copyWith + state + _persist; add updateProfile mirroring it)
    - lib/core/data/mock_data.dart (MockData.user const at lines 8-14; MockData.vehicle at 217-221 — model 'Fiat Uno', fuelEfficiencyKmPerLiter 12.0)
    - test/providers/user_notifier_test.dart (existing group — extend with updateProfile tests inside it)
    - .planning/phases/05-price-registration-profile/05-PATTERNS.md (Pattern 5/6 exact field additions; test cases for updateProfile)
    - .planning/phases/05-price-registration-profile/05-RESEARCH.md (Pattern 5 migration; Pitfall 3 nullable num cast)
  </read_first>
  <behavior>
    - User.fromJson reads `vehicleModel: json['vehicleModel'] as String? ?? ''` and `fuelEfficiency: (json['fuelEfficiency'] as num?)?.toDouble() ?? 0.0` so JSON missing these keys (old sessions) deserializes without throwing (Pitfall 3).
    - User.toJson includes both new keys; copyWith supports both new params with `?? this.field`.
    - updateProfile(name, email, address, vehicleModel, fuelEfficiency) on a logged-in user updates state and persists all five fields; it is a no-op when state is null.
    Tests (run now, must pass GREEN after implementation):
    - 'updateProfile() updates all fields in state': login, updateProfile(...), assert name/vehicleModel/fuelEfficiency.
    - 'updateProfile() persists new fields to SharedPreferences': login, updateProfile, new container from same prefs, assert vehicleModel/fuelEfficiency hydrated.
    - 'updateProfile() is no-op when user is null': call without login, assert state still null.
    - 'fromJson tolerates legacy JSON missing vehicle fields': decode a User from a map without vehicleModel/fuelEfficiency, assert defaults '' and 0.0 (no throw).
  </behavior>
  <action>
    In lib/features/auth/domain/user.dart per PATTERNS Pattern 5: add constructor params `this.vehicleModel = ''` and `this.fuelEfficiency = 0.0`; add `final String vehicleModel;` and `final double fuelEfficiency;`; in fromJson add `vehicleModel: json['vehicleModel'] as String? ?? ''` and `fuelEfficiency: (json['fuelEfficiency'] as num?)?.toDouble() ?? 0.0` (use `as num?` not `as double?` — Pitfall 3); in toJson add `'vehicleModel': vehicleModel,` and `'fuelEfficiency': fuelEfficiency,`; in copyWith add `String? vehicleModel,` and `double? fuelEfficiency,` params with `vehicleModel: vehicleModel ?? this.vehicleModel,` and `fuelEfficiency: fuelEfficiency ?? this.fuelEfficiency,`.
    In lib/core/data/mock_data.dart update the `MockData.user` const (lines 8-14) to add `vehicleModel: 'Fiat Uno',` and `fuelEfficiency: 12.0,` (matching MockData.vehicle). Do not change MockData.vehicle or any other constant.
    In lib/core/providers/user_notifier.dart add a `void updateProfile({required String name, required String email, required String address, required String vehicleModel, required double fuelEfficiency})` method mirroring spendCoins: `final current = state; if (current == null) return;` then `final updated = current.copyWith(name: name, email: email, address: address, vehicleModel: vehicleModel, fuelEfficiency: fuelEfficiency); state = updated; _persist(updated);`. No change to _persist (toJson now carries the new fields).
    In test/providers/user_notifier_test.dart add the four tests from <behavior> inside the existing `group('UserNotifier', ...)`, using the existing SharedPreferences.setMockInitialValues({}) + ProviderContainer(overrides: [sharedPreferencesProvider.overrideWithValue(prefs)]) + addTearDown(container.dispose) setup. For the legacy-JSON test, construct User.fromJson directly from a map lacking the new keys. Do not modify existing tests.
  </action>
  <verify>
    <automated>flutter analyze lib/features/auth/domain/user.dart lib/core/providers/user_notifier.dart lib/core/data/mock_data.dart</automated>
    <automated>flutter test test/providers/user_notifier_test.dart --no-pub</automated>
    <automated>powershell -Command "Select-String -Path lib/core/providers/user_notifier.dart -Pattern 'void updateProfile' -Quiet"</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` clean for user.dart, user_notifier.dart, mock_data.dart
    - `flutter test test/providers/user_notifier_test.dart` passes including all 4 new updateProfile/migration tests
    - user.dart fromJson uses `as num?` (not `as double?`) for fuelEfficiency
    - user_notifier.dart contains `void updateProfile`
    - mock_data.dart MockData.user includes `vehicleModel: 'Fiat Uno'` and `fuelEfficiency: 12.0`
  </acceptance_criteria>
  <done>User model carries migration-safe vehicle fields, MockData.user has defaults, and UserNotifier.updateProfile persists all profile fields with passing provider tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 — profile widget-test stubs (PROF-01, PROF-03)</name>
  <files>test/features/profile/profile_screen_test.dart</files>
  <read_first>
    - test/features/smart_coins/store_screen_test.dart (analog: _makeContainer + _pumpScreen + UncontrolledProviderScope + setUpAll/setUp)
    - lib/core/providers/user_notifier.dart (login() to seed the logged-in user the screen reads in initState)
    - lib/core/providers/coin_notifier.dart (coinProvider — scan count source for PROF-03)
    - lib/core/constants/app_strings.dart (scanReceiptDescription — created in Plan 01 Task 1; the scan-count filter string)
    - .planning/phases/05-price-registration-profile/05-UI-SPEC.md (Section 6.2 — exact field labels and stat texts the tests assert)
    - .planning/phases/05-price-registration-profile/05-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <behavior>
    Stubs marked skip: true (un-skipped in Task 3 once ProfileScreen exists):
    - 'PROF-01: profile shows pre-filled name/email/address fields': pump ProfileScreen with logged-in user, expect find.text('José Augusto') (or the name in a field) and the field labels 'Nome completo', 'E-mail', 'Endereço'.
    - 'PROF-03: impact stats show derived scan count + mocked stats': seed coinProvider with N addCoins(10, AppStrings.scanReceiptDescription) calls, pump, expect the scan count value text and find.text('47') (buscas) and a savings text containing '342'.
  </behavior>
  <action>
    Create test/features/profile/profile_screen_test.dart following store_screen_test.dart shape. Define `_makeContainer()` that calls SharedPreferences.setMockInitialValues({}), getInstance, builds a ProviderContainer overriding sharedPreferencesProvider, addTearDown(dispose), and calls userNotifierProvider.notifier.login() so the screen's initState reads a non-null user. Define `_pumpScreen(tester, container)` wrapping `const ProfileScreen()` in UncontrolledProviderScope + MaterialApp + pumpAndSettle. Add the two PROF tests from <behavior>, each `skip: true`, asserting the exact UI-SPEC Section 6.2 labels/values (field labels 'Nome completo'/'E-mail'/'Endereço'/'Modelo do veículo'/'Consumo médio (km/L)', stat label fragments, '47', '342'). Import lista_smart/features/profile/presentation/profile_screen.dart, user_notifier.dart, coin_notifier.dart, and app_strings.dart.
    Test file only — no production code here.
  </action>
  <verify>
    <automated>flutter test test/features/profile/profile_screen_test.dart --no-pub</automated>
    <automated>powershell -Command "Select-String -Path test/features/profile/profile_screen_test.dart -Pattern 'ProfileScreen' -Quiet"</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/features/profile/profile_screen_test.dart` runs with the 2 PROF tests reported as skipped (not failing)
    - File imports profile_screen.dart, user_notifier.dart, coin_notifier.dart, app_strings.dart
    - File uses UncontrolledProviderScope with a logged-in user via login()
  </acceptance_criteria>
  <done>PROF-01 and PROF-03 widget-test stubs exist (skipped) defining the ProfileScreen contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build ProfileScreen (personal + vehicle + impact + save) and un-skip PROF tests</name>
  <files>lib/features/profile/presentation/profile_screen.dart, test/features/profile/profile_screen_test.dart</files>
  <read_first>
    - lib/features/profile/presentation/profile_screen.dart (placeholder to replace)
    - lib/features/auth/presentation/login_screen.dart (ConsumerStatefulWidget + TextEditingController + TextField decoration + FilledButton patterns)
    - lib/features/smart_coins/presentation/store_screen.dart (SnackBar pattern; glassmorphic card; coinProvider.transactions read)
    - lib/core/providers/user_notifier.dart (userNotifierProvider, updateProfile from Task 1)
    - lib/core/providers/coin_notifier.dart (coinProvider — scan count)
    - lib/core/data/mock_data.dart (MockData.vehicle fallback for empty vehicle fields)
    - lib/core/constants/app_strings.dart (scanReceiptDescription filter)
    - lib/core/constants/app_colors.dart, lib/core/constants/app_sizes.dart (tokens — no literals)
    - .planning/phases/05-price-registration-profile/05-UI-SPEC.md (Section 6.2 full ProfileScreen anatomy; Section 7.2 interactions; Section 8.3 SnackBar copy; Section 11 anti-patterns)
    - .planning/phases/05-price-registration-profile/05-PATTERNS.md (ProfileScreen analog: controller init from ref.read, _save handler, SnackBar)
    - .planning/phases/05-price-registration-profile/05-RESEARCH.md (Pattern 6/8; Pitfall 4 controller init in initState)
    - test/features/profile/profile_screen_test.dart (the 2 skipped stubs to un-skip)
  </read_first>
  <behavior>
    - ProfileScreen is a ConsumerStatefulWidget; in initState it reads ref.read(userNotifierProvider) synchronously and pre-fills five TextEditingControllers (name, email, address, vehicleModel with MockData.vehicle.model fallback when empty, fuelEfficiency with MockData.vehicle.fuelEfficiencyKmPerLiter fallback when 0.0).
    - Layout: CustomScrollView with a pinned SliverAppBar (avatar header: CircleAvatar with user initials, name, email), then 3 glassmorphic sections — 'Dados Pessoais' (name/email/address fields), 'Veículo' (vehicleModel/fuelEfficiency fields), 'Impacto Social' (3 stat chips), and a 'Salvar Alterações' button.
    - Impact stats: scannedCount = ref.watch(coinProvider).transactions.where((tx) => tx.description == AppStrings.scanReceiptDescription).length; buscas fixed '47'; economia fixed 'R$ 342'.
    - Tapping 'Salvar Alterações' calls ref.read(userNotifierProvider.notifier).updateProfile(...) with trimmed text and double.tryParse(fuel) ?? 0.0, then shows SnackBar 'Perfil atualizado!' with AppColors.primary background.
    - The 2 previously-skipped PROF widget tests now pass.
  </behavior>
  <action>
    Replace the placeholder ProfileScreen with a ConsumerStatefulWidget per PATTERNS ProfileScreen analog. State holds five `late final TextEditingController` (_nameCtrl, _emailCtrl, _addressCtrl, _vehicleCtrl, _efficiencyCtrl). In initState read `final user = ref.read(userNotifierProvider);` (synchronous, safe — Pitfall 4) and init controllers: name/email/address from user fields (?? ''); _vehicleCtrl text = `user?.vehicleModel.isNotEmpty == true ? user!.vehicleModel : MockData.vehicle.model`; _efficiencyCtrl text = `user?.fuelEfficiency != 0.0 ? user!.fuelEfficiency.toString() : MockData.vehicle.fuelEfficiencyKmPerLiter.toString()`. Dispose all five in dispose().
    Add `void _save()`: ref.read(userNotifierProvider.notifier).updateProfile(name: _nameCtrl.text.trim(), email: _emailCtrl.text.trim(), address: _addressCtrl.text.trim(), vehicleModel: _vehicleCtrl.text.trim(), fuelEfficiency: double.tryParse(_efficiencyCtrl.text) ?? 0.0); then `if (!mounted) return;` then ScaffoldMessenger.of(context).showSnackBar(const SnackBar(content: Text('Perfil atualizado!'), backgroundColor: AppColors.primary)) (D-07). Use double.tryParse not double.parse (Pitfall 3/V5).
    build() per UI-SPEC Section 6.2: Scaffold(backgroundColor: AppColors.background, body: CustomScrollView(slivers: [SliverAppBar(expandedHeight: 160, pinned: true, backgroundColor: AppColors.background, title: Text('Perfil', textMain), flexibleSpace: FlexibleSpaceBar(background: _AvatarHeader)), SliverToBoxAdapter(Padding(horizontal+bottom spacingM, child: Column[_ProfileSection 'Dados Pessoais', spacingM, _ProfileSection 'Veículo', spacingM, _ImpactSection 'Impacto Social', spacingL, save ElevatedButton, spacingL]))])).
    _AvatarHeader: Column(center) with CircleAvatar(radius 36, backgroundColor AppColors.primary.withValues(alpha:0.15), child Text(initials headlineMedium primary w700)) where initials = `name.split(' ').where((w)=>w.isNotEmpty).map((w)=>w[0]).take(2).join().toUpperCase()`, spacingS, name titleMedium w700 textMain, email bodyMedium textSecondary. Wrap avatar in Semantics(label: 'Avatar de \$userName') per UI-SPEC 8.5.
    _ProfileSection: glassmorphic Container (surface.withValues(alpha:0.7), Border.all(white.withValues(alpha:0.1)), radiusXL, padding spacingL) with Column[section title titleMedium w700, spacingM, fields separated by spacingM]. Fields are TextFormField styled per login_screen.dart decoration recipe (labelText, labelStyle textSecondary, prefixIcon textSecondary, OutlineInputBorder radiusM, enabledBorder white.withValues(alpha:0.1), focusedBorder AppColors.primary width 1.5, filled true, fillColor AppColors.surfaceElevated.withValues(alpha:0.4)), style textMain.
    Dados Pessoais fields (UI-SPEC table): 'Nome completo' LucideIcons.user text _nameCtrl; 'E-mail' LucideIcons.mail emailAddress _emailCtrl; 'Endereço' LucideIcons.mapPin streetAddress _addressCtrl. Veículo fields: 'Modelo do veículo' LucideIcons.car text _vehicleCtrl; 'Consumo médio (km/L)' LucideIcons.fuel numberWithOptions(decimal:true) _efficiencyCtrl.
    _ImpactSection: glassmorphic Container with Column[title 'Impacto Social' titleMedium w700, spacingM, Row of 3 Expanded _StatChip separated by spacingM]. scannedCount derived in build via ref.watch(coinProvider) filter on AppStrings.scanReceiptDescription. _StatChip: Container(surfaceElevated.withValues(alpha:0.6), radiusL, padding symmetric(vertical spacingM, horizontal spacingS), Column center[icon 24px primary, spacingXS, value titleMedium w700 textMain, spacingXS, label bodySmall textSecondary center maxLines 2]). Chips: (LucideIcons.fileText, scannedCount.toString(), 'notas\nescaneadas'); (LucideIcons.search, '47', 'buscas\nefetuadas'); (LucideIcons.trendingDown, 'R\$ 342', 'economia\nestimada'). Add Semantics(label: '\$scannedCount notas escaneadas') on the derived chip per UI-SPEC 8.5.
    Save ElevatedButton: backgroundColor AppColors.primary, foregroundColor AppColors.background, radiusM, minimumSize Size(double.infinity, 52), child Row(LucideIcons.save 18px, spacingS, 'Salvar Alterações' titleMedium w700), onPressed _save. Always enabled (D-07, no dirty tracking).
    In test/features/profile/profile_screen_test.dart remove `skip: true` from both PROF tests and adjust finders to the real widget tree (pumpAndSettle after pump). PROF-03 test seeds coinProvider via container.read(coinProvider.notifier).addCoins(10, AppStrings.scanReceiptDescription) the expected number of times before pumping, then asserts the scan-count value.
    ONLY AppColors.*/AppSizes.* tokens plus explicit UI-SPEC px/size literals (radius 36, expandedHeight 160, icon px 24/18, button height 52). withValues(alpha:) never withOpacity. No StateNotifierProvider. ref.watch only in build, ref.read only in _save.
  </action>
  <verify>
    <automated>flutter analyze lib/features/profile/presentation/profile_screen.dart</automated>
    <automated>flutter test test/features/profile/profile_screen_test.dart --no-pub</automated>
    <automated>powershell -Command "if (Select-String -Path lib/features/profile/presentation/profile_screen.dart -Pattern 'withOpacity' -Quiet) { exit 1 } else { exit 0 }"</automated>
    <automated>powershell -Command "if (Select-String -Path test/features/profile/profile_screen_test.dart -Pattern 'skip: true' -Quiet) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` clean for profile_screen.dart
    - `flutter test test/features/profile/profile_screen_test.dart` passes with 0 skipped (both PROF tests green)
    - profile_screen.dart declares `class ProfileScreen extends ConsumerStatefulWidget`
    - profile_screen.dart contains NO `withOpacity(`
    - profile_screen.dart contains `updateProfile(` and filters transactions on `AppStrings.scanReceiptDescription`
    - profile_screen.dart shows SnackBar 'Perfil atualizado!' with AppColors.primary
  </acceptance_criteria>
  <done>ProfileScreen is fully functional: pre-filled editable personal + vehicle fields, derived/mocked impact stats, and a save action that persists via updateProfile and confirms with a SnackBar; PROF widget tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TextField input → UserNotifier → SharedPreferences | Free-text profile fields persisted locally (no network) |
| Legacy SharedPreferences JSON → User.fromJson | Old sessions lack new keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | Invalid fuelEfficiency input (NaN/negative/empty) | mitigate | `double.tryParse() ?? 0.0` on save (RESEARCH V5 / Pitfall 3); trim() on text fields |
| T-05-05 | Denial of Service | Legacy JSON missing vehicle keys crashes fromJson | mitigate | Nullable casts `as String? ?? ''` and `(as num?)?.toDouble() ?? 0.0` (Pitfall 3) |
| T-05-06 | Tampering | SharedPreferences key collision | accept | Existing named constant `_key = 'lista_smart_user'` in UserNotifier — unchanged |
</threat_model>

<verification>
- `flutter test test/providers/user_notifier_test.dart --no-pub` — updateProfile + migration tests green
- `flutter test test/features/profile/profile_screen_test.dart --no-pub` — both PROF tests green, 0 skipped
- `flutter analyze` clean for user.dart, user_notifier.dart, mock_data.dart, profile_screen.dart
- No `withOpacity(` in profile_screen.dart
</verification>

<success_criteria>
- User model carries vehicleModel + fuelEfficiency with migration-safe fromJson
- UserNotifier.updateProfile persists all five profile fields
- PROF-01: ProfileScreen shows pre-filled name/email/address fields
- PROF-02: ProfileScreen edits vehicleModel + fuelEfficiency, saved together via Salvar Alterações
- PROF-03: impact stats show derived scan count + mocked 47 buscas + R$ 342 economia
- Saving shows 'Perfil atualizado!' SnackBar; full suite green
</success_criteria>

<output>
Create `.planning/phases/05-price-registration-profile/05-02-SUMMARY.md` when done
</output>
