---
phase: 03-core-shopping-loop
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - lib/features/shopping_list/presentation/shopping_list_screen.dart
  - test/features/shopping_list/shopping_list_screen_test.dart
autonomous: true
requirements:
  - SHOP-01
  - SHOP-02
  - SHOP-03
  - SHOP-04
  - SHOP-05
must_haves:
  truths:
    - "ShoppingListScreen com cart vazio exibe ícone LucideIcons.shoppingCart 64px, texto 'Sua lista está vazia' (headlineSmall) e sublabel 'Adicione produtos na tela Home' (bodyMedium textSecondary)"
    - "ShoppingListScreen com cart não-vazio renderiza uma linha por CartItem com nome (titleMedium), marca (bodySmall textSecondary), imagem via Image.network com errorBuilder LucideIcons.packageOpen, botões +/- e ícone X (per SHOP-01/SHOP-02/SHOP-03)"
    - "Botão '+' chama cartProvider.notifier.incrementQuantity(productId) e botão '-' chama decrementQuantity (que remove quando quantity chega a 1) (per SHOP-02)"
    - "Botão X (LucideIcons.x) chama cartProvider.notifier.removeItem(productId) sem dialog (per SHOP-03)"
    - "AppBar action LucideIcons.trash2 só aparece quando cart.isNotEmpty; toque abre AlertDialog 'Limpar carrinho?' com botões 'Manter Itens' e 'Limpar Carrinho' (color AppColors.error); confirmar chama cartProvider.notifier.clear (per SHOP-05/D-04)"
    - "Footer fixo exibe Switch 'Considerar deslocamento' ligado a fuelToggleProvider com sublabel 'Fiat Uno · 12 km/L · R$ 6,50/L'; toggle persiste imediatamente via fuelToggleProvider.notifier.toggle (per SHOP-04/D-07)"
    - "Footer exibe 'Total estimado' formatado em BRL via NumberFormat.currency(locale: pt_BR, symbol: R$, decimalDigits: 2) somando item.unitPrice * item.quantity"
    - "Botão 'Comparar Supermercados' (FilledButton full-width, primary) só aparece quando cart.isNotEmpty; onPressed chama context.push(AppRoutes.comparisonResult) (per D-04, navega para subrota /shopping-list/comparison declarada em Plan 02)"
    - "flutter test passa em test/features/shopping_list/shopping_list_screen_test.dart cobrindo: estado vazio, estado com 2 itens, increment/decrement via tap simulado, removeItem via tap em X, abertura do AlertDialog ao tocar trash2, navegação ao pressionar Comparar Supermercados (com goRouter mock ou observação de route stack)"
  artifacts:
    - path: "lib/features/shopping_list/presentation/shopping_list_screen.dart"
      provides: "ShoppingListScreen ConsumerWidget (ou ConsumerStatefulWidget se necessário para showDialog) implementando layout completo per 03-UI-SPEC.md seção ShoppingListScreen — empty state, lista de cart items com controles +/-/X, footer fixo com Switch+Total+Botão Comparar, AppBar com action trash2 condicional"
      contains: "class ShoppingListScreen"
      min_lines: 180
    - path: "test/features/shopping_list/shopping_list_screen_test.dart"
      provides: "Widget tests cobrindo SHOP-01 (renderiza itens), SHOP-02 (incrementa/decrementa via tap), SHOP-03 (remove via X), SHOP-04 (toggle persiste e sublabel correto), SHOP-05 (AlertDialog confirmação) — usando ProviderScope overrides de cartProvider e sharedPreferencesProvider"
      contains: "ShoppingListScreen"
      min_lines: 120
  key_links:
    - from: "lib/features/shopping_list/presentation/shopping_list_screen.dart"
      to: "cartProvider + fuelToggleProvider"
      via: "ref.watch em build(); ref.read em handlers de +/-/X, switch e trash2"
      pattern: "ref\\.watch\\(cartProvider\\)"
    - from: "lib/features/shopping_list/presentation/shopping_list_screen.dart"
      to: "AppRoutes.comparisonResult"
      via: "context.push(AppRoutes.comparisonResult) no onPressed do FilledButton Comparar Supermercados (push pois é subrota dentro do mesmo tab1)"
      pattern: "context\\.push\\(AppRoutes\\.comparisonResult"
    - from: "lib/features/shopping_list/presentation/shopping_list_screen.dart"
      to: "cartProvider.notifier (incrementQuantity, decrementQuantity, removeItem, clear)"
      via: "ref.read(cartProvider.notifier) em event handlers — nunca ref.watch fora de build"
      pattern: "cartProvider\\.notifier\\)\\.(incrementQuantity|decrementQuantity|removeItem|clear)"
---

<objective>
Implementar a ShoppingListScreen completa (D-04, SHOP-01..05): consome cart + fuelToggle providers do Plan 01 e roteia para a subrota /shopping-list/comparison declarada em Plan 02. Tela substitui o placeholder atual com (a) empty state, (b) ListView de CartItem com controles inline +/- e remoção via X, (c) footer fixo persistente contendo Switch de combustível + total estimado em BRL + FilledButton 'Comparar Supermercados', (d) AppBar action condicional trash2 que dispara AlertDialog de confirmação de limpeza. Acompanha widget test cobrindo todos os cinco requisitos SHOP-01..05.

Purpose: Esta tela é o segundo passo do loop de compras (Home → Lista → Comparação). Sem ela, o usuário monta o carrinho na Home mas não consegue revisar, ajustar quantidades, ativar o cálculo de combustível ou chegar à tela de comparação. É também o único entry point da PriceComparisonScreen per D-04 (tab3 é Scanner placeholder, não Comparar).
Output: 1 arquivo lib/ reescrito (shopping_list_screen.dart) + 1 arquivo de teste novo (shopping_list_screen_test.dart) verde.
</objective>

<execution_context>
@C:/Users/Jose Augusto/Desktop/Unesc/Fase 5/Desenvolvimento Mobile/gsd/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jose Augusto/Desktop/Unesc/Fase 5/Desenvolvimento Mobile/gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/03-core-shopping-loop/03-CONTEXT.md
@.planning/phases/03-core-shopping-loop/03-RESEARCH.md
@.planning/phases/03-core-shopping-loop/03-PATTERNS.md
@.planning/phases/03-core-shopping-loop/03-UI-SPEC.md
@.planning/phases/03-core-shopping-loop/03-VALIDATION.md
@.planning/phases/03-core-shopping-loop/03-01-SUMMARY.md
@.planning/phases/03-core-shopping-loop/03-02-SUMMARY.md

# Source-of-truth files (read before modifying)
@lib/features/shopping_list/presentation/shopping_list_screen.dart
@lib/features/shopping_list/domain/cart_item.dart
@lib/core/providers/cart_notifier.dart
@lib/core/providers/fuel_toggle_notifier.dart
@lib/routing/app_routes.dart
@lib/features/auth/presentation/login_screen.dart
@lib/core/constants/app_colors.dart
@lib/core/constants/app_sizes.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reescrever ShoppingListScreen com empty state, lista de itens, footer fixo (Switch + Total + Comparar) e AlertDialog de limpeza</name>
  <files>lib/features/shopping_list/presentation/shopping_list_screen.dart</files>
  <read_first>
    - lib/features/shopping_list/presentation/shopping_list_screen.dart (estado atual — placeholder; substituir por completo)
    - lib/features/shopping_list/domain/cart_item.dart (campos disponíveis: productId, productName, brand, imageUrl, quantity, unitPrice — usar exatamente esses nomes)
    - lib/core/providers/cart_notifier.dart (métodos disponíveis após Plan 01: addItem, incrementQuantity, decrementQuantity, removeItem, clear — NÃO chamar setState diretamente)
    - lib/core/providers/fuel_toggle_notifier.dart (provider fuelToggleProvider exposto após Plan 01; método toggle())
    - lib/routing/app_routes.dart (AppRoutes.comparisonResult = '/shopping-list/comparison' declarado em Plan 02)
    - lib/features/auth/presentation/login_screen.dart (padrão ConsumerStatefulWidget + dispose + FilledButton.styleFrom + glassmorphic Container)
    - .planning/phases/03-core-shopping-loop/03-UI-SPEC.md (seção "ShoppingListScreen" Layout Structure linhas ~507-585, Copywriting linhas ~167-186, Interaction States linhas ~303-330)
    - .planning/phases/03-core-shopping-loop/03-PATTERNS.md (seção shopping_list_screen.dart — AlertDialog pattern, FilledButton "Comparar" pattern)
    - lib/core/constants/app_colors.dart (AppColors.background, surface, surfaceElevated, primary, error, textMain, textSecondary)
    - lib/core/constants/app_sizes.dart (spacingXS=4, spacingS=8, spacingM=16, spacingL=24, radiusS=8, radiusM=12, radiusL=16)
  </read_first>
  <behavior>
    - Empty state (cart.isEmpty): Center+Column com Icon(LucideIcons.shoppingCart, size:64, color: textSecondary), 'Sua lista está vazia' em headlineSmall, 'Adicione produtos na tela Home' em bodyMedium textSecondary; AppBar SEM action trash2; footer NÃO renderizado
    - Non-empty state: ListView.separated com padding all spacingM e separator SizedBox(height: spacingS); cada linha é um Container glassmórfico (surface.withValues(alpha:0.7), border Colors.white.withValues(alpha:0.1) 1px, radiusL) contendo Row com Image.network 64x64 (errorBuilder LucideIcons.packageOpen + cor textSecondary), Column expandida com productName (titleMedium) + brand (bodySmall textSecondary), bloco de controles com botão '-' (IconButton LucideIcons.minus, touch-target 44x44, tooltip 'Diminuir quantidade'), Text(quantity, bodyMedium), botão '+' (IconButton LucideIcons.plus, touch-target 44x44, tooltip 'Aumentar quantidade'), e IconButton(LucideIcons.x, tooltip 'Remover item', color textSecondary)
    - Botão '-' chama ref.read(cartProvider.notifier).decrementQuantity(item.productId); botão '+' chama incrementQuantity; X chama removeItem (todos sem confirmação per SHOP-03/UI-SPEC interaction states)
    - AppBar: Text('Minha Lista') como title; actions condicionais: if (cart.isNotEmpty) IconButton(LucideIcons.trash2, tooltip:'Limpar carrinho', onPressed: → showDialog AlertDialog)
    - AlertDialog: backgroundColor AppColors.surface, title 'Limpar carrinho?' (textMain), content 'Todos os itens serão removidos.' (bodyMedium textSecondary), actions [TextButton 'Manter Itens' → Navigator.pop(false), FilledButton 'Limpar Carrinho' (background error, foreground textMain) → Navigator.pop(true)]; após pop true chama ref.read(cartProvider.notifier).clear()
    - Footer fixo (somente cart.isNotEmpty): Container com color AppColors.surface, border top white 0.1, padding spacingM, contendo Column de 3 blocos: (1) Row com Column[Text('Considerar deslocamento', bodyMedium), Text('Fiat Uno · 12 km/L · R\$ 6,50/L', bodySmall textSecondary)] + Spacer + Switch(activeColor: primary, value: ref.watch(fuelToggleProvider), onChanged: (_) => ref.read(fuelToggleProvider.notifier).toggle()); (2) SizedBox(spacingS) + Row spaceBetween com Text('Total estimado', bodySmall textSecondary) e Text(brlFormat.format(total), titleMedium semibold textMain); (3) SizedBox(spacingM) + FilledButton full-width estilo FilledButton.styleFrom(backgroundColor: primary, foregroundColor: background, padding vertical spacingM, radius radiusL) com label 'Comparar Supermercados' chamando context.push(AppRoutes.comparisonResult)
    - Total estimado: cart.fold<double>(0.0, (sum, i) => sum + i.unitPrice * i.quantity); formatar via final _brl = NumberFormat.currency(locale: 'pt_BR', symbol: 'R\$', decimalDigits: 2) declarado top-level no arquivo
    - SafeArea: envolver body com SafeArea para evitar overlap com gestos do sistema (especialmente o footer fixo)
    - Imports: package:flutter/material.dart, flutter_riverpod, go_router, lucide_icons, intl, core/constants/{app_colors,app_sizes}, core/providers/{cart_notifier,fuel_toggle_notifier}, routing/app_routes
    - PROIBIDO: withOpacity (usar withValues(alpha:)), StateNotifierProvider, ref.watch em handlers, valores hardcoded de cor/spacing, ícones lucide inexistentes (verificar LucideIcons.minus, plus, x, trash2, shoppingCart, packageOpen existem em ^0.257.0)
  </behavior>
  <action>
    Substituir o conteúdo de lib/features/shopping_list/presentation/shopping_list_screen.dart por uma implementação ConsumerWidget que (1) faz ref.watch(cartProvider) e ref.watch(fuelToggleProvider) em build(); (2) ramifica entre empty state e estado completo conforme cart.isEmpty; (3) renderiza AppBar com title 'Minha Lista' e action condicional trash2; (4) usa ListView.separated para os CartItem (Container glassmórfico per UI-SPEC); (5) renderiza footer fixo via Column(children:[Expanded(ListView), Container(footer)]) no body do Scaffold para garantir scroll independente; (6) declara _brl como NumberFormat.currency top-level no arquivo (não dentro de build); (7) implementa _confirmClear(BuildContext, WidgetRef) helper privado retornando Future<void> que chama showDialog<bool> e em caso de true chama ref.read(cartProvider.notifier).clear(). NÃO declarar provider novo. NÃO modificar cart_notifier.dart (já estendido em Plan 01). NÃO modificar app_router.dart (subrota já declarada em Plan 02 — apenas consumir AppRoutes.comparisonResult). NÃO inserir fences de código.
  </action>
  <verify>
    <automated>flutter analyze lib/features/shopping_list/presentation/shopping_list_screen.dart</automated>
    <automated>flutter test</automated>
  </verify>
  <acceptance_criteria>
    - flutter analyze lib/features/shopping_list/presentation/shopping_list_screen.dart reporta 0 errors e 0 warnings
    - grep -n 'class ShoppingListScreen' lib/features/shopping_list/presentation/shopping_list_screen.dart retorna exatamente 1 match
    - grep -n 'cartProvider.notifier).incrementQuantity\|cartProvider.notifier).decrementQuantity\|cartProvider.notifier).removeItem\|cartProvider.notifier).clear' lib/features/shopping_list/presentation/shopping_list_screen.dart retorna ≥ 4 matches distintos
    - grep -n 'context.push(AppRoutes.comparisonResult)' lib/features/shopping_list/presentation/shopping_list_screen.dart retorna exatamente 1 match
    - grep -nE 'withOpacity\(' lib/features/shopping_list/presentation/shopping_list_screen.dart retorna 0 matches
    - grep -n "NumberFormat.currency" lib/features/shopping_list/presentation/shopping_list_screen.dart retorna ≥ 1 match
    - grep -n "AlertDialog" lib/features/shopping_list/presentation/shopping_list_screen.dart retorna ≥ 1 match
    - grep -nE "'(Sua lista está vazia|Adicione produtos na tela Home|Considerar deslocamento|Total estimado|Comparar Supermercados|Limpar carrinho\?|Manter Itens|Limpar Carrinho|Minha Lista)'" lib/features/shopping_list/presentation/shopping_list_screen.dart retorna ≥ 9 matches (todas as strings copy obrigatórias do UI-SPEC)
    - flutter test passa (suite completa permanece verde após a edição)
  </acceptance_criteria>
  <done>ShoppingListScreen renderiza empty state correto, lista de cart items com controles funcionais, footer fixo com Switch+Total+Botão, AlertDialog de limpeza funcional, navegação para /shopping-list/comparison ao pressionar Comparar. Suite flutter test segue verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widget test cobrindo SHOP-01..05 (lista renderiza, controles disparam Notifier, AlertDialog, navegação Comparar)</name>
  <files>test/features/shopping_list/shopping_list_screen_test.dart</files>
  <read_first>
    - lib/features/shopping_list/presentation/shopping_list_screen.dart (versão recém-escrita na Task 1 — base para widgetTester find queries)
    - lib/core/providers/cart_notifier.dart (assinaturas de addItem/incrementQuantity/decrementQuantity/removeItem/clear)
    - lib/features/shopping_list/domain/cart_item.dart (constructor de CartItem usado para fixtures de teste)
    - lib/core/persistence/shared_preferences_provider.dart (sharedPreferencesProvider deve ser override-ado com SharedPreferences.setMockInitialValues({}) + await SharedPreferences.getInstance())
    - test/widget_test.dart (padrão atual de smoke test do projeto)
    - .planning/phases/03-core-shopping-loop/03-VALIDATION.md (mapping SHOP-01..05 → este arquivo de teste)
    - .planning/phases/03-core-shopping-loop/03-PATTERNS.md (padrão de ProviderScope overrides + MaterialApp.router wrapper para widgets que usam context.push)
  </read_first>
  <behavior>
    - testWidgets 'cart vazio exibe empty state e oculta footer/trash2': overrides cartProvider com lista vazia; expect.find(text('Sua lista está vazia')), find.byIcon(LucideIcons.shoppingCart); find.text('Comparar Supermercados') retorna findsNothing; find.byIcon(LucideIcons.trash2) retorna findsNothing (per SHOP-01 empty state + SHOP-05 trash2 condicional)
    - testWidgets 'cart com 2 itens renderiza cards, controles e footer': overrides cartProvider com [CartItem p01 qty 2, CartItem p02 qty 1]; expect 2 finds por productName, finds.byIcon(LucideIcons.plus) e minus aparecem (per SHOP-01); find.text('Comparar Supermercados') findsOneWidget (per SHOP-04 footer); find.byIcon(LucideIcons.trash2) findsOneWidget no AppBar (per SHOP-05)
    - testWidgets 'tap em + dispara incrementQuantity via Notifier real persistido': override sharedPreferencesProvider com mock; pré-adicionar item via cartProvider.notifier; tap em find.byIcon(LucideIcons.plus).first; await tester.pump(); verificar cartProvider.state[0].quantity incrementou (per SHOP-02)
    - testWidgets 'tap em - com qty=1 remove item via decrementQuantity': override com 1 item qty=1; tap em LucideIcons.minus; expect cartProvider.state.isEmpty (per SHOP-02)
    - testWidgets 'tap em X remove item': override com 1 item; tap em LucideIcons.x; expect cart vazio após pump (per SHOP-03)
    - testWidgets 'tap em trash2 abre AlertDialog e Limpar Carrinho confirma': override com 2 itens; tap em LucideIcons.trash2; pumpAndSettle; expect find.text('Limpar carrinho?') findsOneWidget; tap em find.widgetWithText(FilledButton, 'Limpar Carrinho'); pumpAndSettle; expect cartProvider.state.isEmpty (per SHOP-05)
    - testWidgets 'Switch toggle persiste em fuelToggleProvider': override sharedPreferencesProvider; expect Switch.value inicial true; tap em Switch; expect fuelToggleProvider.state == false após pump (per SHOP-04/D-07)
    - testWidgets 'tap em Comparar Supermercados empurra rota /shopping-list/comparison': envolver com MaterialApp.router usando GoRouter mock que captura location; tap em find.widgetWithText(FilledButton, 'Comparar Supermercados'); pumpAndSettle; verificar route location == AppRoutes.comparisonResult (per SHOP-04/D-04)
  </behavior>
  <action>
    Criar test/features/shopping_list/shopping_list_screen_test.dart com setUpAll(WidgetsFlutterBinding.ensureInitialized + SharedPreferences.setMockInitialValues({})) e setUp/group cobrindo as 7 cenários acima. Cada testWidgets monta ProviderScope com overrides explícitos de sharedPreferencesProvider (resolvendo a instância via await SharedPreferences.getInstance()) e — quando necessário — override de cartProvider via cartProvider.overrideWith((ref) => CartNotifier()..state = [...fixtures]); usar MaterialApp(home: ShoppingListScreen()) para a maioria dos testes; para o teste de navegação, montar MaterialApp.router com um GoRouter simples contendo as rotas /shopping-list e /shopping-list/comparison apontando para Containers placeholder. Importar package:flutter_test/flutter_test.dart, package:flutter_riverpod/flutter_riverpod.dart, package:lucide_icons/lucide_icons.dart, package:go_router/go_router.dart, package:shared_preferences/shared_preferences.dart, todos os providers consumidos, e a tela. NÃO usar reflect/mockito — usar Notifier reais com SharedPreferences mock. NÃO inserir fences de código.
  </action>
  <verify>
    <automated>flutter test test/features/shopping_list/shopping_list_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - flutter test test/features/shopping_list/shopping_list_screen_test.dart exits 0 com ≥ 7 tests passing
    - grep -n "testWidgets" test/features/shopping_list/shopping_list_screen_test.dart retorna ≥ 7 matches
    - grep -nE "(SHOP-01|SHOP-02|SHOP-03|SHOP-04|SHOP-05|empty state|incrementQuantity|decrementQuantity|removeItem|AlertDialog|Comparar Supermercados)" test/features/shopping_list/shopping_list_screen_test.dart retorna ≥ 7 matches distintos (cobertura visível dos 5 requisitos)
    - grep -n "SharedPreferences.setMockInitialValues" test/features/shopping_list/shopping_list_screen_test.dart retorna ≥ 1 match
    - grep -n "ProviderScope" test/features/shopping_list/shopping_list_screen_test.dart retorna ≥ 1 match
    - flutter test (full suite) exits 0 após a adição
  </acceptance_criteria>
  <done>Arquivo de teste novo cobre os 5 requisitos SHOP-01..05 + comportamento do Switch (SHOP-04) e navegação Comparar; suite completa verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → cart state | Tap em +/-/X/trash2 muda quantidade ou remove itens do carrinho persistido em SharedPreferences |
| User input → fuelToggle state | Tap no Switch alterna bool persistido em SharedPreferences (key: lista_smart_fuel_toggle) |
| Screen → router | context.push(AppRoutes.comparisonResult) navega para subrota dentro do mesmo tab1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | SharedPreferences (cart payload) | accept | App local single-user acadêmico; SharedPreferences é storage não-encriptado padrão Flutter; nenhum dado sensível |
| T-03-03-02 | Denial of Service | clear() acidental sem confirmação | mitigate | AlertDialog de confirmação obrigatória per SHOP-05 antes de cartProvider.notifier.clear() |
| T-03-03-03 | Tampering | Image.network com URL arbitrária do mock | accept | URLs vêm de MockData hardcoded (CONTEXT.md D-11); errorBuilder garante degradação graciosa para qualquer falha de rede |
| T-03-03-SC | Tampering | npm/pub installs | mitigate | Nenhum pacote novo adicionado nesta plano — RESEARCH.md Package Legitimacy Audit confirma zero adições |
</threat_model>

<verification>
- Suite `flutter test` verde após cada task
- `flutter analyze lib/features/shopping_list/presentation/shopping_list_screen.dart` reporta 0 errors/warnings
- ShoppingListScreen renderiza sem overflow em tamanhos de tela padrão (verificação manual diferida ao end-of-phase human-check)
- AlertDialog "Limpar carrinho?" aparece em pt-BR conforme UI-SPEC; botão "Limpar Carrinho" em cor `AppColors.error`
- Nenhuma string copy hardcoded fora do UI-SPEC; nenhuma cor/spacing hardcoded
- Switch persiste estado entre rebuilds de teste (validado via Notifier real + SharedPreferences mock)
</verification>

<success_criteria>
- ShoppingListScreen substitui o placeholder anterior e renderiza estado vazio + cart cheio conforme UI-SPEC
- Botões +/-/X disparam métodos corretos do CartNotifier sem confirmação intermediária (per SHOP-02/SHOP-03)
- AppBar action trash2 + AlertDialog satisfazem SHOP-05 com botão destrutivo em cor error
- Footer fixo: Switch fuelToggle + sublabel correto + Total estimado em BRL + botão Comparar Supermercados visíveis apenas com cart.isNotEmpty
- Botão Comparar Supermercados empurra `/shopping-list/comparison` (subrota declarada em Plan 02)
- Widget test cobre os 5 requisitos SHOP-01..05 e suite completa permanece verde
- Todos os tokens de cor e espaçamento via `AppColors.*` e `AppSizes.*`; zero `withOpacity()`; zero `StateNotifierProvider`
- Decisão D-04 cumprida: este é o único entry point da PriceComparisonScreen; tab3 permanece Scanner (mudança feita em Plan 02)
- Decisão D-07 cumprida: fuelToggle persiste imediatamente em SharedPreferences via fuelToggleProvider.notifier.toggle()
</success_criteria>

<output>
Create `.planning/phases/03-core-shopping-loop/03-03-SUMMARY.md` when done
</output>
