---
phase: 03-core-shopping-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/profile/domain/product.dart
  - lib/features/profile/domain/nutritional_info.dart
  - lib/core/data/mock_data.dart
  - lib/core/providers/products_provider.dart
  - lib/core/providers/prices_provider.dart
  - lib/core/providers/vehicle_provider.dart
  - lib/core/providers/search_query_notifier.dart
  - lib/core/providers/view_mode_notifier.dart
  - lib/core/providers/fuel_toggle_notifier.dart
  - lib/core/providers/filtered_products_provider.dart
  - lib/core/providers/comparison_results_provider.dart
  - lib/core/providers/cart_notifier.dart
  - test/providers/filtered_products_provider_test.dart
  - test/providers/cart_notifier_quantity_test.dart
  - test/providers/comparison_results_provider_test.dart
  - test/providers/fuel_toggle_notifier_test.dart
  - test/providers/view_mode_notifier_test.dart
  - test/providers/search_query_notifier_test.dart
autonomous: true
requirements:
  - HOME-02
  - HOME-04
  - SHOP-02
  - SHOP-04
  - COMP-01
  - COMP-02
  - COMP-03
must_haves:
  truths:
    - "MockData expõe prices (Map productId→supermarket→double), fuelPrice (6.50) e vehicle (Fiat Uno, 12 km/L) via fuelEfficiencyKmPerLiter"
    - "Product carrega ean, subcategory, department e nutritionalInfo opcionais sem quebrar os 12 constructors existentes em MockData.products"
    - "filteredProductsProvider retorna lista completa quando query vazia e filtra por name/brand/category case-insensitive quando query preenchida"
    - "fuelToggleProvider lê estado inicial de SharedPreferences key lista_smart_fuel_toggle com default true e persiste toggle"
    - "comparisonResultsProvider produz List<SupermarketTotal> ordenada ascendente por totalCost para os 4 supermercados de MockData.supermarketDistances"
    - "CartNotifier.incrementQuantity e decrementQuantity ajustam quantidade imutavelmente; decrementQuantity remove item quando quantity chega a 1"
    - "flutter test passa em todos os arquivos novos de test/providers/"
  artifacts:
    - path: "lib/features/profile/domain/nutritional_info.dart"
      provides: "NutritionalInfo immutable com 6 nutrientes + servingSize, toJson/fromJson"
      contains: "class NutritionalInfo"
    - path: "lib/features/profile/domain/product.dart"
      provides: "Product extension com ean/subcategory/department/nutritionalInfo (defaults para retrocompat)"
      contains: "NutritionalInfo? nutritionalInfo"
    - path: "lib/core/data/mock_data.dart"
      provides: "MockData.prices, MockData.fuelPrice, MockData.vehicle (Fiat Uno, 12 km/L)"
      contains: "static const Map<String, Map<String, double>> prices"
    - path: "lib/core/providers/products_provider.dart"
      provides: "Provider<List<Product>> productsProvider"
      exports: ["productsProvider"]
    - path: "lib/core/providers/prices_provider.dart"
      provides: "Provider<Map<String, Map<String, double>>> pricesProvider"
      exports: ["pricesProvider"]
    - path: "lib/core/providers/vehicle_provider.dart"
      provides: "Provider<Vehicle> vehicleProvider"
      exports: ["vehicleProvider"]
    - path: "lib/core/providers/search_query_notifier.dart"
      provides: "NotifierProvider<SearchQueryNotifier, String> searchQueryProvider"
      exports: ["searchQueryProvider", "SearchQueryNotifier"]
    - path: "lib/core/providers/view_mode_notifier.dart"
      provides: "NotifierProvider<ViewModeNotifier, ViewMode> viewModeProvider + enum ViewMode { grid, list }"
      exports: ["viewModeProvider", "ViewModeNotifier", "ViewMode"]
    - path: "lib/core/providers/fuel_toggle_notifier.dart"
      provides: "NotifierProvider<FuelToggleNotifier, bool> fuelToggleProvider com SharedPrefs"
      exports: ["fuelToggleProvider", "FuelToggleNotifier"]
    - path: "lib/core/providers/filtered_products_provider.dart"
      provides: "Provider<List<Product>> filteredProductsProvider derivado"
      exports: ["filteredProductsProvider"]
    - path: "lib/core/providers/comparison_results_provider.dart"
      provides: "Provider<List<SupermarketTotal>> + class SupermarketTotal"
      exports: ["comparisonResultsProvider", "SupermarketTotal"]
    - path: "lib/core/providers/cart_notifier.dart"
      provides: "CartNotifier.incrementQuantity e decrementQuantity (novos métodos)"
      contains: "void incrementQuantity"
    - path: "test/providers/filtered_products_provider_test.dart"
      provides: "Testes HOME-02 — query vazia retorna todos; query filtra por name/brand"
      contains: "filteredProductsProvider"
    - path: "test/providers/cart_notifier_quantity_test.dart"
      provides: "Testes SHOP-02 — increment/decrement/decrement-remove"
      contains: "incrementQuantity"
    - path: "test/providers/comparison_results_provider_test.dart"
      provides: "Testes COMP-01/02/03 — 4 supermercados, ordem ascendente, fuel on/off"
      contains: "comparisonResultsProvider"
    - path: "test/providers/fuel_toggle_notifier_test.dart"
      provides: "Testes SHOP-04/D-07 — persistência SharedPrefs"
      contains: "lista_smart_fuel_toggle"
  key_links:
    - from: "lib/core/providers/filtered_products_provider.dart"
      to: "productsProvider + searchQueryProvider"
      via: "ref.watch em build do Provider derivado"
      pattern: "ref\\.watch\\(productsProvider\\)"
    - from: "lib/core/providers/comparison_results_provider.dart"
      to: "cartProvider + pricesProvider + vehicleProvider + fuelToggleProvider + MockData.fuelPrice + MockData.supermarketDistances"
      via: "ref.watch em build, lookup MockData direto"
      pattern: "vehicle\\.fuelEfficiencyKmPerLiter"
    - from: "lib/core/providers/fuel_toggle_notifier.dart"
      to: "sharedPreferencesProvider"
      via: "ref.watch em build + ref.read em toggle()"
      pattern: "lista_smart_fuel_toggle"
---

<objective>
Construir a fundação de dados e estado da Phase 3 (D-01 a D-12): estender o model Product com 4 novos campos (D-10), criar NutritionalInfo, popular MockData com prices/fuelPrice/vehicle (D-01, D-05, D-06), criar 8 providers Riverpod novos (products/prices/vehicle read-only; search/viewMode/fuelToggle Notifiers; filtered + comparison derivados), estender CartNotifier com incrementQuantity/decrementQuantity (SHOP-02), e cobrir tudo com testes unitários que satisfazem HOME-02/HOME-04/SHOP-02/SHOP-04/COMP-01..03.

Purpose: Esta camada habilita todas as 4 telas da fase (Plan 02 e Plan 03). Sem prices/vehicle/fuelToggle, a comparação não calcula; sem filtered/viewMode/search, a Home não filtra; sem increment/decrement, a ShoppingList não ajusta quantidades.
Output: 11 arquivos lib/ novos ou estendidos + 4 arquivos de teste em test/providers/ verdes.
</objective>

<execution_context>
@C:/Users/Jose Augusto/Desktop/Unesc/Fase 5/Desenvolvimento Mobile/gsd/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jose Augusto/Desktop/Unesc/Fase 5/Desenvolvimento Mobile/gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-core-shopping-loop/03-CONTEXT.md
@.planning/phases/03-core-shopping-loop/03-RESEARCH.md
@.planning/phases/03-core-shopping-loop/03-PATTERNS.md
@.planning/phases/03-core-shopping-loop/03-VALIDATION.md

# Source-of-truth files (read before modifying analogs)
@lib/features/profile/domain/vehicle.dart
@lib/features/profile/domain/product.dart
@lib/features/auth/domain/user.dart
@lib/core/data/mock_data.dart
@lib/core/providers/cart_notifier.dart
@lib/core/providers/favorites_notifier.dart
@lib/core/persistence/shared_preferences_provider.dart
@lib/features/shopping_list/domain/cart_item.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend domain models (NutritionalInfo + Product) and MockData with prices/fuelPrice/vehicle</name>
  <files>lib/features/profile/domain/nutritional_info.dart, lib/features/profile/domain/product.dart, lib/core/data/mock_data.dart</files>
  <read_first>
    - lib/features/profile/domain/vehicle.dart (exato analog para NutritionalInfo: @immutable + fromJson com (json[x] as num).toDouble() + toJson + copyWith)
    - lib/features/profile/domain/product.dart (estado atual do model — preserve campos existentes ordem/required)
    - lib/features/auth/domain/user.dart (padrão de campo opcional com default no constructor: this.field = '' / this.field = 0)
    - lib/core/data/mock_data.dart (12 constructors const Product nas linhas 14-127 — NÃO podem quebrar)
    - .planning/phases/03-core-shopping-loop/03-CONTEXT.md (D-01 estrutura de prices, D-05 vehicle Fiat Uno, D-06 fuelPrice 6.50, D-10 campos do Product)
    - .planning/phases/03-core-shopping-loop/03-PATTERNS.md (seção "lib/features/profile/domain/product.dart" e "lib/core/data/mock_data.dart")
  </read_first>
  <behavior>
    - NutritionalInfo: instanciar com calories/protein/carbs/fat/fiber/sodium (double) + servingSize (String); toJson/fromJson roundtrip preserva valores
    - Product novo: continua aceitando os 12 const constructors existentes em MockData.products sem alteração (defaults aplicam)
    - Product novo: aceita também ean, subcategory, department, nutritionalInfo nomeados
    - Product.fromJson tolera ausência de ean/subcategory/department (default '') e nutritionalInfo (default null)
    - Product.toJson sempre inclui as 7 chaves originais; chaves novas incluídas apenas quando não-default
    - MockData.prices contém entrada para CADA productId p01..p12 com chaves Bistek/Giassi/Angeloni/Atacadão (valores realistas em BRL, todos doubles distintos para evitar empates em testes de ordenação)
    - MockData.fuelPrice == 6.50 (static const double)
    - MockData.vehicle == const Vehicle(id: 'vehicle_default', model: 'Fiat Uno', fuelEfficiencyKmPerLiter: 12.0) — usar o nome de campo REAL fuelEfficiencyKmPerLiter (D-05 escreveu fuelEfficiency mas o model existente usa fuelEfficiencyKmPerLiter — ver Pitfall 3)
  </behavior>
  <action>
    Crie lib/features/profile/domain/nutritional_info.dart (novo) seguindo o padrão de vehicle.dart per D-10: import flutter/foundation @immutable, classe NutritionalInfo com const constructor de 7 campos required (calories, protein, carbs, fat, fiber, sodium como final double; servingSize como final String), factory fromJson aplicando (json[k] as num).toDouble() para os 6 doubles e cast direto String para servingSize, toJson retornando todas as 7 chaves.

    Estenda lib/features/profile/domain/product.dart per D-10 adicionando 4 campos finais OPCIONAIS no constructor (this.ean = '', this.subcategory = '', this.department = '', this.nutritionalInfo) e 4 declarações final (String ean; String subcategory; String department; NutritionalInfo? nutritionalInfo;). Atualize Product.fromJson para ler ean/subcategory/department com fallback string vazia (ex: json['ean'] as String? ?? '') e nutritionalInfo via NutritionalInfo.fromJson(json['nutritionalInfo'] as Map<String, dynamic>) quando não-null. Atualize Product.toJson para incluir as 4 chaves novas (nutritionalInfo serializa via toJson() ou null). Atualize copyWith para receber os 4 campos novos. NÃO altere a assinatura nem ordem dos campos required existentes — os 12 constructors em MockData.products devem continuar compilando sem mudança.

    Estenda lib/core/data/mock_data.dart per D-01/D-05/D-06: adicione import '../../features/profile/domain/vehicle.dart'; adicione static const double fuelPrice = 6.50; adicione static const Vehicle vehicle = Vehicle(id: 'vehicle_default', model: 'Fiat Uno', fuelEfficiencyKmPerLiter: 12.0); adicione static const Map<String, Map<String, double>> prices com uma entrada por productId p01..p12, cada uma mapeando Bistek/Giassi/Angeloni/Atacadão para preços realistas em BRL (escolha valores distintos por supermercado dentro de cada produto e mantenha variação entre produtos para que ordenação por totalCost no comparisonResultsProvider seja determinística).

    NÃO modifique os 12 constructors existentes de MockData.products. NÃO renomeie o campo fuelEfficiencyKmPerLiter no model Vehicle (out of scope; quebraria persistência da Phase 2).
  </action>
  <verify>
    <automated>flutter analyze lib/features/profile/domain/nutritional_info.dart lib/features/profile/domain/product.dart lib/core/data/mock_data.dart</automated>
    <automated>flutter test test/models/</automated>
  </verify>
  <acceptance_criteria>
    - flutter analyze sobre os 3 arquivos reporta 0 errors e 0 warnings
    - test/models/ existente (Product/Vehicle tests da Phase 1) continua passando — provando que defaults preservaram retrocompat
    - grep "static const double fuelPrice = 6.50" em lib/core/data/mock_data.dart retorna 1 match
    - grep "model: 'Fiat Uno'" em lib/core/data/mock_data.dart retorna 1 match
    - grep "fuelEfficiencyKmPerLiter: 12.0" em lib/core/data/mock_data.dart retorna 1 match
    - grep "'p12':" em lib/core/data/mock_data.dart retorna 1 match (todos os 12 productIds presentes em prices)
    - grep "class NutritionalInfo" em lib/features/profile/domain/nutritional_info.dart retorna 1 match
    - grep "NutritionalInfo? nutritionalInfo" em lib/features/profile/domain/product.dart retorna ao menos 1 match (campo final)
  </acceptance_criteria>
  <done>NutritionalInfo criado; Product estendido com 4 campos opcionais; MockData popula prices (12 productIds × 4 supermercados), fuelPrice e vehicle; os 12 const Product constructors continuam válidos; flutter analyze sem erros.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create read-only and state providers (products/prices/vehicle/search/viewMode/fuelToggle) and extend CartNotifier</name>
  <files>lib/core/providers/products_provider.dart, lib/core/providers/prices_provider.dart, lib/core/providers/vehicle_provider.dart, lib/core/providers/search_query_notifier.dart, lib/core/providers/view_mode_notifier.dart, lib/core/providers/fuel_toggle_notifier.dart, lib/core/providers/cart_notifier.dart, test/providers/fuel_toggle_notifier_test.dart, test/providers/view_mode_notifier_test.dart, test/providers/search_query_notifier_test.dart, test/providers/cart_notifier_quantity_test.dart</files>
  <read_first>
    - lib/core/providers/cart_notifier.dart (padrão Notifier + _persist + spread-update immutable + addItem mostra como detectar índice e copyWith)
    - lib/core/providers/favorites_notifier.dart (padrão Notifier + SharedPrefs build/read/write — analog exato para FuelToggleNotifier)
    - lib/core/persistence/shared_preferences_provider.dart (assinatura do sharedPreferencesProvider — Provider<SharedPreferences>)
    - lib/features/profile/domain/vehicle.dart (campo correto: fuelEfficiencyKmPerLiter)
    - .planning/phases/03-core-shopping-loop/03-RESEARCH.md (Pattern 1-4, Pattern 10 com código de increment/decrement)
    - .planning/phases/03-core-shopping-loop/03-PATTERNS.md (seções de cada provider novo)
    - .planning/phases/03-core-shopping-loop/03-CONTEXT.md (D-07 fuel toggle SharedPrefs key + default true, D-13 enum ViewMode)
  </read_first>
  <behavior>
    - productsProvider retorna MockData.products (referência identidade)
    - pricesProvider retorna MockData.prices (referência identidade)
    - vehicleProvider retorna MockData.vehicle (referência identidade)
    - searchQueryProvider.build() == ''; .update(s) altera state; .clear() volta a ''
    - viewModeProvider.build() == ViewMode.grid; .setList() e .setGrid() alteram state; enum ViewMode tem apenas { grid, list }
    - fuelToggleProvider.build() lê prefs.getBool('lista_smart_fuel_toggle') ?? true; toggle() inverte state E persiste setBool
    - CartNotifier.incrementQuantity(id) com item existente: quantity += 1; sem item: no-op silencioso
    - CartNotifier.decrementQuantity(id) com quantity > 1: quantity -= 1; com quantity == 1: remove item da lista; sem item: no-op
    - Ambos increment/decrement chamam _persist após atualização
  </behavior>
  <action>
    Crie os 3 read-only providers (products_provider.dart, prices_provider.dart, vehicle_provider.dart) seguindo o template da seção Pattern 1 do RESEARCH.md: import package:flutter_riverpod/flutter_riverpod.dart, imports relativos para MockData e tipos do domain (Product/Vehicle), declaração top-level final XxxProvider = Provider<T>((ref) => MockData.x);.

    Crie lib/core/providers/search_query_notifier.dart com class SearchQueryNotifier extends Notifier<String> seguindo Pattern 3: build() retorna ''; update(String) seta state; clear() seta ''. Declare final searchQueryProvider = NotifierProvider<SearchQueryNotifier, String>(SearchQueryNotifier.new);.

    Crie lib/core/providers/view_mode_notifier.dart com enum ViewMode { grid, list } no topo do arquivo; class ViewModeNotifier extends Notifier<ViewMode> com build() retornando ViewMode.grid e métodos setGrid()/setList(). Declare final viewModeProvider = NotifierProvider<ViewModeNotifier, ViewMode>(ViewModeNotifier.new);.

    Crie lib/core/providers/fuel_toggle_notifier.dart espelhando favorites_notifier.dart per D-07: static const _key = 'lista_smart_fuel_toggle'; build() faz ref.watch(sharedPreferencesProvider).getBool(_key) ?? true; toggle() seta state = !state e chama ref.read(sharedPreferencesProvider).setBool(_key, state). Declare final fuelToggleProvider = NotifierProvider<FuelToggleNotifier, bool>(FuelToggleNotifier.new);.

    Estenda lib/core/providers/cart_notifier.dart adicionando dois métodos public (incrementQuantity, decrementQuantity) per Pattern 10 do RESEARCH.md: usar indexWhere(productId), guard (idx < 0) return; immutable spread update via sublist/copyWith(quantity: ...); decrementQuantity com guard state[idx].quantity <= 1 que chama removeItem(productId) e retorna; ambos chamam _persist() ao final. NÃO altere métodos existentes addItem/removeItem/clear/build.

    Crie 4 arquivos de teste em test/providers/ usando flutter_test e ProviderContainer com override do sharedPreferencesProvider:
    - test/providers/search_query_notifier_test.dart: cobre build()=='', update muda state, clear zera state
    - test/providers/view_mode_notifier_test.dart: cobre build()==ViewMode.grid, setList muda, setGrid reverte
    - test/providers/fuel_toggle_notifier_test.dart: cobre default true sem prefs, lê false quando prefs.setBool feito antes, toggle alterna E persiste em prefs (assertar prefs.getBool após toggle) — usar SharedPreferences.setMockInitialValues({}) e SharedPreferences.getInstance() injetado via override
    - test/providers/cart_notifier_quantity_test.dart: cobre incrementQuantity em item existente (quantity vai de 1→2), decrementQuantity quantity 2→1, decrementQuantity quantity 1→remoção do item, incrementQuantity em productId ausente é no-op
  </action>
  <verify>
    <automated>flutter test test/providers/search_query_notifier_test.dart test/providers/view_mode_notifier_test.dart test/providers/fuel_toggle_notifier_test.dart test/providers/cart_notifier_quantity_test.dart</automated>
    <automated>flutter analyze lib/core/providers/</automated>
  </verify>
  <acceptance_criteria>
    - flutter test sobre os 4 arquivos novos sai com exit code 0
    - flutter analyze sobre lib/core/providers/ reporta 0 errors
    - grep "extends Notifier<bool>" em lib/core/providers/fuel_toggle_notifier.dart retorna 1 match
    - grep "lista_smart_fuel_toggle" em lib/core/providers/fuel_toggle_notifier.dart retorna 1 match
    - grep "enum ViewMode" em lib/core/providers/view_mode_notifier.dart retorna 1 match
    - grep "void incrementQuantity" em lib/core/providers/cart_notifier.dart retorna 1 match
    - grep "void decrementQuantity" em lib/core/providers/cart_notifier.dart retorna 1 match
    - grep -E "ref\\.watch" em lib/core/providers/products_provider.dart retorna 0 matches (provider read-only puro, sem watch)
  </acceptance_criteria>
  <done>3 providers read-only criados; 3 NotifierProviders (search/viewMode/fuelToggle) criados; CartNotifier estendido com incrementQuantity/decrementQuantity; 4 arquivos de teste verdes; flutter analyze limpo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create derived providers (filtered + comparison) with SupermarketTotal and dedicated tests</name>
  <files>lib/core/providers/filtered_products_provider.dart, lib/core/providers/comparison_results_provider.dart, test/providers/filtered_products_provider_test.dart, test/providers/comparison_results_provider_test.dart</files>
  <read_first>
    - lib/core/providers/cart_notifier.dart (assinatura do cartProvider — exportado como NotifierProvider<CartNotifier, List<CartItem>>)
    - lib/features/profile/domain/vehicle.dart (campo fuelEfficiencyKmPerLiter — uso CRÍTICO no fuelCost)
    - lib/features/profile/domain/product.dart (campos para filtro: name, brand, category)
    - lib/core/data/mock_data.dart (após Task 1 — prices, fuelPrice, supermarketDistances)
    - .planning/phases/03-core-shopping-loop/03-CONTEXT.md (D-03 fórmula: fuelCost = distance*2/fuelEfficiency*fuelPrice; D-12 filtro reativo)
    - .planning/phases/03-core-shopping-loop/03-RESEARCH.md (Pattern 2 filtered, Pattern 5 comparisonResults com código completo)
    - .planning/phases/03-core-shopping-loop/03-PATTERNS.md (seção filtered_products_provider e comparison_results_provider — código completo)
  </read_first>
  <behavior>
    - filteredProductsProvider com searchQuery == '' retorna lista IGUAL a productsProvider (12 itens)
    - filteredProductsProvider com query 'leite' (case-insensitive, trim) retorna apenas Product cujos name/brand/category contenham a substring
    - filteredProductsProvider com query sem matches retorna lista vazia
    - SupermarketTotal é @immutable com supermarket/productsCost/fuelCost/distanceKm/totalCost (5 campos final)
    - comparisonResultsProvider retorna exatamente 4 SupermarketTotal (Bistek, Giassi, Angeloni, Atacadão)
    - Resultados ordenados ascendente por totalCost (índice 0 == vencedor)
    - Com cart vazio: productsCost == 0.0 para todos; fuelCost depende apenas do toggle
    - Com fuelToggle == true: fuelCost == (distanceKm * 2 / vehicle.fuelEfficiencyKmPerLiter) * MockData.fuelPrice
    - Com fuelToggle == false: fuelCost == 0.0 para todos
    - productsCost == soma sobre cart de (prices[item.productId][supermarket] ?? item.unitPrice) * item.quantity
  </behavior>
  <action>
    Crie lib/core/providers/filtered_products_provider.dart seguindo Pattern 2 do RESEARCH.md: import flutter_riverpod, productsProvider, searchQueryProvider, Product. Declare final filteredProductsProvider = Provider<List<Product>>((ref) {...}). Dentro: final products = ref.watch(productsProvider); final query = ref.watch(searchQueryProvider).toLowerCase().trim(); se query.isEmpty retorne products; senão retorne products.where((p) => p.name.toLowerCase().contains(query) || p.brand.toLowerCase().contains(query) || p.category.toLowerCase().contains(query)).toList().

    Crie lib/core/providers/comparison_results_provider.dart seguindo Pattern 5 do RESEARCH.md. Imports: flutter/foundation @immutable; flutter_riverpod; ../data/mock_data.dart; cart_notifier.dart; fuel_toggle_notifier.dart; prices_provider.dart; vehicle_provider.dart. Declare class SupermarketTotal @immutable com const constructor required de 5 campos (supermarket String, productsCost double, fuelCost double, distanceKm double, totalCost double) e os 5 finals correspondentes. Declare final comparisonResultsProvider = Provider<List<SupermarketTotal>>((ref) {...}). Dentro: ref.watch(cartProvider) para cart; ref.watch(pricesProvider) para prices; ref.watch(vehicleProvider) para vehicle; ref.watch(fuelToggleProvider) para fuelToggle; const fuelPrice = MockData.fuelPrice; final distances = MockData.supermarketDistances. Mapeie distances.entries para SupermarketTotal calculando productsCost via cart.fold com price = prices[item.productId]?[supermarket] ?? item.unitPrice, fuelCost via expressão ternária com vehicle.fuelEfficiencyKmPerLiter (NÃO fuelEfficiency — ver Pitfall 3), totalCost = productsCost + fuelCost. Retorne lista ordenada com ..sort((a, b) => a.totalCost.compareTo(b.totalCost)).

    Crie test/providers/filtered_products_provider_test.dart cobrindo: (1) query vazia retorna List<Product> com length == MockData.products.length, (2) query 'leite' retorna ao menos 1 item cujo name.toLowerCase contém 'leite', (3) query 'XYZ_inexistente' retorna lista vazia, (4) query 'LEITE' (uppercase) retorna mesmo resultado de 'leite' (case-insensitive). Setup: ProviderContainer com override do sharedPreferencesProvider via SharedPreferences.setMockInitialValues({}) + SharedPreferences.getInstance().

    Crie test/providers/comparison_results_provider_test.dart cobrindo COMP-01/02/03: (1) cart vazio + fuelToggle true: length == 4, todos productsCost == 0.0, fuelCost > 0 e ordenado por distância (Bistek 2.3 mais barato), (2) cart vazio + fuelToggle false: todos fuelCost == 0.0 e totalCost == 0.0, (3) cart com 1 item de p01 quantity 2: productsCost de cada supermercado == MockData.prices['p01'][supermarket] * 2, (4) lista sempre ordenada ascendente por totalCost (assertar results[0].totalCost <= results[1].totalCost <= ... <= results[3].totalCost), (5) verificar valor exato de fuelCost para Bistek com toggle true: (2.3 * 2 / 12.0) * 6.50 == ~2.49 (use closeTo com tolerância 0.01). Setup: ProviderContainer com override do sharedPreferencesProvider; usar cartProvider.notifier.addItem para popular carrinho.
  </action>
  <verify>
    <automated>flutter test test/providers/filtered_products_provider_test.dart test/providers/comparison_results_provider_test.dart</automated>
    <automated>flutter analyze lib/core/providers/filtered_products_provider.dart lib/core/providers/comparison_results_provider.dart</automated>
    <automated>flutter test</automated>
  </verify>
  <acceptance_criteria>
    - Os 2 arquivos de teste novos passam (exit code 0)
    - flutter test suite completa exit code 0 (toda Phase 1 + Phase 2 + Phase 3 Wave 1)
    - flutter analyze sobre os 2 arquivos reporta 0 errors
    - grep "class SupermarketTotal" em lib/core/providers/comparison_results_provider.dart retorna 1 match
    - grep "vehicle.fuelEfficiencyKmPerLiter" em lib/core/providers/comparison_results_provider.dart retorna 1 match (NÃO fuelEfficiency sozinho)
    - grep -v '^//' lib/core/providers/comparison_results_provider.dart | grep -c "fuelEfficiency[^K]" retorna 0 (campo errado não aparece em linha de código)
    - grep "\\.sort((a, b) => a\\.totalCost\\.compareTo(b\\.totalCost))" em lib/core/providers/comparison_results_provider.dart retorna 1 match
    - grep "ref.watch(productsProvider)" em lib/core/providers/filtered_products_provider.dart retorna 1 match
  </acceptance_criteria>
  <done>filteredProductsProvider e comparisonResultsProvider implementados com SupermarketTotal; 2 testes verdes (filtered cobre HOME-02; comparison cobre COMP-01/02/03); suite completa flutter test verde.</done>
</task>

</tasks>

<verification>
- flutter analyze lib/ sem erros
- flutter test (suite completa) verde — incluindo todos os novos testes em test/providers/
- Nenhum constructor Product em MockData.products foi alterado (assertar via git diff em mock_data.dart: linhas 14-127 inalteradas exceto pelas adições novas em outras posições)
- Campo correto fuelEfficiencyKmPerLiter usado em vehicle_provider e comparison_results_provider
</verification>

<success_criteria>
- 11 arquivos de produção criados/estendidos compilam (flutter analyze 0 erros)
- 6 arquivos de teste novos em test/providers/ verdes (Wave 0 da VALIDATION.md fechado para tudo que não é UI)
- Cobertura de requisitos: HOME-02 (filteredProductsProvider), HOME-04 (favoritesProvider já existe — re-validar via teste herdado), SHOP-02 (cart increment/decrement), SHOP-04 (fuel toggle SharedPrefs), COMP-01/02/03 (4 supermercados, ordenação, breakdown produtos+fuel)
- Suite total flutter test verde para destravar Plan 02 e Plan 03
</success_criteria>

<output>
Criar .planning/phases/03-core-shopping-loop/03-01-SUMMARY.md ao finalizar todas as tasks.
</output>
