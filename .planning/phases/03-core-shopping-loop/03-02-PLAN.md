---
phase: 03-core-shopping-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - lib/routing/app_routes.dart
  - lib/routing/app_router.dart
  - lib/features/price_registration/presentation/scanner_screen.dart
  - lib/features/home/presentation/home_screen.dart
  - lib/features/home/presentation/product_card_grid.dart
  - lib/features/home/presentation/product_card_list.dart
  - lib/features/home/presentation/product_detail_screen.dart
  - lib/features/home/presentation/nutritional_info_bottom_sheet.dart
  - test/features/home/home_screen_test.dart
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05
  - HOME-06
  - HOME-07
must_haves:
  truths:
    - "Toque no card de produto na Home empurra /home/product/:productId e ProductDetailScreen renderiza o produto correto"
    - "Toggle grid/list alterna entre SliverGrid 2-colunas e SliverList renderizando os mesmos N itens filtrados"
    - "Digitar texto no campo de busca filtra a grid/list reativamente sem debounce"
    - "Toque na estrela do card alterna estado favorito e a cor do ícone reflete (success quando ativo; textSecondary quando inativo)"
    - "FAB com LucideIcons.scanLine chama context.go('/scanner') e o bottom nav destaca o tab índice 2"
    - "Badge JA (iniciais do usuário) no leading do SliverAppBar é touch-target que empurra /profile"
    - "ProductDetailScreen exibe 4 linhas de preço (Bistek, Giassi, Angeloni, Atacadão) com a linha de menor preço destacada (fundo primary 10% e texto primary)"
    - "Botão 'Adicionar ao Carrinho' chama cartProvider.notifier.addItem com CartItem derivado do Product (unitPrice=product.averagePrice)"
    - "Botão 'Ver Tabela Nutricional' abre NutritionalInfoBottomSheet (DraggableScrollableSheet) com 6 nutrientes + servingSize"
    - "Tab 2 do bottom nav exibe label 'Scanner' e ícone LucideIcons.scanLine; PriceComparisonScreen NÃO é mais rota raiz de tab2"
  artifacts:
    - path: "lib/routing/app_routes.dart"
      provides: "AppRoutes.scanner ('/scanner'), AppRoutes.productDetailPath(id), AppRoutes.comparisonResult ('/shopping-list/comparison')"
      contains: "static const String scanner"
    - path: "lib/routing/app_router.dart"
      provides: "Subrota /home/product/:productId em tab0; ScannerScreen como rota raiz de tab2 ('/scanner'); subrota /shopping-list/comparison declarada em tab1 (consumida em Plan 03); tab2 NavigationDestination label='Scanner' + LucideIcons.scanLine"
      contains: "path: 'product/:productId'"
    - path: "lib/features/price_registration/presentation/scanner_screen.dart"
      provides: "ScannerScreen placeholder StatelessWidget mostrando ícone scanLine + texto 'Cadastro de notas — em breve'"
      contains: "class ScannerScreen"
    - path: "lib/features/home/presentation/home_screen.dart"
      provides: "HomeScreen redesenhada com CustomScrollView (SliverAppBar pinned com badge JA + grid/list toggle, SliverPersistentHeader pinned com TextField de busca, SliverGrid ou SliverList condicional, FAB)"
      contains: "CustomScrollView"
    - path: "lib/features/home/presentation/product_card_grid.dart"
      provides: "ProductCardGrid ConsumerWidget com Image.network + nome + marca + tag chip + averagePrice + estrela toggle; onTap empurra productDetailPath"
      contains: "class ProductCardGrid"
    - path: "lib/features/home/presentation/product_card_list.dart"
      provides: "ProductCardList ConsumerWidget com layout horizontal (Row): Image.network 64×64 + info coluna + estrela; mesmo onTap"
      contains: "class ProductCardList"
    - path: "lib/features/home/presentation/product_detail_screen.dart"
      provides: "ProductDetailScreen ConsumerWidget recebendo productId via constructor; layout per UI-SPEC; FilledButton 'Adicionar ao Carrinho'; OutlinedButton 'Ver Tabela Nutricional'"
      contains: "class ProductDetailScreen"
    - path: "lib/features/home/presentation/nutritional_info_bottom_sheet.dart"
      provides: "showNutritionalInfoBottomSheet(context, NutritionalInfo) ou widget NutritionalInfoBottomSheet (DraggableScrollableSheet com 6 _NutriRow + handle + título + servingSize + Fechar Tabela)"
      contains: "DraggableScrollableSheet"
    - path: "test/features/home/home_screen_test.dart"
      provides: "Widget tests cobrindo HOME-01 (toggle grid/list), HOME-03 (cards renderizam nome/marca/averagePrice formatado), HOME-07 (tap dispara navegação — mock via GoRouter inspecionando route stack ou MockGoRouter)"
      contains: "ProductCardGrid"
  key_links:
    - from: "lib/features/home/presentation/home_screen.dart"
      to: "filteredProductsProvider + viewModeProvider + searchQueryProvider + userNotifierProvider"
      via: "ref.watch em build(); ref.read em onChanged do TextField e onPressed dos toggles"
      pattern: "ref\\.watch\\(filteredProductsProvider\\)"
    - from: "lib/features/home/presentation/product_card_grid.dart"
      to: "favoritesProvider + AppRoutes.productDetailPath"
      via: "ref.watch(favoritesProvider.select((f) => f.contains(product.id))); context.push(AppRoutes.productDetailPath(product.id))"
      pattern: "context\\.push\\(AppRoutes\\.productDetailPath"
    - from: "lib/features/home/presentation/product_detail_screen.dart"
      to: "productsProvider + pricesProvider + cartProvider"
      via: "ref.watch para products+prices em build; ref.read(cartProvider.notifier).addItem em onPressed"
      pattern: "cartProvider\\.notifier\\)\\.addItem"
    - from: "lib/routing/app_router.dart tab0"
      to: "ProductDetailScreen"
      via: "GoRoute aninhada com path relativo 'product/:productId' lendo pathParameters['productId']"
      pattern: "path: 'product/:productId'"
    - from: "lib/routing/app_router.dart tab2"
      to: "ScannerScreen"
      via: "GoRoute(path: AppRoutes.scanner, builder: ScannerScreen)"
      pattern: "AppRoutes\\.scanner"
---

<objective>
Implementar a fatia vertical Home: estender o roteamento (subrotas /home/product/:id e /shopping-list/comparison, renomeação tab2 para Scanner com ScannerScreen placeholder per D-04), redesenhar HomeScreen com CustomScrollView completo (SliverAppBar com badge JA + grid/list toggle per D-13, SliverPersistentHeader pinned com busca per D-12, grid/list condicional per HOME-01, FAB scanner per D-14/HOME-05), criar ProductCardGrid e ProductCardList (HOME-03/HOME-04), criar ProductDetailScreen com lookup de preços por supermercado e destaque do menor preço (D-08/D-09), criar NutritionalInfoBottomSheet via DraggableScrollableSheet (D-10), e cobrir o ciclo com widget test cobrindo HOME-01/03/07.

Purpose: Esta é a primeira fatia user-facing da Phase 3 — destrava a navegação ponta-a-ponta (Home → Detail → Cart) para o usuário e prepara a infraestrutura de roteamento que o Plan 03 consome (subrota /shopping-list/comparison).
Output: 1 arquivo de rotas estendido, 1 arquivo de router modificado, 1 ScannerScreen placeholder, 5 arquivos de feature/home (4 novos + 1 redesenhado), 1 arquivo de teste verde.
</objective>

<execution_context>
@C:/Users/Jose Augusto/Desktop/Unesc/Fase 5/Desenvolvimento Mobile/gsd/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jose Augusto/Desktop/Unesc/Fase 5/Desenvolvimento Mobile/gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/03-core-shopping-loop/03-CONTEXT.md
@.planning/phases/03-core-shopping-loop/03-RESEARCH.md
@.planning/phases/03-core-shopping-loop/03-PATTERNS.md
@.planning/phases/03-core-shopping-loop/03-UI-SPEC.md
@.planning/phases/03-core-shopping-loop/03-01-SUMMARY.md

# Source-of-truth files (read before modifying)
@lib/routing/app_routes.dart
@lib/routing/app_router.dart
@lib/features/home/presentation/home_screen.dart
@lib/features/auth/presentation/login_screen.dart
@lib/core/providers/favorites_notifier.dart
@lib/core/providers/user_notifier.dart
@lib/core/constants/app_colors.dart
@lib/core/constants/app_sizes.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend routing (subrotas + ScannerScreen placeholder + tab2 rename)</name>
  <files>lib/routing/app_routes.dart, lib/routing/app_router.dart, lib/features/price_registration/presentation/scanner_screen.dart</files>
  <read_first>
    - lib/routing/app_routes.dart (estado atual — não alterar valores existentes)
    - lib/routing/app_router.dart (estrutura StatefulShellRoute + 5 branches existentes — alvo das modificações)
    - .planning/phases/03-core-shopping-loop/03-RESEARCH.md (Pattern 6 subrotas, Pitfall 1 path relativo SEM /, Pitfall 5 context.go vs push, Pitfall 8 nome shoppingList vs lista)
    - .planning/phases/03-core-shopping-loop/03-PATTERNS.md (seção lib/routing/app_router.dart com router modificado)
    - .planning/phases/03-core-shopping-loop/03-CONTEXT.md (D-04 tab2 = Scanner, D-08 rota produto, D-14 FAB scanLine)
    - lib/features/price_comparison/presentation/price_comparison_screen.dart (continua existindo como classe — apenas move de tab2 root para subrota de tab1; consumido em Plan 03)
  </read_first>
  <action>
    Estenda lib/routing/app_routes.dart adicionando 3 constantes sem alterar as existentes: static const String scanner = '/scanner'; static const String productDetailPattern = '/home/product/:productId'; static const String comparisonResult = '/shopping-list/comparison'; e um helper static String productDetailPath(String productId) => '/home/product/$productId';. NÃO altere o valor de shoppingList (continua '/shopping-list'). NÃO remova AppRoutes.comparison (será descontinuado naturalmente quando tab2 deixar de usá-lo).

    Crie lib/features/price_registration/presentation/scanner_screen.dart com class ScannerScreen extends StatelessWidget: Scaffold com backgroundColor AppColors.background, AppBar simples (title 'Scanner'), body Center com Column contendo Icon(LucideIcons.scanLine, size: 64, color: AppColors.textSecondary), SizedBox(height: AppSizes.spacingM), Text('Cadastro de notas — em breve', style: Theme.of(context).textTheme.bodyMedium com color textSecondary). Placeholder simples — não implementa o fluxo real (Phase 5).

    Modifique lib/routing/app_router.dart per RESEARCH.md Pattern 6 e Pitfall 1: (1) Em StatefulShellBranch _tab0Key, adicione no GoRoute existente (path: AppRoutes.home) uma lista routes: contendo GoRoute(path: 'product/:productId', builder: (context, state) => ProductDetailScreen(productId: state.pathParameters['productId']!)). Path RELATIVO sem '/' inicial. (2) Em StatefulShellBranch _tab1Key, no GoRoute existente (path: AppRoutes.shoppingList) adicione lista routes: com GoRoute(path: 'comparison', builder: (_, __) => const PriceComparisonScreen()) — também relativo. (3) Em StatefulShellBranch _tab2Key, substitua path: AppRoutes.comparison por path: AppRoutes.scanner e builder de PriceComparisonScreen por const ScannerScreen() — ScannerScreen passa a ser a tela raiz do tab2. (4) Na NavigationBar.destinations, modifique o terceiro NavigationDestination: icon de LucideIcons.barChart2 para LucideIcons.scanLine, label de 'Comparar' para 'Scanner'. (5) Atualize imports: adicione import '../features/home/presentation/product_detail_screen.dart' e import '../features/price_registration/presentation/scanner_screen.dart'; mantenha import existente de price_comparison_screen.dart (continua usado em tab1 subrota).
  </action>
  <verify>
    <automated>flutter analyze lib/routing/ lib/features/price_registration/presentation/scanner_screen.dart</automated>
    <automated>flutter test test/routing/</automated>
  </verify>
  <acceptance_criteria>
    - flutter analyze sobre os 3 arquivos modificados/novos reporta 0 errors (warnings aceitáveis se forem de TODO; erros não)
    - grep "static const String scanner = '/scanner'" em lib/routing/app_routes.dart retorna 1 match
    - grep "productDetailPath" em lib/routing/app_routes.dart retorna ao menos 1 match
    - grep "path: 'product/:productId'" em lib/routing/app_router.dart retorna 1 match (sem barra inicial)
    - grep "path: 'comparison'" em lib/routing/app_router.dart retorna 1 match (subrota em tab1)
    - grep "label: 'Scanner'" em lib/routing/app_router.dart retorna 1 match
    - grep "LucideIcons.scanLine" em lib/routing/app_router.dart retorna ao menos 1 match
    - grep "label: 'Comparar'" em lib/routing/app_router.dart retorna 0 matches (renomeado)
    - grep "class ScannerScreen" em lib/features/price_registration/presentation/scanner_screen.dart retorna 1 match
    - Testes herdados em test/routing/ continuam passando (a re-organização de subrotas não deve quebrar router_notifier_test.dart nem outros)
  </acceptance_criteria>
  <done>3 rotas adicionadas; ScannerScreen placeholder criado; router modificado com 2 subrotas (tab0 e tab1) e tab2 renomeado para Scanner; flutter analyze limpo; testes de routing herdados verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HomeScreen redesign + ProductCardGrid + ProductCardList + widget test</name>
  <files>lib/features/home/presentation/home_screen.dart, lib/features/home/presentation/product_card_grid.dart, lib/features/home/presentation/product_card_list.dart, test/features/home/home_screen_test.dart</files>
  <read_first>
    - lib/features/home/presentation/home_screen.dart (estado atual stub — alvo do redesign)
    - lib/features/auth/presentation/login_screen.dart (padrão ConsumerStatefulWidget com TextEditingController + dispose + ref.read em handlers + glassmorphic Container; FilledButton style canônico)
    - lib/core/providers/favorites_notifier.dart (assinatura .toggle(productId), .isFavorite, exportação favoritesProvider)
    - lib/core/providers/user_notifier.dart (formato para extrair name do user logado para badge JA)
    - lib/core/constants/app_colors.dart e lib/core/constants/app_sizes.dart (tokens — usar SEMPRE, nunca literais)
    - .planning/phases/03-core-shopping-loop/03-UI-SPEC.md (seções Copywriting/HomeScreen, Layout/HomeScreen, Interaction States/HomeScreen, Accessibility)
    - .planning/phases/03-core-shopping-loop/03-RESEARCH.md (Pattern 7 SliverPersistentHeader com delegate + ColoredBox; Pattern 8 Image.network errorBuilder; Pattern 9 FAB context.go; Pattern 13 iniciais)
    - .planning/phases/03-core-shopping-loop/03-PATTERNS.md (seções HomeScreen + ProductCardGrid + ProductCardList)
  </read_first>
  <behavior>
    - HomeScreen ConsumerStatefulWidget com _searchController (TextEditingController) disposto no dispose()
    - SliverAppBar.pinned: leading = GestureDetector circular 36×36 com texto das iniciais do usuário (ex: 'JA'), fundo AppColors.primary, texto AppColors.background, onTap context.push(AppRoutes.profile); title Text('Home'); actions [IconButton(LucideIcons.layoutGrid, tooltip 'Exibir em grade', onPressed ref.read(viewModeProvider.notifier).setGrid()), IconButton(LucideIcons.list, tooltip 'Exibir em lista', onPressed setList())]; o ícone do viewMode ativo usa AppColors.primary, o inativo AppColors.textSecondary
    - SliverPersistentHeader.pinned com delegate customizado (minExtent==maxExtent==64) contendo ColoredBox(color: AppColors.background) wrapping TextField com hintText 'Buscar produtos...', prefixIcon LucideIcons.search, fillColor AppColors.surfaceElevated, focusedBorder primary, onChanged chama ref.read(searchQueryProvider.notifier).update(value)
    - Corpo: ref.watch(viewModeProvider) decide se renderiza SliverPadding(SliverGrid(crossAxisCount: 2, mainAxisSpacing: spacingS, crossAxisSpacing: spacingS, childAspectRatio: 0.72)) com ProductCardGrid para cada filteredProducts, OU SliverList com ProductCardList; em qualquer caso usar ref.watch(filteredProductsProvider) como fonte
    - Estado vazio (filteredProducts empty E searchQuery não vazio): SliverFillRemaining com Center exibindo 'Nenhum produto encontrado para "$query"'
    - FloatingActionButton com LucideIcons.scanLine, backgroundColor primary, foregroundColor background, tooltip 'Escanear nota fiscal', onPressed: context.go(AppRoutes.scanner) — context.go (NÃO push) per Pitfall 5
    - ProductCardGrid ConsumerWidget recebendo final Product product: Container glassmorphic (color surface.withValues(alpha: 0.7), border white.withValues(0.1) 1px, radiusL); Column com Expanded(Image.network(product.imageUrl, fit: BoxFit.contain, errorBuilder retornando Column centralizada com Icon(LucideIcons.packageOpen, 48, textSecondary) + 'Imagem indisponível')) + Padding com Text(product.name titleMedium textMain), Text(product.brand bodySmall textSecondary), Text(formatBRL(product.averagePrice) titleMedium semibold); estrela top-right via Stack ou Align — IconButton(isFav ? LucideIcons.star (solid se disponível na 0.257.0, senão usar star + color success) : LucideIcons.star, color: isFav ? AppColors.success : AppColors.textSecondary, onPressed ref.read(favoritesProvider.notifier).toggle(product.id)); GestureDetector ou InkWell em volta do card chama context.push(AppRoutes.productDetailPath(product.id))
    - ProductCardList ConsumerWidget: layout horizontal (Row) com Image.network 64×64 à esquerda + Column(name + brand + averagePrice) Expanded + estrela à direita; mesmas propriedades de favorito e onTap; radiusM (12.0) em vez de radiusL
    - Widget test usa ProviderScope com overrides de sharedPreferencesProvider (mock vazio), monta HomeScreen dentro de MaterialApp.router (ou MaterialApp + Scaffold simulado dependendo da viabilidade) e assertar: (a) com viewMode==grid renderiza ao menos 1 ProductCardGrid; (b) ao bombear ref.read(viewModeProvider.notifier).setList() renderiza ProductCardList; (c) com searchQuery 'leite' o número de cards renderizados <= total; (d) toque na estrela alterna favoritesProvider (ler estado via container.read).
  </behavior>
  <action>
    Redesenhe lib/features/home/presentation/home_screen.dart per UI-SPEC seção Layout/HomeScreen e Copywriting/HomeScreen. Use ConsumerStatefulWidget. Imports: flutter/material, flutter_riverpod, go_router, lucide_icons, intl (NumberFormat), AppColors/AppSizes, filteredProductsProvider, searchQueryProvider, viewModeProvider, userNotifierProvider (para iniciais — fallback 'JA' caso user==null), AppRoutes. Implemente: _HomeScreenState com _searchController; dispose; helper _initials(String name) per Pattern 13 retornando 2 letras maiúsculas; _StickySearchBarDelegate como classe private dentro do arquivo (minExtent=maxExtent=64, child wrapped em ColoredBox(AppColors.background) per Pitfall 2); build() retorna Scaffold(backgroundColor: AppColors.background, body: CustomScrollView com slivers conforme behavior, floatingActionButton: FAB scanLine que context.go(AppRoutes.scanner)). ref.watch APENAS em build; ref.read em onChanged/onPressed.

    Crie lib/features/home/presentation/product_card_grid.dart como ConsumerWidget recebendo Product product no constructor. Use favoritesProvider.select((favs) => favs.contains(product.id)) para evitar rebuilds desnecessários. Layout per UI-SPEC: glassmorphic Container, Image.network com errorBuilder LucideIcons.packageOpen, info abaixo, estrela top-right. onTap (envolvendo o Container) chama context.push(AppRoutes.productDetailPath(product.id)). Formatação BRL via NumberFormat.currency(locale: 'pt_BR', symbol: 'R\$', decimalDigits: 2). NÃO use withOpacity, NÃO use literais de cor ou tamanho.

    Crie lib/features/home/presentation/product_card_list.dart como ConsumerWidget seguindo o mesmo padrão de ProductCardGrid, porém com layout horizontal (Row) e radiusM. Imagem 64×64 à esquerda; Column(name/brand/price) Expanded; estrela à direita. Mesmo onTap.

    Crie test/features/home/home_screen_test.dart cobrindo HOME-01/03/07 conforme behavior. Use SharedPreferences.setMockInitialValues({}), SharedPreferences.getInstance(), ProviderScope(overrides: [sharedPreferencesProvider.overrideWithValue(prefs)]) wrapping MaterialApp simples com home: HomeScreen(). Para HOME-07 (navegação) basta assertar que o GestureDetector/InkWell do card tem onTap não-null (a navegação concreta via router pode ser testada manualmente em human-check do Plan 03). Para HOME-01 alternar viewMode via container.read(viewModeProvider.notifier).setList() e pumpAndSettle, depois find.byType(ProductCardList) > 0 e find.byType(ProductCardGrid) == 0.
  </action>
  <verify>
    <automated>flutter analyze lib/features/home/presentation/</automated>
    <automated>flutter test test/features/home/home_screen_test.dart</automated>
    <automated>flutter test</automated>
  </verify>
  <acceptance_criteria>
    - flutter analyze sobre lib/features/home/presentation/ reporta 0 errors
    - flutter test test/features/home/home_screen_test.dart exit code 0
    - flutter test (suite completa) exit code 0
    - grep "class ProductCardGrid extends ConsumerWidget" em lib/features/home/presentation/product_card_grid.dart retorna 1 match
    - grep "class ProductCardList extends ConsumerWidget" em lib/features/home/presentation/product_card_list.dart retorna 1 match
    - grep "SliverPersistentHeader" em lib/features/home/presentation/home_screen.dart retorna ao menos 1 match
    - grep "ColoredBox" em lib/features/home/presentation/home_screen.dart retorna ao menos 1 match (Pitfall 2 fundo opaco no delegate)
    - grep "context.go(AppRoutes.scanner)" em lib/features/home/presentation/home_screen.dart retorna 1 match (não push)
    - grep "context.push(AppRoutes.productDetailPath" em lib/features/home/presentation/product_card_grid.dart retorna 1 match
    - grep "context.push(AppRoutes.productDetailPath" em lib/features/home/presentation/product_card_list.dart retorna 1 match
    - grep -v '^//' lib/features/home/presentation/home_screen.dart | grep -c "withOpacity" retorna 0
    - grep -v '^//' lib/features/home/presentation/product_card_grid.dart | grep -c "withOpacity" retorna 0
  </acceptance_criteria>
  <done>HomeScreen redesenhada com SliverAppBar+busca+grid/list+FAB; ProductCardGrid e ProductCardList criados; HOME-01/02/03/04/05/06/07 cobertos por código; widget test verde; flutter test suite verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ProductDetailScreen + NutritionalInfoBottomSheet (preços por supermercado + add ao carrinho + nutricional)</name>
  <files>lib/features/home/presentation/product_detail_screen.dart, lib/features/home/presentation/nutritional_info_bottom_sheet.dart</files>
  <read_first>
    - lib/features/profile/domain/product.dart (após Task 1 do Plan 01 — campos ean/subcategory/department/nutritionalInfo disponíveis)
    - lib/features/shopping_list/domain/cart_item.dart (constructor required — addItem espera CartItem completo)
    - lib/core/providers/cart_notifier.dart (addItem incrementa se já existe per código; chamamos sempre addItem(...) novo CartItem)
    - .planning/phases/03-core-shopping-loop/03-UI-SPEC.md (seções Copywriting/ProductDetailScreen e Layout/ProductDetailScreen e Layout/NutritionalInfoBottomSheet)
    - .planning/phases/03-core-shopping-loop/03-CONTEXT.md (D-08, D-09, D-11)
    - .planning/phases/03-core-shopping-loop/03-RESEARCH.md (Pattern 8 Image.network errorBuilder, Pattern 11 DraggableScrollableSheet + scrollController CRÍTICO, Pattern 12 BRL)
    - .planning/phases/03-core-shopping-loop/03-PATTERNS.md (seções product_detail_screen e nutritional_info_bottom_sheet)
  </read_first>
  <behavior>
    - ProductDetailScreen ConsumerWidget recebe final String productId no constructor
    - Busca o Product via ref.watch(productsProvider).firstWhere((p) => p.id == productId); se não encontrado, exibir Scaffold com mensagem 'Produto não encontrado' (defensivo)
    - Renderiza imagem (200px altura) com Image.network + errorBuilder Column(packageOpen 64px + 'Imagem indisponível')
    - Bloco de metadados: 4 linhas (Departamento, Categoria, Subcategoria, EAN) — cada uma _MetadataRow(label, value) com label bodySmall textSecondary e value bodyMedium textMain; só renderizar valor se não-vazio (ean default '' pula a linha)
    - Seção 'Preços por Supermercado' (titleMedium): para cada um dos 4 supermercados em MockData.supermarketDistances.keys (ou prices[productId].keys), renderiza linha com nome do supermercado + valor formatBRL(price); a linha com menor preço (calculado via prices[productId].entries.reduce min) recebe fundo AppColors.primary.withValues(alpha: 0.1), texto do preço em AppColors.primary com weight 600, e chip 'Melhor preço' à direita
    - FilledButton 'Adicionar ao Carrinho' full-width (style backgroundColor primary, foreground background, padding spacingM, radiusL): onPressed chama ref.read(cartProvider.notifier).addItem(CartItem(productId: product.id, productName: product.name, brand: product.brand, imageUrl: product.imageUrl, quantity: 1, unitPrice: product.averagePrice)); seguido de ScaffoldMessenger SnackBar 'Adicionado ao carrinho' (feedback)
    - OutlinedButton.icon 'Ver Tabela Nutricional' (ícone LucideIcons.info, borda textSecondary): onPressed abre showModalBottomSheet(context, isScrollControlled: true, backgroundColor: Colors.transparent, builder: ... NutritionalInfoBottomSheet(info: product.nutritionalInfo!)). Se product.nutritionalInfo == null, o botão fica disabled (onPressed: null)
    - NutritionalInfoBottomSheet recebe final NutritionalInfo info no constructor (StatelessWidget); retorna DraggableScrollableSheet(initialChildSize: 0.5, maxChildSize: 0.85, minChildSize: 0.3, builder: (_, scrollController) => Container glassmorphic com BorderRadius vertical top radiusXL, child ListView com handle (32×4 textSecondary 0.4 radiusS) + título 'Informação Nutricional' headlineSmall + 'Porção: ${info.servingSize}' bodySmall textSecondary + 6 _NutriRow (Valor Energético/Proteínas/Carboidratos/Gorduras Totais/Fibra Alimentar/Sódio com valor + unidade) + TextButton 'Fechar Tabela' Navigator.pop)
    - CRÍTICO: o scrollController do DraggableScrollableSheet DEVE ser passado para o ListView interno (Pattern 11) — sem isso o scroll do sheet não funciona
  </behavior>
  <action>
    Crie lib/features/home/presentation/product_detail_screen.dart per UI-SPEC Layout/ProductDetailScreen. ConsumerWidget. Imports: flutter/material, flutter_riverpod, go_router, lucide_icons, intl, AppColors/AppSizes, productsProvider, pricesProvider, cartProvider, AppRoutes, Product, CartItem, nutritional_info_bottom_sheet.dart. Helper formatBRL inline ou top-level (NumberFormat). Helper _MetadataRow(String label, String value) StatelessWidget. Helper _PriceRow(String supermarket, double price, bool isLowest) StatelessWidget. Em build: derivar product via firstWhere com guard; derivar productPrices = ref.watch(pricesProvider)[productId] ?? {}; derivar lowestSupermarket = productPrices.entries.fold(null, (a, b) => a == null || b.value < a.value ? b : a)?.key. Layout: Scaffold(backgroundColor background, appBar AppBar(title 'Detalhes do Produto'), body SingleChildScrollView(Column(image + Padding spacingM(Column nome+marca+metadados+seção preços+OutlinedButton nutricional+FilledButton carrinho)))). onPressed do FilledButton: ref.read(cartProvider.notifier).addItem(...) + ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text('Adicionado ao carrinho'))).

    Crie lib/features/home/presentation/nutritional_info_bottom_sheet.dart com StatelessWidget NutritionalInfoBottomSheet recebendo final NutritionalInfo info. Implementação per UI-SPEC Layout/NutritionalInfoBottomSheet e Pattern 11 RESEARCH.md. Helper privado _NutriRow(String label, double value, String unit) renderiza Row(Expanded(Text(label bodyMedium)) + Text('$value $unit' bodyMedium semibold)). DraggableScrollableSheet builder retorna Container(decoration: BoxDecoration(color: AppColors.surface, borderRadius: BorderRadius.vertical(top: Radius.circular(AppSizes.radiusXL)), border: Border.all(color: Colors.white.withValues(alpha: 0.1)))), child: ListView(controller: scrollController, ...). NÃO esqueça de passar scrollController para o ListView.

    Adicione função top-level (opcional) showNutritionalInfoBottomSheet(BuildContext context, NutritionalInfo info) que chama showModalBottomSheet(...) — ou deixe a chamada inline no ProductDetailScreen. Qualquer abordagem é aceitável desde que o sheet renderize.
  </action>
  <verify>
    <automated>flutter analyze lib/features/home/presentation/product_detail_screen.dart lib/features/home/presentation/nutritional_info_bottom_sheet.dart</automated>
    <automated>flutter test</automated>
  </verify>
  <acceptance_criteria>
    - flutter analyze sobre os 2 arquivos reporta 0 errors
    - flutter test (suite completa) exit code 0 (não regrediu)
    - grep "class ProductDetailScreen extends ConsumerWidget" em lib/features/home/presentation/product_detail_screen.dart retorna 1 match
    - grep "required this.productId" em lib/features/home/presentation/product_detail_screen.dart retorna 1 match
    - grep "ref.read(cartProvider.notifier).addItem" em lib/features/home/presentation/product_detail_screen.dart retorna 1 match
    - grep "showModalBottomSheet" em lib/features/home/presentation/product_detail_screen.dart retorna 1 match
    - grep "DraggableScrollableSheet" em lib/features/home/presentation/nutritional_info_bottom_sheet.dart retorna 1 match
    - grep "controller: scrollController" em lib/features/home/presentation/nutritional_info_bottom_sheet.dart retorna 1 match (Pattern 11 crítico)
    - grep "LucideIcons.packageOpen" em lib/features/home/presentation/product_detail_screen.dart retorna 1 match (errorBuilder)
    - grep -v '^//' lib/features/home/presentation/product_detail_screen.dart | grep -c "withOpacity" retorna 0
  </acceptance_criteria>
  <done>ProductDetailScreen renderiza imagem+metadados+preços por supermercado com destaque do menor+CTAs nutricional/carrinho; NutritionalInfoBottomSheet renderiza tabela completa com scrollController correto; flutter test suite verde.</done>
</task>

</tasks>

<verification>
- flutter analyze lib/ sem erros
- flutter test suite verde
- Manualmente (executor pode confirmar via flutter run em smoke): app abre, login leva à Home, Home mostra 12 produtos em grid, toggle para lista funciona, busca filtra, tap em card abre ProductDetailScreen, tap em FAB navega para tab Scanner (bottom nav destaca tab 2 com label 'Scanner'); ProductDetailScreen mostra 4 preços com destaque do menor, botão 'Adicionar ao Carrinho' incrementa o cartProvider, botão 'Ver Tabela Nutricional' abre bottom sheet
- A subrota /shopping-list/comparison existe no router (Plan 03 vai navegar para ela)
</verification>

<success_criteria>
- HOME-01/02/03/04/05/06/07 todos cobertos por código e UI funcional
- D-04/D-08/D-09/D-10/D-11/D-12/D-13/D-14 implementados conforme decisões
- Tab 2 renomeado para Scanner com ScannerScreen placeholder; subrotas /home/product/:id e /shopping-list/comparison declaradas
- Widget test test/features/home/home_screen_test.dart verde cobrindo HOME-01/03 (e 07 via onTap presença)
- Suite completa flutter test verde — pronto para Plan 03
</success_criteria>

<output>
Criar .planning/phases/03-core-shopping-loop/03-02-SUMMARY.md ao finalizar todas as tasks.
</output>
